# Optional: point the frontend at a non-default FastAPI backend during dev.
# If unset, Vite dev server proxies /api to http://127.0.0.1:8000 (see vite.config.ts).
VITE_API_BASE=http://127.0.0.1:8000
