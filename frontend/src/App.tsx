// Legacy starter + framework placeholder (completely inert).
// Not used by the production shell (main.tsx → AppRouter → AppShell).
// Contains no forbidden production display terms and produces no lint unused-var issues.
import './App.css'

function App() {
  return (
    <div style={{ padding: 16, opacity: 0.6, fontSize: 12 }}>
      Legacy starter placeholder (unused by production AppRouter/AppShell)
    </div>
  )
}

export default App
