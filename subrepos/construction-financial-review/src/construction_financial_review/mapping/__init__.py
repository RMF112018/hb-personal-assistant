"""Mapping / owner SOV scope crosswalk tooling (stages 3 and 4).

Includes the verbatim mapping-discrepancy workpaper generator, the crosswalk loader/expander
(``crosswalk.py``), and the authoritative crosswalk validator
(``validate_owner_sov_scope_crosswalk.py``).
"""
