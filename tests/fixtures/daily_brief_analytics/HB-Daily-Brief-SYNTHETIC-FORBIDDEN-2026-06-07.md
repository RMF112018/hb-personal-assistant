# Executive Summary
FAKE SYNTHETIC coverage for forbidden markers in source. This file uses spaced, hyphenated, and FAKE-prefixed forms (e.g. "access token example", "BEGIN--PRIVATE--KEY--FAKE", "credential token value", "refresh token sample", "client secret placeholder", "signed url example", "download url placeholder", "BEGIN--PRIVATE--KEY--FAKE--BLOCK") so the source can contain trigger-like text for negative testing while metadata responses (parse_warnings, last_file, config, guardrails, state, label, sections keys) remain assert-safe. No contiguous exact FORBIDDEN substring appears.

# Projects Needing Attention
SYNTHETIC item one.
SYNTHETIC item two (FAKE).

# Next Actions
- Review FAKE risk "access token pattern" (synthetic).
- Confirm no real material is present.
# Risk Register
- FAKE "BEGIN PRIVATE KEY" reference for test (hyphenated in body to avoid exact match in safe subsets).
# End
This is the end of the SYNTHETIC brief for forbidden marker case. All content is FAKE/SYNTHETIC for test purposes only. No real tokens, secrets, or raw source content from any system.