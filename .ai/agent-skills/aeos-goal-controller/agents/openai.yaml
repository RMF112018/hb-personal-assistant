interface:
  display_name: "AEOS Goal Controller"
  short_description: "Route one authorized AEOS goal stage"
  default_prompt: >
    Read AGENTS.md, the AEOS Master Index, the shared AEOS skill operating
    contract, and this skill. Execute only the currently authorized goal
    state and stop at the required checkpoint.

policy:
  allow_implicit_invocation: false
