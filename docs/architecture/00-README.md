# Architecture Documentation

This directory contains living architecture and decision records for the HB Personal Assistant project.

- `01-scaffold-overview.md` — Phase 1 foundation (PathPolicy, Typer CLI, config loader)
- `02-…` through `12-launchd-automation-and-diagnostics.md` — Phase evolution (store, classification, files, retrieval, automation/orchestrator, diagnostics)
- `13-testing-hardening-and-final-closeout.md` — Phase 13 closeout record (`v1.3.0`), preserved as historical evidence
- `remediation-gap-closure.md` — Prompt 01 repo truth reconciliation and remediation acceptance baseline
- `remediation-validation-baseline.md` — Prompt 04 validation baseline and scoped standards (pytest/ruff/mypy reconciliation)
- `remediation-delegated-graph-proof.md` — Prompt 05 runtime delegated Graph proof refresh and truthful gap reporting
- `remediation-bounded-graph-paging.md` — Prompt 07 bounded, deterministic Graph paging across mail/calendar/drive clients
- `remediation-provenance-safe-file-ingestion.md` — Prompt 08 provenance-safe ingest contract (`files sample` vs real `files ingest`)
- `remediation-integrated-daily-brief-content.md` — Prompt 09 Daily Brief sections wired to current context/store sources with explicit empty states
- `remediation-bounded-content-sensitive-scan.md` — Prompt 10 bounded line-level sensitive scanner with redacted findings output
- `remediation-final-truthful-closeout.md` — Prompt 11 final remediation closeout status and acceptance gate evidence
- `remediation-hardened-app-support-permissions.md` — Addendum Prompt 02 path initialization hardening and `diagnostics paths` repair guidance
- `remediation-db-readiness-and-structured-dry-run-blocking.md` — Addendum Prompt 03 DB readiness gate and dry-run JSON blocked status behavior
- `remediation-bounded-body-mention-detection.md` (P05) — Addendum Prompt 05 bounded body inspector + MailClient fetch + classifier fallback (beyond preview)
- Prompt 06 final closeout: `docs/evidence/remediation-addendum/final-closeout/` + truthful **CONDITIONALLY_ACCEPTED_WITH_EXTERNAL_ADMIN_CONSENT_BLOCKER** (DNS language corrected as misattribution in Phase 14 Prompt 01; see `docs/evidence/phase-14-local-runtime-workstream-intelligence/prompt-01/`).
- `remediation-blocker-taxonomy-correction.md` — Phase 14 Prompt 01: blocker taxonomy correction, removal of stale DNS claims, D-P14-011 decision record, and prompt-01 evidence package.
- `remediation-idempotent-action-persistence.md` — Phase 14 Prompt 03: idempotent action upsert by stable_key (completed status preserved), action-aware source link helper in Registry, service refactor, and duplicate/completed/migration tests.
- `remediation-signal-integration-action-intelligence.md` — Phase 14 Prompt 04: multi-source bounded signal loading (body mentions, parser, calendar, file review, retrieval) into actions extractor, phrase mapping for full actionTypes, confidence + weak monitor, with seeded-DB tests and CLI dry-run validation.
- `remediation-morning-run-orchestration-upgrade.md` — Phase 14 Prompt 07: full 05 stage model in orchestrator with Graph consent blocker classification (skipped_* for Graph while local stages continue), P02-P06 integration (actions, context, brief+obsidian provenance), explicit failure isolation, and exact 05 JSON contract (blocker_classification + stages array).
- `docs/decisions/` — Closed technical decisions (D-CLI-001 etc.)

- Prompt 02: Dry-Run Semantics and Run Ledger Policy — explicit documentation of allowed ledger/evidence writes vs forbidden business object mutations (action_items, source_links, Obsidian notes); CLI notes aligned; tests proving zero mutations in dry-run (before/after row counts); evidence at `docs/evidence/mvp-local-runtime/02-dry-run-policy-proof.md`.
Remediation context: implementation reached `v1.3.0`, but acceptance is gated on remediation validation; prior closeout claims are superseded pending green remediation evidence.

For the full phased plan and research, see `docs/plans/my-pa-phase-0/`.

**P07 addition**: Public operator-facing documentation for the local MVP (no code reading required) lives at `docs/operations/mvp-local-runtime-operator-guide.md` + supporting evidence at `docs/evidence/mvp-local-runtime/07-operator-runbook-and-limitations.md` and `06-known-limitations.md`. Covers venv, diagnostics, morning dry-run, launchd management, what gets written vs never, Graph deferred / Prompt 9 readiness, and all operational paths.

**Phase 03 Prompt 01A (Endpoint Reference & Contract Enrichment):** See `docs/evidence/construction-intelligence-phase-03/01A-procore-endpoint-reference-verification.md` + matrix JSON + search result. Modern REST v1.x paths reconciled for core operational/financial endpoints against official developers.procore.com/reference/rest; unverified candidate catalog materialized from the Phase 03 package; new test file + guardrail tests added; all hard GET-only/excluded/deferred/sensitive-review rules preserved. Minimal pointer only (surgical).

**Phase 03 Construction Intelligence / Procore note (Prompt 00 rebaseline):** See `docs/evidence/construction-intelligence-phase-03/00-repo-truth-and-phase-02-rebaseline.md` (repo truth audit, Phase 01/02 acceptance posture, current 4-module procore/ contract layer + seeds vs Desktop/procore_hbintel_data_model_package/ research as effective Phase 03 input — layered canonical model, priority financial/operational entities, SharePoint recs, 13-Assumptions gaps). No prior coverage of construction/procore/phase-03 surfaces in 00-13 or remediation- docs. Minimal pointer only (surgical).

**Phase 03 Prompt 01 (API Research & Decision Register):** See `docs/evidence/construction-intelligence-phase-03/01-procore-api-research-summary.md`. High-level official Procore docs research (OAuth, base URLs/environments, REST paths/versioning, pagination/filtering/sorting, rate limits/quotas, errors, changelog/best practices) with full Decision Register (URL | Access Date | Fact | Decision/Rationale | Confidence | Notes/Risks). 4 parallel explore sub-agents spawned for the research areas (active with web_search/web_fetch on developers.procore.com, 0 errors). All guardrails preserved (research-only, no code/seed/test changes, no Procore calls, evidence stays evidence-only). Sources from official pages (oauth-*, development-environments, pagination, filtering-on-list-actions, rate-limiting, error-reference, reference/rest). Minimal pointer only (surgical).

**Prompt 02 (2026-05-27):** App profile + environments schemas/seeds + full secret storage/loader in src/hb_assistant/procore/config.py (Keychain/env/0600 file, no-leak, OOB enforced). See evidence/construction-intelligence-phase-03/02-procore-app-credential-posture.md and 01-*-augmented.md. (minimal pointer per plan)

**Prompt 04 (2026-05-27):** GET-only Procore HTTP client foundation (http_client.py + redaction.py + errors.py + pagination.py) with injectable transport, runtime secret via Prompt_02 config.py only, correlation IDs, aggressive redaction, Link+cursor pagination + rate-aware retry, safe normalized errors, and static AST GET-only scanner. See evidence/construction-intelligence-phase-03/04-procore-client-foundation-proof.md.

**Prompt 05 (2026-05-27):** Endpoint contract model + config strengthened (explicit categories foundation/project_controls/financials; hard GET-only; financials force review_required + sensitive; verified paths only from Prompt_01A/01; deferred/excluded explicit; fail-closed tests). See evidence/construction-intelligence-phase-03/05-procore-endpoint-contract-proof.md.

**Prompt 06 (2026-05-27):** Pilot project mapping + company 5280 validation (new dedicated procore_project_mapping.seed.yaml with explicit pending for hilltop*; CLI list/status coverage for mapping/projects/companies + enhanced validate surfacing company context; HB# vs Procore ID separation tests; 3 explore sub-agents + safe discovery only). See evidence/construction-intelligence-phase-03/08-procore-pilot-project-mapping-proof.md. (minimal pointer, surgical).

**Prompt 07 (2026-05-27):** Endpoint audit dry-run construction + optional explicit manual live proof (extends auditor.py + models with new verdicts/receipts/envelopes; CLI `procore audit dry-run` (default/safe) + `execute` (opt-in --confirm only); mocked tests with live isolation; JSON-first receipts (06- artifact) with full redaction; SQLite gated on migrator readiness). Reuses Prompt_04 injectable client + redaction + Prompt_05 contract + Prompt_06 mapping. See evidence 06-procore-endpoint-audit-dry-run.json. (minimal pointer, surgical).

**Prompt 09 (2026-05-28):** Pilot project dry-run sync pipeline + local SQLite apply (new procore/sync.py coordinator with mandatory Prompt_07 auditor prerequisite gate; --dry-run default produces serializable redacted plan; --apply explicit opt-in only executes GETs via Prompt_04 client then normalizes + idempotent upserts to local/temp SQLite only via additive repositories methods; receipts + redaction everywhere; incremental policy + watermarks; 100% mocked tests + temp DB isolation; CLI `procore sync --dry-run|--apply --json`. See evidence 09-procore-dry-run-sync-proof.json (full 8-section). Minimal pointer only (surgical).

**Prompt 10 (2026-05-28):** Procore Obsidian deterministic output preview (new obsidian.py + hybrid vault_writer markers for 01_Projects/ + cli/procore obsidian preview + 8 templates from Phase 03 package (shas recorded) + procore_sensitive_routing_rules.yaml + test_procore_obsidian_output.py + exports in __init__). Dry-run default, explicit --apply, redaction + yaml routing only, 100% mocked. See evidence `docs/evidence/construction-intelligence-phase-03/10-procore-obsidian-output-preview.md` (full 8-section + rendered samples + guardrails + checklists). Minimal pointer only (surgical).

**Prompt 11 (2026-05-28):** Procore CLI surface finalization + operator runbook + `procore validate` (new `procore/validate.py` read-only stack-readiness check across seeds/mapping/redaction/Obsidian templates/vault writer/schema/auth; new `docs/operations/procore-operator-runbook.md`; `obsidian preview --json` default normalized to True for parity; redacted exception envelope; 7 mocked tests). See evidence `docs/evidence/construction-intelligence-phase-03/11-procore-cli-surface-and-operator-runbook.md` (full 8-section). Minimal pointer only (surgical).

**Prompt 12 (2026-05-28):** Procore test fixtures + security guardrails (new `procore/fixtures.py` with synthetic paginated/error/rate-limit/sensitive-routing/malformed-body payloads; new `tests/test_procore_redaction.py` with 10 boundary tests; new `tests/test_repo_sensitive_scan.py` running existing `SensitiveScanner` over the repo with explicit allowlist; new `tests/test_procore_offline_enforcement.py` AST-asserting no real HTTP client imports in procore tests; pytest markers `integration`/`manual`/`live` registered in pyproject; new `docs/operations/test-discipline.md`). See evidence `docs/evidence/construction-intelligence-phase-03/12-test-fixtures-and-security-guardrails.md` (full 8-section). Minimal pointer only (surgical).

**Prompt 13 (2026-05-28):** Phase 03 final closeout — docs-only acceptance attestation across the Prompt 00–14 arc. Cites prior remediation evidence (`13-complete-validation-and-green-suite.md`, `14-mypy-remediation-and-strict-green.md`) for the full green pytest/ruff/mypy/compileall matrix rather than re-running it; re-attests sensitive-scan posture; updates `session-handoff.md` with Prompts 11–14 closure + Phase 03 acceptance-with-deferrals posture; recommends Phase 04 Procore OAuth + live-ingestion workstream. See evidence `docs/evidence/construction-intelligence-phase-03/14-final-closeout-summary.md` (full 8-section). Minimal pointer only (surgical).

**Phase 04 Prompt 00 (2026-05-28):** Phase 04 (Procore Core Project Controls) repo-truth rebaseline — read-only baseline at Phase 03 closeout (`19e21db`); pytest 640/640, ruff/mypy/compileall green; `procore validate/tools list/mapping validate --json` snapshots captured with `read_only`/`no_writeback`/`no_secrets` guardrails confirmed; Phase 03 residuals (test_procore_http_client.py env-var dep, `mapping_consistent` pending pilots, scanner tree-wide allowlists, live OAuth deferral) carried forward unchanged. See evidence `docs/evidence/construction-intelligence-phase-04/00-phase-04-rebaseline.md` (full 8-section). Minimal pointer only (surgical).

**Phase 04 Prompt 01 (2026-05-28):** Procore entry hardening — bearer-token regression closed (`http_client.py` now requires an OAuth access-token provider; raises `ProcoreAuthRequired` on missing token; never reads `PROCORE_CLIENT_SECRET`); pagination method renamed to align with `sync.py` call site; fake `_Project`/`_Projects` stub (hard-coded ID `2525840`) removed from sync.py; default sync target list now derived from mapping `status == "pilot"` only; `_assert_no_pending` fail-closed guard in `plan()`/`apply()` with `--allow-pending` explicit override; `__init__.py` docstring + exports refreshed (added `ProcoreHTTPClient`, `ProcoreSyncCoordinator`, `run_sync`, `SyncReceipt`, plus 3 new exceptions); `procore validate` extended to 15 checks (4 new Phase-04 checks); new `tests/test_procore_sync_guards.py`. Pytest 649/649, ruff/mypy/compileall green. No OAuth implementation yet (deferred to Prompt 02). See evidence `docs/evidence/construction-intelligence-phase-04/01-entry-hardening-proof.md` (full 8-section + before/after hazard matrix). Minimal pointer only (surgical).

**Phase 04 Prompt 02 (2026-05-28):** Procore OAuth token-provider boundary — new `procore/token_provider.py` formalizes `ProcoreTokenProvider` (`Protocol`, `runtime_checkable`) with concrete `MissingTokenProvider` (fail-closed default), `EnvOrKeychainTokenProvider` (env + macOS Keychain access-token), `LocalOAuthCacheTokenProvider` (strictly read-only consumer of `auth/procore_token.json` — never acquires/refreshes/writes), `_ChainedTokenProvider`, and `default_procore_token_provider()` chain (env → cache → missing); `http_client.py` swapped callable alias for the Protocol via `adapt_token_source()`; new AST-level boundary test `test_procore_client_secret_isolation.py` proves no Procore module outside `{config, errors, redaction}.py` imports/calls `get_procore_client_secret`; `procore validate` extended to 16 checks (new `token_provider_default_chain_shape`). Pytest 671/671, ruff/mypy/compileall green. No OAuth exchange yet (deferred to Prompt 03). See evidence `docs/evidence/construction-intelligence-phase-04/02-token-provider-proof.md` (full 8-section + provider matrix). Minimal pointer only (surgical).

**Phase 04 Prompt 02 remediation (2026-05-28):** Complete the OAuth token-acquisition path that Prompt 02 had originally deferred — new `procore/oauth.py` with `ProcoreOAuthClient` (OOB Installed-Apps flow), `TokenSet`, `ProcoreOAuthError`; cache writer/clear in `token_provider.py` (atomic via tempfile + `os.replace`, `0o600`/`0o700` perms enforced); new `RefreshingOAuthTokenProvider` (transparent near-expiry refresh + cache write-back; fail-closed on OAuth error); default chain now `env_or_keychain → oauth_refreshing → missing`; new CLI commands `hb-assistant procore auth login [--code <code>] / refresh / logout`; extended `procore auth status --json` envelope with `cache_present`, `access_token_present`, `refresh_token_present`, `expires_in_seconds_if_known`, `chain_order`; AST isolation allowlist += `oauth.py` (sole non-`config.py` consumer of `get_procore_client_secret`); `procore validate` extended to 19 checks (new `oauth_acquisition_path_present`); new `tests/test_procore_oauth.py` + `test_procore_token_cache_io.py` + `test_procore_cli_auth.py` + live-gated `test_procore_oauth_live.py` (skipped without `HB_PROCORE_LIVE=1 + PROCORE_TEST_REFRESH_TOKEN`). Pytest 719/719 + 1 skipped; ruff/mypy/compileall green. See evidence `docs/evidence/construction-intelligence-phase-04/02b-oauth-acquisition-proof.md` (full 8-section). Minimal pointer only (surgical).

**Phase 04 Prompt 03 (2026-05-28):** Structured Procore endpoint verification catalog — lifted the freeform `verification_status: ...` strings out of `notes:` into 5 typed Pydantic fields on `ProcoreEndpoint` (`verification_status`, `official_reference_url`, `verified_at_utc`, `verified_by`, `live_dry_run_receipt_id`, plus `verification_reason`); new `VerificationStatus` Literal enum; new model_validator pairs `status<->verification_status` guardrail invariants and requires URL-or-reason for included Phase-01 endpoints; derived `is_live_eligible` computed_field; `ProcoreEndpointContract.get_endpoint()` helper; seed YAML migrated for all 13 endpoints; new `procore tools catalog [--json] [--include-ineligible]` CLI command; sync.apply() now skips ineligible endpoints with `skipped_not_live_eligible` receipt entries; sync.plan() exposes `live_eligible` per endpoint; `procore validate` extended to 18 checks (new `endpoint_verification_metadata_complete` and `live_eligibility_blocks_ineligible`); new `tests/test_procore_endpoint_verification.py` + `tests/test_procore_tools_catalog.py`; construction fixtures + test_procore_endpoint_audit helper updated for new required fields. Pytest 690/690, ruff/mypy/compileall green. Catalog evidence captured as `docs/evidence/construction-intelligence-phase-04/03-endpoint-catalog-validation.json`. Minimal pointer only (surgical).

**Phase 04 Prompt 04 (2026-05-28):** RFI dry-run sync normalization — new `src/hb_assistant/procore/normalizers/` package with `normalize_rfi()` / `normalize_rfi_reply()` / `normalize_rfi_payload_block()` pure functions (canonical RFI shape: id-keyed `entity_stable_key`, whitelist `canonical_fields` (number/subject/status/assignee_id/due_date/initiated_at/created_at/updated_at/source_url), heuristic `review_required` with `routing_reason`, subject excerpt ≤200 chars, SHA-256 hash-only body summary for replies — body text never persisted); `sync.py` now dispatches `list-rfis` via `NORMALIZER_DISPATCH` and persists RFI parent + reply rows as separate canonical entities (replies always `review_required=True`, `category="rfi_replies"`, distinct `reply-<parent>-<id>` stable key); `procore sync run` gains repeatable `--endpoints / -e` filter; dry-run receipt entry for `list-rfis` carries `normalization_schema_version` + `would_persist_children_separately`; RFI fixture `RFI_SAMPLE_PAYLOAD` (3 synthetic RFIs + 5 nested replies, all `synthetic-` prefixed) added to `construction/fixtures/procore.py`; `procore validate` extended to 20 checks (new `rfi_normalizer_dispatch_present`); new `tests/test_procore_rfi_normalizer.py` (8 tests), `tests/test_procore_cli_sync_rfi_dry_run.py` (4 tests), `tests/test_procore_rfi_sqlite_idempotency.py` (3 tests). Pytest 740/740 + 1 skipped (live OAuth), ruff/mypy/compileall green. See evidence `docs/evidence/construction-intelligence-phase-04/rfi-sync-dry-run.json` (deterministic sync plan receipt + normalized preview + canonical field keys + guardrail attestation). Minimal pointer only (surgical).

**Phase 04 Prompt 05 (2026-05-28):** Submittal dry-run sync normalization — new `src/hb_assistant/procore/normalizers/submittal.py` mirroring the RFI template with `normalize_submittal()` / `normalize_submittal_response()` / `normalize_submittal_package()` / `normalize_submittal_payload_block()` pure functions (canonical submittal shape: id-keyed `entity_stable_key`, whitelist `canonical_fields` (number/title/status/type/specification_section/assignee_id/ball_in_court_id/due_date/initiated_at/created_at/updated_at/source_url), heuristic `review_required` extended with submittal-specific fragments (rejected/revise/resubmit/void) + missing assignee-or-ball-in-court detection); `sync.py` registers `list-submittals` in `NORMALIZER_DISPATCH` and persists submittal + response + package rows as separate canonical entities (responses always `review_required=True` with `submittal_response_default_review_required` routing reason, comment bodies reduced to SHA-256 hash-only summary; packages always `review_required=True` with `submittal_package_default_review_required`, descriptions hash-only); `plan()` gains optional `submittal_preview_payload`; dry-run receipt entry for `list-submittals` carries `normalization_schema_version` + `would_persist_children_separately`; submittal fixture `SUBMITTAL_SAMPLE_PAYLOAD` (3 synthetic submittals + 4 nested responses + 2 nested packages) added to `construction/fixtures/procore.py`; `procore validate` extended to 21 checks (new `submittal_normalizer_dispatch_present`); new `tests/test_procore_submittal_normalizer.py` (9 tests), `tests/test_procore_cli_sync_submittal_dry_run.py` (3 tests), `tests/test_procore_submittal_sqlite_idempotency.py` (3 tests). Pytest 754/754 + 1 skipped, ruff/mypy/compileall green. See evidence `docs/evidence/construction-intelligence-phase-04/submittal-sync-dry-run.json` (deterministic sync plan receipt + normalized preview + canonical field keys × 3 entities + guardrail attestation). Minimal pointer only (surgical).

**Phase 04 Prompt 06 (2026-05-28):** Observation dry-run sync normalization with safety-sensitive routing — new `list-observations` endpoint added to `resources/config/procore_endpoint_contract.seed.yaml` as `verification_status: candidate` (is_live_eligible=False; apply() emits `skipped_not_live_eligible` until promoted in a future prompt); new `src/hb_assistant/procore/normalizers/observation.py` with `normalize_observation()` / `normalize_observation_comment()` / `normalize_observation_payload_block()` pure functions extending the submittal template — canonical observation shape: id-keyed `entity_stable_key`, whitelist `canonical_fields` (number/title/status/type/subtype/assignee_id/created_by_id/observed_at/due_date/closed_at/created_at/updated_at/source_url/severity/priority), four-field safety heuristic that scans status/type/subtype/title/description for fragments (safety/incident/injury/near-miss/corrective/unsafe/violation/PPE/fall/first aid/personnel/hospital/emergency) and sets `safety_route: true` when a safety-specific fragment fires; description text reduced to SHA-256 hash-only summary; `redacted_excerpt` from title only ≤200 chars; comments always `review_required=True` with hash-only body. `sync.py` registers `list-observations` in `NORMALIZER_DISPATCH`, adds parallel apply() branch persisting observation + comment rows as separate canonical entities, threads `observation_preview_payload` through `plan()` and `run_sync()`. Observation fixture `OBSERVATION_SAMPLE_PAYLOAD` (3 synthetic observations + 3 nested comments; observation #2 = near-miss type, observation #3 = injury body text + missing assignee) added to `construction/fixtures/procore.py`. `procore validate` extended to 22 checks (new `observation_normalizer_dispatch_present`); contract count rises to 14 endpoints (10 official_docs_verified + 1 excluded_by_guardrail + 2 deferred_by_guardrail + 1 candidate). New `tests/test_procore_observation_normalizer.py` (10 tests), `tests/test_procore_cli_sync_observation_dry_run.py` (3 tests), `tests/test_procore_observation_sqlite_idempotency.py` (3 tests). Pytest 770/770 + 1 skipped, ruff/mypy/compileall green. See evidence `docs/evidence/construction-intelligence-phase-04/04-observation-sync-dry-run.json` (deterministic sync plan receipt + normalized preview + canonical field keys × 2 entities + safety_routing_summary + endpoint_posture + guardrail attestation). Minimal pointer only (surgical).

**Phase 04 Prompt 07 (2026-05-28):** Meeting + meeting-topic dry-run sync normalization — two new candidate endpoints added to `resources/config/procore_endpoint_contract.seed.yaml`: `list-meetings` (path `/rest/v1.0/projects/{project_id}/meetings`, category `meetings`, sensitivity `medium`) and `list-meeting-topics` (path `/rest/v1.0/projects/{project_id}/meetings/{meeting_id}/topics`, category `meeting-topics`, sensitivity `medium`) — both `verification_status: candidate`, both `is_live_eligible: false`. **Structural deviation**: meetings and topics are two separate Procore endpoints (not a parent payload with nested children like RFI replies / observation comments), so each gets its own normalizer + dispatch entry. New `src/hb_assistant/procore/normalizers/meeting.py` with four pure functions: `normalize_meeting()` (metadata-only; whitelist `canonical_fields` number/title/status/start_time/end_time/location/organizer_id/project_id/source_url/created_at/updated_at; review heuristic on status + title only — no body scan); `normalize_meeting_topic()` (id-keyed independent rows; canonical_fields id/title/status/sequence_number/assignee_id/due_date/parent_meeting_id/source_url/created_at/updated_at; **four-field scan** of status + title + description + action_items for fragments (legal/financial/hold/dispute/review/escalat + claim/delay/cost/safety/incident/injury/corrective/near-miss/unsafe/violation/PPE/fall); description and action_items both reduced to SHA-256 hash-only summaries; `safety_route: true` when a safety-specific fragment fires); two `_payload_block` functions returning one-tuples. `sync.py` registers both endpoints in `NORMALIZER_DISPATCH`, adds two parallel apply() branches, threads `meeting_preview_payload` and `meeting_topic_preview_payload` through `plan()` and `run_sync()`. Fixtures `MEETING_SAMPLE_PAYLOAD` (3 meetings) + `MEETING_TOPIC_SAMPLE_PAYLOAD` (4 topics with one safety body trigger + one claim-title generic review) added to `construction/fixtures/procore.py`. `procore validate` extended to 23 checks (new `meeting_normalizer_dispatch_present` covers both endpoints); contract count rises to 16 endpoints (10 verified + 1 excluded + 2 deferred + 3 candidate). New `tests/test_procore_meeting_normalizer.py` (14 tests), `tests/test_procore_cli_sync_meeting_dry_run.py` (5 tests), `tests/test_procore_meeting_sqlite_idempotency.py` (3 tests — the topic test also patches the path_template to strip the `{meeting_id}` placeholder so apply()'s two-arg format call succeeds in the test environment). Pytest 792/792 + 1 skipped, ruff/mypy/compileall green. See evidence `docs/evidence/construction-intelligence-phase-04/meeting-sync-dry-run.json` (deterministic sync plan receipt spanning both endpoints + normalized preview × 2 entities + canonical field keys + safety_routing_summary + endpoint_posture × 2 + guardrail attestation). Minimal pointer only (surgical).
