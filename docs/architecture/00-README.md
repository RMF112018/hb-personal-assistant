# Architecture Documentation

This directory contains living architecture and decision records for the HB Personal Assistant project.

- `01-scaffold-overview.md` — Phase 1 foundation (PathPolicy, Typer CLI, config loader)
- `02-…` through `12-launchd-automation-and-diagnostics.md` — Phase evolution (store, classification, files, retrieval, automation/orchestrator, diagnostics)
- `13-testing-hardening-and-final-closeout.md` — Phase 13 closeout record (`v1.3.0`), preserved as historical evidence
- `remediation-gap-closure.md` — Prompt 01 repo truth reconciliation and remediation acceptance baseline
- `remediation-validation-baseline.md` — Prompt 04 validation baseline and scoped standards (pytest/ruff/mypy reconciliation)
- `remediation-delegated-graph-proof.md` — Prompt 05 runtime delegated Graph proof refresh and truthful gap reporting
- `remediation-bounded-graph-paging.md` — Prompt 07 bounded, deterministic Graph paging across mail/calendar/drive clients
- `remediation-provenance-safe-file-ingestion.md` — Prompt 08 provenance-safe ingest contract (`files sample` vs real `files ingest`)
- `remediation-integrated-daily-brief-content.md` — Prompt 09 Daily Brief sections wired to current context/store sources with explicit empty states
- `remediation-bounded-content-sensitive-scan.md` — Prompt 10 bounded line-level sensitive scanner with redacted findings output
- `14-procore-live-sync-phase-04a.md` — Phase 04A per-endpoint live GET sync (gate → verified-or-fail-closed → pagination → normalize → SQLite latest-state)
- `15-procore-second-brain-phase-04b.md` — Phase 04B enrichment: V7 history/diff/timeline + cross-cutting entities/edges/signals/text-intelligence + read-only query commands + Obsidian register
- `16-procore-financials-phase-05.md` — Phase 05 contracts & financials: endpoint registry + live-gate shell (32 fail-closed endpoints) and V8 financial schema + repository (13 decimal-safe projection tables, amount-fact ledger); normalizers + live dispatch to follow
- `remediation-final-truthful-closeout.md` — Prompt 11 final remediation closeout status and acceptance gate evidence
- `remediation-hardened-app-support-permissions.md` — Addendum Prompt 02 path initialization hardening and `diagnostics paths` repair guidance
- `remediation-db-readiness-and-structured-dry-run-blocking.md` — Addendum Prompt 03 DB readiness gate and dry-run JSON blocked status behavior
- `remediation-bounded-body-mention-detection.md` (P05) — Addendum Prompt 05 bounded body inspector + MailClient fetch + classifier fallback (beyond preview)
- Prompt 06 final closeout: `docs/evidence/remediation-addendum/final-closeout/` + truthful **CONDITIONALLY_ACCEPTED_WITH_EXTERNAL_ADMIN_CONSENT_BLOCKER** (DNS language corrected as misattribution in Phase 14 Prompt 01; see `docs/evidence/phase-14-local-runtime-workstream-intelligence/prompt-01/`).
- `remediation-blocker-taxonomy-correction.md` — Phase 14 Prompt 01: blocker taxonomy correction, removal of stale DNS claims, D-P14-011 decision record, and prompt-01 evidence package.
- `remediation-idempotent-action-persistence.md` — Phase 14 Prompt 03: idempotent action upsert by stable_key (completed status preserved), action-aware source link helper in Registry, service refactor, and duplicate/completed/migration tests.
- `remediation-signal-integration-action-intelligence.md` — Phase 14 Prompt 04: multi-source bounded signal loading (body mentions, parser, calendar, file review, retrieval) into actions extractor, phrase mapping for full actionTypes, confidence + weak monitor, with seeded-DB tests and CLI dry-run validation.
- `remediation-morning-run-orchestration-upgrade.md` — Phase 14 Prompt 07: full 05 stage model in orchestrator with Graph consent blocker classification (skipped_* for Graph while local stages continue), P02-P06 integration (actions, context, brief+obsidian provenance), explicit failure isolation, and exact 05 JSON contract (blocker_classification + stages array).
- `docs/decisions/` — Closed technical decisions (D-CLI-001 etc.)

- Prompt 02: Dry-Run Semantics and Run Ledger Policy — explicit documentation of allowed ledger/evidence writes vs forbidden business object mutations (action_items, source_links, Obsidian notes); CLI notes aligned; tests proving zero mutations in dry-run (before/after row counts); evidence at `docs/evidence/mvp-local-runtime/02-dry-run-policy-proof.md`.
Remediation context: implementation reached `v1.3.0`, but acceptance is gated on remediation validation; prior closeout claims are superseded pending green remediation evidence.

For the full phased plan and research, see `docs/plans/my-pa-phase-0/`.

- `87-phase-08b-executor-policy-contracts-and-no-schema-rebaseline.md` — Phase 08B Addendum Prompt 01: 5 executor contracts (automation_executor, executor_stage, safe_replay, automation_execution_gate, validation_matrix) + registration in PHASE_08B_CONTRACT_FILES; 4 dedicated policy seeds + high-level sections + reason codes in main 08b automation policy seed/contract + gates contract; 4 load_*_seed fns in automation_policy.py; tests in contracts_and_seed.py; explicit no-schema confirmation (V34/151); proof evidence. Executor impl + gate flip still deferred.
- `88-phase-08b-automation-execution-planner.md` — Phase 08B Addendum Prompt 02: deterministic planner (ExecutionRequest/Stage/Decision/Plan models), consumes P01 seeds/contracts + registry/retry/launchd/health/freshness/08B surfaces; required default 8 stages (preflight_status, source_freshness_check, daily_brief_generate, ... closeout); modes (manual/launchd/catch_up/replay); dup prevention + weekend/catch-up decisions; dry-run emits plan only (no side effects); tests + dry-run-plan evidence json; arch 88- + 00-README. Planner only (apply/runner later); gate still deferred; no schema.
- `89-phase-08b-automation-execution-service-and-stage-runner.md` — Phase 08B Addendum Prompt 03: executor service + stage runner (AutomationExecutor, dry vs apply+--confirm two-factor, lock acquire before register_run before 8-stage sequential dispatch, StageReceipt/ExecutionResult, record_run_step + emit_receipt receipts (V29+V28+), failed+downstream_skipped marking, guaranteed finally release, recovery recommendation, injected fakes only in tests). Reuses P02 planner + P05/P08–P12 surfaces for the 5 domains. CLI execute + execution-status. New service test + 2 evidence jsons (apply-simulated-run + execution-proof) + arch 89- + 00-README. Gate remains deferred; no schema; additive guardrails preserved.
- `90-phase-08b-automation-retry-backoff-weekend-catchup.md` — Phase 08B Addendum Prompt 04: bounded retry/backoff + missed-run catch-up. Failure classification (transient_local vs permanent_policy/safety/missing/no-writeback/unknown; never retry non-transient). Injectable clock + sleep_fn for deterministic tests/proofs. Weekend policy gate now calendar-aware (`_is_weekend` + conditional skip decision only on Sat/Sun; weekday proceed). First-run-after-wake catch-up proceeds on needed (fresh/missed+post-schedule via launchd evaluator), persists via register reason=EXECUTOR_STARTED_CATCHUP + catchup_decision step marker (reason CATCH_UP_NEEDED). Dup prevention via registry latest succeeded + target date in started_utc (plus delivery surface fallback); early skip all stages with DUPLICATE... . All paths persist via V29 steps + V30 retry_receipts (attempt/backoff/outcome). 4 new proof builders + 4 evidence JSONs (retry-backoff-*, weekend-catchup-*, first-run-after-wake-*, duplicate-prevention-*). Fixed latent model/dict .get in decisions; added "skipped" to ExecutionResult status. Extended service test. Arch 90- + 00-README. Gate still deferred (1); no schema; guardrails + no-raw preserved.
- `91-phase-08b-safe-replay-and-recovery-execution.md` — Phase 08B Addendum Prompt 05: safe replay/recovery execution (turns P03 recovery recs into operator-approved replay). Replay request validation (safe_replay_contract + registry/lock/delivery checks -> SAFE_REPLAY_BLOCKED_*). ExecutionRequest + original_run_registry_id/replay_selector (failed-only/failed-and-following/explicit)/replay_stages (reuse force). Selectors from original read_run_steps history. New replay run (REPLAY_EXECUTION reason + replay_link marker step with original/selector/force) while preserving original rows untouched. --apply --confirm + no-overlap lock required. Block non-replay-safe (hardcoded REPLAY_SAFE_STAGES; delivery ones only with force). Dedupe delivery artifacts in replay unless force (SAFE_REPLAY_IDEMPOTENT_SKIP). Recovery rec updated with --mode=replay --replay-of examples. build_safe_replay..._proof + evidence json (pre-pop failed orig, selectors, link, block, dedup, fakes, contract, lock, 34, no raw). CLI execute + --replay-of/--replay-selector/--replay-stages. Test + exports + 91 arch + 00-README. Gate still deferred; no schema; guardrails preserved.
- `92-phase-08b-cli-automation-run-status-diagnostics-replay.md` — Phase 08B Addendum Prompt 06: CLI surface (run/replay/status/diagnostics/last-good-run per suggested grammar). run supports --kind/--date/--catch-up + --dry-run/--apply --confirm; delegates to executor (P05 replay for replay cmd). status/diagnostics use new build_* in executor (registry + lock + retry receipts + P05 eligibility + redacted recovery using run/replay grammar). All JSONs include command/mode/status/run_id/target_date/stage_summary/retry_summary/lock_status/replay_eligibility/recovery_command_redacted/guardrails. Evidence previews generated. Exports + test. Arch 92- + 00-README. Gate still deferred; no schema; guardrails + ignore-unrelated preserved.
- `93-phase-08b-job-health-last-good-run-observability.md` — Phase 08B Addendum Prompt 07: connect executor outcomes to job health (always after run via terminal stage exempt + outcome-enriched call to injectable), last-good-run (explicit update_last_good_run + last_good_run query ONLY on full success; replay/catchup full success counts for target), surfaces (last_failed_stage + failure_class from P04 step reason_code, retry_exhausted, catch-up status; P05/6 replay elg + recovery preserved). Registry helpers (V29 marker only), executor (model/execute/dispatch/builders), CLI enhancements, 4 scenario tests + 2 named evidence proofs, 93 arch + 00-README. Gate still deferred_not_blocking (16/1); no schema; guardrails + "ignore unrelated" + only-commit-summary-after-land preserved.
- `94-phase-08b-automation-execution-gate-and-consolidated-proofs.md` — Phase 08B Addendum Prompt 08: extend build_automation_execution_proof to aggregate/call all 11 sub-proofs (P02 dry, P03 apply, P04 4 resilience, P05 replay, P07 health/last-good) + safety no_writeback + lock/metadata asserts; write automation-execution-proof.md; flip automation_execution gate in data_quality (hardcoded deferred -> _proof_gate on the builder) + relax deferred>=1 in build_phase_08b_gates_proof (0 now expected/correct); update guards/comments; 4 scenario coverage + asserts; 94 arch + 00-README. Now 16/0 (automation pass); evidence phase-08b-final-gates-proof.json + automation-execution-proof.md; full verif. Guardrails + only P08 files + only-summary-after-land preserved.
- `95-phase-08b-no-writeback-executor-proof.md` — Phase 08B Addendum Prompt 09: extend build_second_brain_no_writeback_proof (safety) for executor (modules already enumerated in second_brain/ walk + explicit rels/findings_ok; receipts/tables (run_registry/steps/retry V29/V30) already in PHASE list + guard/content probe; evidence: added _scan_evidence_outputs on 08b-automation-hardening subdir for raw/secret; confirms 4-7 no delivery/raw/logs-locks-outside/no-mcp-llama via report + generated .md); write phase-08b-final-no-writeback-proof.md; return augmented with P09 extension section; 95 arch + 00-README. Evidence phase-08b-final-no-writeback-proof.json + .md; verif (build now covers 7 reqs + prior). Guardrails + only P09 files + only-summary-after-land.
- `96-phase-08b-final-automation-execution-closeout.md` — Phase 08B Addendum Prompt 10: final validation closeout + Phase 08B closure (docs/evidence/README only; no code/schema/gate re-flip; schema stays V34). Full matrix recorded verbatim (compileall exit 0; ruff clean; mypy 255 files; safe pytest 2834 passed/1 deselected; construction validate 4/4; second-brain status 34/34; automation health ok/RUN_OK; phase-08b-gates 16 pass/0 else; no-writeback-proof proof_passed=true). Confirms automation_execution=pass + all 16 gates pass + readiness_overstated=false (Prompt 00 stop-condition not triggered -> close). Evidence phase-08b-final-validation-closeout.md; README ledger -> Phase 08B Closed; handoff to 08C/08D/09. This closeout commit also lands the dangling P09 95- arch doc + its index line so arch 86-96 is complete. Guardrails + ignore-unrelated + only-summary-after-land + readiness-not-overstated preserved.
- `97-phase-08c-financial-readiness-repo-truth-rebaseline.md` — Phase 08C Prompt 00: repo-truth audit + financial substrate rebaseline at exact 08B closeout `dcecea8` (V34, 1.3.0). Confirmed ancestry, 08B gates (automation_execution=pass, 16/0, overstated=false), both no-writeback proofs (repo_sha match, financial tables guarded), full matrix green. Re-audited 32 endpoints (endpoints.py), 15 procore_financial_* tables (V8/V9 only), amount handling (TEXT + source_field_path; Decimal only for >0/delta, never float, never stored), WBS/cost/project joins, existing financial CLI + exposure + obsidian register (explicit "advisory/review aid only — no entitlement... amounts never summed"), advisory labeling, procore no-writeback coverage of financials. Minimal SB financial exposure yet (expected). Produced 08C evidence 00- + this arch record + 00-README index. Staged only 3 paths. No code changes. 08C not closed. Stop conditions not tripped.
- `98-phase-08c-schema-contracts-and-table-lifecycle.md` — Phase 08C Prompt 01: additive V35 (10 second_brain_financial_* tables with full 08C guards incl. raw_financial_source + determination_*/advisory_only=1, money as decimal TEXT+minor_units, no raw/float); JSON contracts + YAML seeds; lifecycle to 161; new v35 schema test + inventory update; 08c gates/CLI read-only; proof.md. Staged only required. 08C not closed.
- `99-phase-08c-financial-endpoint-table-inventory-audit.md` — Phase 08C Prompt 02: repo-truth inventory of 32 financial endpoints (29 live-verified phase05, 3 fail-closed: purchase-order-detail-line-items/shell, budget-details/unresolved_prompt00-3.2, budget-change-line-items/shell) + 25 tables (15 procore V8/V9 05/populated + 10 08C V35 08C/empty_expected); amount/currency/WBS/cost/source fields identified (TEXT canonical_decimal + minor_units INTEGER, no float/REAL); normalizers (parse_amount str); CLI surfaces (procore live financial *, second-brain financial * + phase-08c-gates); generated 2 JSON audits (metadata only, no raw); arch update. Staged only 2 JSONs + arch. No code. 08C not closed. Verification: 08c-gates pass, no-writeback pass, stops clear, sensitive clean.
- `100-phase-08c-decimal-amount-normalization.md` — Phase 08C Prompt 03: Decimal-only helpers (parse_amount raises on float; to_canonical/compute_minor/classify_amount using Decimal(str(v)) only) + tests prohibiting float money; discover amount fields from P02 inventory JSON; classify 7 statuses (parseable/rejected/missing/ambiguous/stale/conflicting/review_required per contract); store canonical + hash + ref + reason + advisory in V35 (source TEXT preserved in procore_financial_*); generated amount-normalization-proof.json + rejected-values.json (stats, no_float, source_preserved, advisory). Gate wired for real counts. Staged only required src+test+module+2 JSONs+arch. 08C not closed. Verification: tests 13/13, 08c-gates real stats, proofs clean, no float/REAL, stops clear.
- `101-phase-08c-currency-wbs-cost-code-completeness.md` — Phase 08C Prompt 04: Currency/WBS/cost-code/line/source completeness (snapshots per contracts: 6 currency statuses with evidence_backed_project_default ONLY under 4 policy conditions + marked; 5 wbs statuses + counts per dim; source coverage per family). Project default currency handling evidence-backed only. Missing/inconsistent/ambiguous routed to review_required_items (correct triggers). Generated currency-completeness-report.json + wbs-cost-code-coverage-report.json. Gates now real (counts/%/statuses); coverage CLI real snapshots. Focused tests (explicit/default-allowed/default-blocked/inconsistent/missing-wbs/missing-source). Staged only required. 08C not closed. Verification: tests pass, gates real, reports clean (policy enforced, no raw, source preserved), stops clear.
- `102-phase-08c-financial-source-coverage-matrix.md` — Phase 08C Prompt 05: Financial Source Coverage Matrix. Builder in financial_completeness (P02 endpoint inv for 32 eps/family/normalizers/amount/currency/wbs/source_field_path_support/live_verified + local_tables map + relationship_keys; contract for exact 6 statuses + 10 required_families; facts for source_row_count + field presence counts; classify: fail_closed for 3 P02 shells, covered_ready/missing_context/deferred_not_blocking per live+counts; advisory labels only). Wires to 08c source_coverage gate + financial coverage CLI (real summary + matrix_path + gen side-effect). Generated financial-source-coverage-matrix.json (37 sources, 3 fail_closed exact, by_status, no_raw_in_matrix:true, maps+counts+advisory+contract+notes "NO raw payloads or full source values", "advisory review aid only"; sensitive clean). Focused test (all 6 statuses, fail_closed=3, maps, advisory, no raw in JSON). Arch 102 + 00-README. Staged only required (completeness+data_quality+cli+test+matrix JSON+102+00-README). 08C not closed. Verification: pytest new+08c, 08c-gates with matrix, coverage --json real, no-writeback, construction, harness, ruff, matrix scan 0 raw + correct structure/3 fail_closed, stops clear.
- `103-phase-08c-financial-exposure-read-models.md` — Phase 08C Prompt 06: Financial Exposure Read Models (advisory marts). Builders in financial_completeness (exposure mart preview + V35 snapshot using normalized_amount_ref only + deterministic from cost/schedule exposure patterns vs candidate; contract categories + required_fields; relationship_kind; confidence/review/advisory_status). Wires: exposure-summary CLI now surfaces preview_path + summary; 08c-gates exposure_marts real (advisory check + no det). Generated exposure-mart-preview.json (items with all fields, det/cand counts, advisory language, no raw/det claims). Focused test (normalized str, relationship_kind, advisory, json, CLI, no det). Arch 103 + 00-README. Staged only required (completeness+data_quality+cli+test+preview JSON+103+00-README). 08C not closed. Verification: pytest, 08c-gates (exposure_marts), exposure-summary real preview, no-writeback, construction, harness, ruff, preview attest (fields + "advisory review aid only" + "not a final", no raw, normalized refs). Stops clear.
- `104-phase-08c-financial-fact-readiness-agent.md` — Phase 08C Prompt 07: Financial Fact Readiness Agent (deterministic controller). Agent fn in financial_completeness orchestrates P03-P06 subs (amount/completeness/coverage/exposure) + forecast/review; emits V35 receipt (readiness_agent_runs with status + full guards) + financial-readiness-agent-proof.json (sub_results, counts, guardrails incl. "model_use": "absent_or_mock_safe_only", "deterministic", "advisory review aids only — no determinations"). CLI "financial readiness" now surfaces agent_run_receipt + proof_path + summary; readiness_agent gate real (calls agent, asserts advisory/guards/no det). Focused test (receipt row + guards, proof structure + "no model required", CLI). Arch 104 + 00-README. Staged only required (completeness+data_quality+cli+test+proof JSON+104+00-README). 08C not closed. Verification: pytest -k readiness/agent, 08c-gates (readiness_agent), financial readiness real proof, no-writeback, construction, harness, proof attest (counts, "deterministic", "advisory", "no model", guards, no raw/no det claim). Stops clear.
- `105-phase-08c-forecast-readiness-gates.md` — Phase 08C Prompt 08: Forecast Readiness Gates (gate evaluator). Evaluator in financial_completeness (8 gates: amount_normalization/currency_completeness/wbs_cost_code_completeness/source_coverage/relationship_completeness/review_backlog/no_writeback_no_raw_proof/advisory_labeling; pass/warning/fail_blocking/deferred_not_blocking + 5 readiness_status; V35 forecast_readiness_runs run + 19 guards; emit forecast-readiness-gates.md ("This is a readiness report only... No forecasts are computed or recommended") + proof json (gates[8], summary, stop_checks.forecast_decision_made=false, advisory)). Wires: data_quality 08c-gates forecast_readiness real (status + proof/md), agent sub real. Focused test (8 gates+allowed statuses, "readiness report" + "No forecasts..." wording, DB row+guards, no raw/decision, CLI). Arch 105 + 00-README. Staged only required (completeness+data_quality+test+2 evidence+105+00-README). 08C not closed. Verification: pytest -k forecast/gate, 08c-gates (forecast real), financial readiness (real sub), no-writeback, construction, harness, proof/md attest (wording, 8 gates, statuses, guards, no raw/decision). Stops clear.

**P07 addition**: Public operator-facing documentation for the local MVP (no code reading required) lives at `docs/operations/mvp-local-runtime-operator-guide.md` + supporting evidence at `docs/evidence/mvp-local-runtime/07-operator-runbook-and-limitations.md` and `06-known-limitations.md`. Covers venv, diagnostics, morning dry-run, launchd management, what gets written vs never, Graph deferred / Prompt 9 readiness, and all operational paths.

**Phase 03 Prompt 01A (Endpoint Reference & Contract Enrichment):** See `docs/evidence/construction-intelligence-phase-03/01A-procore-endpoint-reference-verification.md` + matrix JSON + search result. Modern REST v1.x paths reconciled for core operational/financial endpoints against official developers.procore.com/reference/rest; unverified candidate catalog materialized from the Phase 03 package; new test file + guardrail tests added; all hard GET-only/excluded/deferred/sensitive-review rules preserved. Minimal pointer only (surgical).

**Phase 03 Construction Intelligence / Procore note (Prompt 00 rebaseline):** See `docs/evidence/construction-intelligence-phase-03/00-repo-truth-and-phase-02-rebaseline.md` (repo truth audit, Phase 01/02 acceptance posture, current 4-module procore/ contract layer + seeds vs Desktop/procore_hbintel_data_model_package/ research as effective Phase 03 input — layered canonical model, priority financial/operational entities, SharePoint recs, 13-Assumptions gaps). No prior coverage of construction/procore/phase-03 surfaces in 00-13 or remediation- docs. Minimal pointer only (surgical).

**Phase 03 Prompt 01 (API Research & Decision Register):** See `docs/evidence/construction-intelligence-phase-03/01-procore-api-research-summary.md`. High-level official Procore docs research (OAuth, base URLs/environments, REST paths/versioning, pagination/filtering/sorting, rate limits/quotas, errors, changelog/best practices) with full Decision Register (URL | Access Date | Fact | Decision/Rationale | Confidence | Notes/Risks). 4 parallel explore sub-agents spawned for the research areas (active with web_search/web_fetch on developers.procore.com, 0 errors). All guardrails preserved (research-only, no code/seed/test changes, no Procore calls, evidence stays evidence-only). Sources from official pages (oauth-*, development-environments, pagination, filtering-on-list-actions, rate-limiting, error-reference, reference/rest). Minimal pointer only (surgical).

**Prompt 02 (2026-05-27):** App profile + environments schemas/seeds + full secret storage/loader in src/hb_assistant/procore/config.py (Keychain/env/0600 file, no-leak, OOB enforced). See evidence/construction-intelligence-phase-03/02-procore-app-credential-posture.md and 01-*-augmented.md. (minimal pointer per plan)

**Prompt 04 (2026-05-27):** GET-only Procore HTTP client foundation (http_client.py + redaction.py + errors.py + pagination.py) with injectable transport, runtime secret via Prompt_02 config.py only, correlation IDs, aggressive redaction, Link+cursor pagination + rate-aware retry, safe normalized errors, and static AST GET-only scanner. See evidence/construction-intelligence-phase-03/04-procore-client-foundation-proof.md.

**Prompt 05 (2026-05-27):** Endpoint contract model + config strengthened (explicit categories foundation/project_controls/financials; hard GET-only; financials force review_required + sensitive; verified paths only from Prompt_01A/01; deferred/excluded explicit; fail-closed tests). See evidence/construction-intelligence-phase-03/05-procore-endpoint-contract-proof.md.

**Prompt 06 (2026-05-27):** Pilot project mapping + company 5280 validation (new dedicated procore_project_mapping.seed.yaml with explicit pending for hilltop*; CLI list/status coverage for mapping/projects/companies + enhanced validate surfacing company context; HB# vs Procore ID separation tests; 3 explore sub-agents + safe discovery only). See evidence/construction-intelligence-phase-03/08-procore-pilot-project-mapping-proof.md. (minimal pointer, surgical).

**Prompt 07 (2026-05-27):** Endpoint audit dry-run construction + optional explicit manual live proof (extends auditor.py + models with new verdicts/receipts/envelopes; CLI `procore audit dry-run` (default/safe) + `execute` (opt-in --confirm only); mocked tests with live isolation; JSON-first receipts (06- artifact) with full redaction; SQLite gated on migrator readiness). Reuses Prompt_04 injectable client + redaction + Prompt_05 contract + Prompt_06 mapping. See evidence 06-procore-endpoint-audit-dry-run.json. (minimal pointer, surgical).

**Prompt 09 (2026-05-28):** Pilot project dry-run sync pipeline + local SQLite apply (new procore/sync.py coordinator with mandatory Prompt_07 auditor prerequisite gate; --dry-run default produces serializable redacted plan; --apply explicit opt-in only executes GETs via Prompt_04 client then normalizes + idempotent upserts to local/temp SQLite only via additive repositories methods; receipts + redaction everywhere; incremental policy + watermarks; 100% mocked tests + temp DB isolation; CLI `procore sync --dry-run|--apply --json`. See evidence 09-procore-dry-run-sync-proof.json (full 8-section). Minimal pointer only (surgical).

**Prompt 10 (2026-05-28):** Procore Obsidian deterministic output preview (new obsidian.py + hybrid vault_writer markers for 01_Projects/ + cli/procore obsidian preview + 8 templates from Phase 03 package (shas recorded) + procore_sensitive_routing_rules.yaml + test_procore_obsidian_output.py + exports in __init__). Dry-run default, explicit --apply, redaction + yaml routing only, 100% mocked. See evidence `docs/evidence/construction-intelligence-phase-03/10-procore-obsidian-output-preview.md` (full 8-section + rendered samples + guardrails + checklists). Minimal pointer only (surgical).

**Prompt 11 (2026-05-28):** Procore CLI surface finalization + operator runbook + `procore validate` (new `procore/validate.py` read-only stack-readiness check across seeds/mapping/redaction/Obsidian templates/vault writer/schema/auth; new `docs/operations/procore-operator-runbook.md`; `obsidian preview --json` default normalized to True for parity; redacted exception envelope; 7 mocked tests). See evidence `docs/evidence/construction-intelligence-phase-03/11-procore-cli-surface-and-operator-runbook.md` (full 8-section). Minimal pointer only (surgical).

**Prompt 12 (2026-05-28):** Procore test fixtures + security guardrails (new `procore/fixtures.py` with synthetic paginated/error/rate-limit/sensitive-routing/malformed-body payloads; new `tests/test_procore_redaction.py` with 10 boundary tests; new `tests/test_repo_sensitive_scan.py` running existing `SensitiveScanner` over the repo with explicit allowlist; new `tests/test_procore_offline_enforcement.py` AST-asserting no real HTTP client imports in procore tests; pytest markers `integration`/`manual`/`live` registered in pyproject; new `docs/operations/test-discipline.md`). See evidence `docs/evidence/construction-intelligence-phase-03/12-test-fixtures-and-security-guardrails.md` (full 8-section). Minimal pointer only (surgical).

**Prompt 13 (2026-05-28):** Phase 03 final closeout — docs-only acceptance attestation across the Prompt 00–14 arc. Cites prior remediation evidence (`13-complete-validation-and-green-suite.md`, `14-mypy-remediation-and-strict-green.md`) for the full green pytest/ruff/mypy/compileall matrix rather than re-running it; re-attests sensitive-scan posture; updates `session-handoff.md` with Prompts 11–14 closure + Phase 03 acceptance-with-deferrals posture; recommends Phase 04 Procore OAuth + live-ingestion workstream. See evidence `docs/evidence/construction-intelligence-phase-03/14-final-closeout-summary.md` (full 8-section). Minimal pointer only (surgical).

**Phase 04 Prompt 00 (2026-05-28):** Phase 04 (Procore Core Project Controls) repo-truth rebaseline — read-only baseline at Phase 03 closeout (`19e21db`); pytest 640/640, ruff/mypy/compileall green; `procore validate/tools list/mapping validate --json` snapshots captured with `read_only`/`no_writeback`/`no_secrets` guardrails confirmed; Phase 03 residuals (test_procore_http_client.py env-var dep, `mapping_consistent` pending pilots, scanner tree-wide allowlists, live OAuth deferral) carried forward unchanged. See evidence `docs/evidence/construction-intelligence-phase-04/00-phase-04-rebaseline.md` (full 8-section). Minimal pointer only (surgical).

**Phase 04 Prompt 01 (2026-05-28):** Procore entry hardening — bearer-token regression closed (`http_client.py` now requires an OAuth access-token provider; raises `ProcoreAuthRequired` on missing token; never reads `PROCORE_CLIENT_SECRET`); pagination method renamed to align with `sync.py` call site; fake `_Project`/`_Projects` stub (hard-coded ID `2525840`) removed from sync.py; default sync target list now derived from mapping `status == "pilot"` only; `_assert_no_pending` fail-closed guard in `plan()`/`apply()` with `--allow-pending` explicit override; `__init__.py` docstring + exports refreshed (added `ProcoreHTTPClient`, `ProcoreSyncCoordinator`, `run_sync`, `SyncReceipt`, plus 3 new exceptions); `procore validate` extended to 15 checks (4 new Phase-04 checks); new `tests/test_procore_sync_guards.py`. Pytest 649/649, ruff/mypy/compileall green. No OAuth implementation yet (deferred to Prompt 02). See evidence `docs/evidence/construction-intelligence-phase-04/01-entry-hardening-proof.md` (full 8-section + before/after hazard matrix). Minimal pointer only (surgical).

**Phase 04 Prompt 02 (2026-05-28):** Procore OAuth token-provider boundary — new `procore/token_provider.py` formalizes `ProcoreTokenProvider` (`Protocol`, `runtime_checkable`) with concrete `MissingTokenProvider` (fail-closed default), `EnvOrKeychainTokenProvider` (env + macOS Keychain access-token), `LocalOAuthCacheTokenProvider` (strictly read-only consumer of `auth/procore_token.json` — never acquires/refreshes/writes), `_ChainedTokenProvider`, and `default_procore_token_provider()` chain (env → cache → missing); `http_client.py` swapped callable alias for the Protocol via `adapt_token_source()`; new AST-level boundary test `test_procore_client_secret_isolation.py` proves no Procore module outside `{config, errors, redaction}.py` imports/calls `get_procore_client_secret`; `procore validate` extended to 16 checks (new `token_provider_default_chain_shape`). Pytest 671/671, ruff/mypy/compileall green. No OAuth exchange yet (deferred to Prompt 03). See evidence `docs/evidence/construction-intelligence-phase-04/02-token-provider-proof.md` (full 8-section + provider matrix). Minimal pointer only (surgical).

**Phase 04 Prompt 02 remediation (2026-05-28):** Complete the OAuth token-acquisition path that Prompt 02 had originally deferred — new `procore/oauth.py` with `ProcoreOAuthClient` (OOB Installed-Apps flow), `TokenSet`, `ProcoreOAuthError`; cache writer/clear in `token_provider.py` (atomic via tempfile + `os.replace`, `0o600`/`0o700` perms enforced); new `RefreshingOAuthTokenProvider` (transparent near-expiry refresh + cache write-back; fail-closed on OAuth error); default chain now `env_or_keychain → oauth_refreshing → missing`; new CLI commands `hb-assistant procore auth login [--code <code>] / refresh / logout`; extended `procore auth status --json` envelope with `cache_present`, `access_token_present`, `refresh_token_present`, `expires_in_seconds_if_known`, `chain_order`; AST isolation allowlist += `oauth.py` (sole non-`config.py` consumer of `get_procore_client_secret`); `procore validate` extended to 19 checks (new `oauth_acquisition_path_present`); new `tests/test_procore_oauth.py` + `test_procore_token_cache_io.py` + `test_procore_cli_auth.py` + live-gated `test_procore_oauth_live.py` (skipped without `HB_PROCORE_LIVE=1 + PROCORE_TEST_REFRESH_TOKEN`). Pytest 719/719 + 1 skipped; ruff/mypy/compileall green. See evidence `docs/evidence/construction-intelligence-phase-04/02b-oauth-acquisition-proof.md` (full 8-section). Minimal pointer only (surgical).

**Phase 04 Prompt 03 (2026-05-28):** Structured Procore endpoint verification catalog — lifted the freeform `verification_status: ...` strings out of `notes:` into 5 typed Pydantic fields on `ProcoreEndpoint` (`verification_status`, `official_reference_url`, `verified_at_utc`, `verified_by`, `live_dry_run_receipt_id`, plus `verification_reason`); new `VerificationStatus` Literal enum; new model_validator pairs `status<->verification_status` guardrail invariants and requires URL-or-reason for included Phase-01 endpoints; derived `is_live_eligible` computed_field; `ProcoreEndpointContract.get_endpoint()` helper; seed YAML migrated for all 13 endpoints; new `procore tools catalog [--json] [--include-ineligible]` CLI command; sync.apply() now skips ineligible endpoints with `skipped_not_live_eligible` receipt entries; sync.plan() exposes `live_eligible` per endpoint; `procore validate` extended to 18 checks (new `endpoint_verification_metadata_complete` and `live_eligibility_blocks_ineligible`); new `tests/test_procore_endpoint_verification.py` + `tests/test_procore_tools_catalog.py`; construction fixtures + test_procore_endpoint_audit helper updated for new required fields. Pytest 690/690, ruff/mypy/compileall green. Catalog evidence captured as `docs/evidence/construction-intelligence-phase-04/03-endpoint-catalog-validation.json`. Minimal pointer only (surgical).

**Phase 04 Prompt 04 (2026-05-28):** RFI dry-run sync normalization — new `src/hb_assistant/procore/normalizers/` package with `normalize_rfi()` / `normalize_rfi_reply()` / `normalize_rfi_payload_block()` pure functions (canonical RFI shape: id-keyed `entity_stable_key`, whitelist `canonical_fields` (number/subject/status/assignee_id/due_date/initiated_at/created_at/updated_at/source_url), heuristic `review_required` with `routing_reason`, subject excerpt ≤200 chars, SHA-256 hash-only body summary for replies — body text never persisted); `sync.py` now dispatches `list-rfis` via `NORMALIZER_DISPATCH` and persists RFI parent + reply rows as separate canonical entities (replies always `review_required=True`, `category="rfi_replies"`, distinct `reply-<parent>-<id>` stable key); `procore sync run` gains repeatable `--endpoints / -e` filter; dry-run receipt entry for `list-rfis` carries `normalization_schema_version` + `would_persist_children_separately`; RFI fixture `RFI_SAMPLE_PAYLOAD` (3 synthetic RFIs + 5 nested replies, all `synthetic-` prefixed) added to `construction/fixtures/procore.py`; `procore validate` extended to 20 checks (new `rfi_normalizer_dispatch_present`); new `tests/test_procore_rfi_normalizer.py` (8 tests), `tests/test_procore_cli_sync_rfi_dry_run.py` (4 tests), `tests/test_procore_rfi_sqlite_idempotency.py` (3 tests). Pytest 740/740 + 1 skipped (live OAuth), ruff/mypy/compileall green. See evidence `docs/evidence/construction-intelligence-phase-04/rfi-sync-dry-run.json` (deterministic sync plan receipt + normalized preview + canonical field keys + guardrail attestation). Minimal pointer only (surgical).

**Phase 04 Prompt 05 (2026-05-28):** Submittal dry-run sync normalization — new `src/hb_assistant/procore/normalizers/submittal.py` mirroring the RFI template with `normalize_submittal()` / `normalize_submittal_response()` / `normalize_submittal_package()` / `normalize_submittal_payload_block()` pure functions (canonical submittal shape: id-keyed `entity_stable_key`, whitelist `canonical_fields` (number/title/status/type/specification_section/assignee_id/ball_in_court_id/due_date/initiated_at/created_at/updated_at/source_url), heuristic `review_required` extended with submittal-specific fragments (rejected/revise/resubmit/void) + missing assignee-or-ball-in-court detection); `sync.py` registers `list-submittals` in `NORMALIZER_DISPATCH` and persists submittal + response + package rows as separate canonical entities (responses always `review_required=True` with `submittal_response_default_review_required` routing reason, comment bodies reduced to SHA-256 hash-only summary; packages always `review_required=True` with `submittal_package_default_review_required`, descriptions hash-only); `plan()` gains optional `submittal_preview_payload`; dry-run receipt entry for `list-submittals` carries `normalization_schema_version` + `would_persist_children_separately`; submittal fixture `SUBMITTAL_SAMPLE_PAYLOAD` (3 synthetic submittals + 4 nested responses + 2 nested packages) added to `construction/fixtures/procore.py`; `procore validate` extended to 21 checks (new `submittal_normalizer_dispatch_present`); new `tests/test_procore_submittal_normalizer.py` (9 tests), `tests/test_procore_cli_sync_submittal_dry_run.py` (3 tests), `tests/test_procore_submittal_sqlite_idempotency.py` (3 tests). Pytest 754/754 + 1 skipped, ruff/mypy/compileall green. See evidence `docs/evidence/construction-intelligence-phase-04/submittal-sync-dry-run.json` (deterministic sync plan receipt + normalized preview + canonical field keys × 3 entities + guardrail attestation). Minimal pointer only (surgical).

**Phase 04 Prompt 06 (2026-05-28):** Observation dry-run sync normalization with safety-sensitive routing — new `list-observations` endpoint added to `resources/config/procore_endpoint_contract.seed.yaml` as `verification_status: candidate` (is_live_eligible=False; apply() emits `skipped_not_live_eligible` until promoted in a future prompt); new `src/hb_assistant/procore/normalizers/observation.py` with `normalize_observation()` / `normalize_observation_comment()` / `normalize_observation_payload_block()` pure functions extending the submittal template — canonical observation shape: id-keyed `entity_stable_key`, whitelist `canonical_fields` (number/title/status/type/subtype/assignee_id/created_by_id/observed_at/due_date/closed_at/created_at/updated_at/source_url/severity/priority), four-field safety heuristic that scans status/type/subtype/title/description for fragments (safety/incident/injury/near-miss/corrective/unsafe/violation/PPE/fall/first aid/personnel/hospital/emergency) and sets `safety_route: true` when a safety-specific fragment fires; description text reduced to SHA-256 hash-only summary; `redacted_excerpt` from title only ≤200 chars; comments always `review_required=True` with hash-only body. `sync.py` registers `list-observations` in `NORMALIZER_DISPATCH`, adds parallel apply() branch persisting observation + comment rows as separate canonical entities, threads `observation_preview_payload` through `plan()` and `run_sync()`. Observation fixture `OBSERVATION_SAMPLE_PAYLOAD` (3 synthetic observations + 3 nested comments; observation #2 = near-miss type, observation #3 = injury body text + missing assignee) added to `construction/fixtures/procore.py`. `procore validate` extended to 22 checks (new `observation_normalizer_dispatch_present`); contract count rises to 14 endpoints (10 official_docs_verified + 1 excluded_by_guardrail + 2 deferred_by_guardrail + 1 candidate). New `tests/test_procore_observation_normalizer.py` (10 tests), `tests/test_procore_cli_sync_observation_dry_run.py` (3 tests), `tests/test_procore_observation_sqlite_idempotency.py` (3 tests). Pytest 770/770 + 1 skipped, ruff/mypy/compileall green. See evidence `docs/evidence/construction-intelligence-phase-04/04-observation-sync-dry-run.json` (deterministic sync plan receipt + normalized preview + canonical field keys × 2 entities + safety_routing_summary + endpoint_posture + guardrail attestation). Minimal pointer only (surgical).

**Phase 04 Prompt 07 (2026-05-28):** Meeting + meeting-topic dry-run sync normalization — two new candidate endpoints added to `resources/config/procore_endpoint_contract.seed.yaml`: `list-meetings` (path `/rest/v1.0/projects/{project_id}/meetings`, category `meetings`, sensitivity `medium`) and `list-meeting-topics` (path `/rest/v1.0/projects/{project_id}/meetings/{meeting_id}/topics`, category `meeting-topics`, sensitivity `medium`) — both `verification_status: candidate`, both `is_live_eligible: false`. **Structural deviation**: meetings and topics are two separate Procore endpoints (not a parent payload with nested children like RFI replies / observation comments), so each gets its own normalizer + dispatch entry. New `src/hb_assistant/procore/normalizers/meeting.py` with four pure functions: `normalize_meeting()` (metadata-only; whitelist `canonical_fields` number/title/status/start_time/end_time/location/organizer_id/project_id/source_url/created_at/updated_at; review heuristic on status + title only — no body scan); `normalize_meeting_topic()` (id-keyed independent rows; canonical_fields id/title/status/sequence_number/assignee_id/due_date/parent_meeting_id/source_url/created_at/updated_at; **four-field scan** of status + title + description + action_items for fragments (legal/financial/hold/dispute/review/escalat + claim/delay/cost/safety/incident/injury/corrective/near-miss/unsafe/violation/PPE/fall); description and action_items both reduced to SHA-256 hash-only summaries; `safety_route: true` when a safety-specific fragment fires); two `_payload_block` functions returning one-tuples. `sync.py` registers both endpoints in `NORMALIZER_DISPATCH`, adds two parallel apply() branches, threads `meeting_preview_payload` and `meeting_topic_preview_payload` through `plan()` and `run_sync()`. Fixtures `MEETING_SAMPLE_PAYLOAD` (3 meetings) + `MEETING_TOPIC_SAMPLE_PAYLOAD` (4 topics with one safety body trigger + one claim-title generic review) added to `construction/fixtures/procore.py`. `procore validate` extended to 23 checks (new `meeting_normalizer_dispatch_present` covers both endpoints); contract count rises to 16 endpoints (10 verified + 1 excluded + 2 deferred + 3 candidate). New `tests/test_procore_meeting_normalizer.py` (14 tests), `tests/test_procore_cli_sync_meeting_dry_run.py` (5 tests), `tests/test_procore_meeting_sqlite_idempotency.py` (3 tests — the topic test also patches the path_template to strip the `{meeting_id}` placeholder so apply()'s two-arg format call succeeds in the test environment). Pytest 792/792 + 1 skipped, ruff/mypy/compileall green. See evidence `docs/evidence/construction-intelligence-phase-04/meeting-sync-dry-run.json` (deterministic sync plan receipt spanning both endpoints + normalized preview × 2 entities + canonical field keys + safety_routing_summary + endpoint_posture × 2 + guardrail attestation). Minimal pointer only (surgical).

**Phase 04 Prompt 12 (2026-05-28):** Final closeout summary + fresh-session handoff (docs-only). Two new artifacts: `docs/evidence/construction-intelligence-phase-04/06-final-closeout-summary.md` (canonical 8-section closeout mirroring Phase 03 shape — HEAD before/after, commit arc 00–11, commands run, outputs summarised, guardrails matrix, residual risk, Phase 05 entry recommendations 05-A → 05-E) and `docs/evidence/construction-intelligence-phase-04/session-handoff.md` (7-section fresh-session handoff + Prompts 00–11 arc appendix with commit SHAs). Acceptance posture: **Phase 04 — ACCEPTED-WITH-DEFERRALS — 2026-05-28**. Manifest stays at 1.3.0. Minimal pointer only (surgical).

**Phase 04 Prompt 11 (2026-05-28):** Full validation & evidence closeout — verification-only prompt; no functional code changed. Re-ran the README baseline block (`pytest 831/1 skipped`, `ruff/mypy/compileall` green, `procore validate --json` at 26 checks / 25 passing with the lone `mapping_consistent` failure carried over from Phase 03), inventoried + fingerprinted all 12 prior Phase 04 evidence artifacts with per-file leakage scan (zero hits), and re-ran `tests/test_repo_sensitive_scan.py` clean. See evidence `docs/evidence/construction-intelligence-phase-04/05-validation-final.md` (9-section attestation — baseline / validation suite / CLI envelopes / evidence inventory + SHA-256(12) fingerprints / sensitive-scan attestation / validate-check trajectory / deferral ledger / stop-condition matrix / **ACCEPTED-WITH-DEFERRALS** acceptance line). Minimal pointer only (surgical).

**Phase 04 Prompt 10 (2026-05-28):** Obsidian register preview — fills the rendering gap left by Prompts 04–08. Two new builder methods on `ProcoreObsidianRenderer` (`build_observation_register`, `build_meeting_register`) plus a rewrite of `build_daily_log_index` to match the section-aware categories now emitted by `normalize_daily_log_section_item` (filter `category LIKE 'daily_log_%'`). Two new templates (`resources/templates/procore_observation_register.template.md`, `procore_meeting_register.template.md`); the daily-log index template columns are updated to `Log Date | Section | Bucket | Review | Safety | Body Hash | Source`. Two new marker pairs registered in `_PROCORE_MARKERS` (`HB-PROCORE-OBSERVATION-REGISTER:START/END`, `HB-PROCORE-MEETING-REGISTER:START/END`). `_query_synced_entities` gains an optional `category_like` kwarg; existing exact-match callers unchanged. `procore_obsidian_preview()` now builds 10 templates (was 8); the apply path writes 2 new procore artifacts using the same `_write_procore_artifact` atomic + marker-bounded pattern. Safety-routed observations (`safety_route: true` from the normalizer) and meeting topics carrying YAML rule-keyword content (e.g. `settlement`) are excluded from registers and surfaced via `_make_review_item` into the existing review-required collector. `procore validate` extended to 26 checks (new `obsidian_renderer_phase_04_register_coverage` asserts the 5 family builders are callable). New / extended tests in `tests/test_procore_obsidian_output.py` cover routing, byte-level idempotency, marker-bounded write idempotency, source-link presence, and zero raw-literal leakage. Pytest green; ruff/mypy/compileall green. See evidence `docs/evidence/construction-intelligence-phase-04/obsidian-register-preview.md` (builder coverage + renderer/writer idempotency attestations + source-link demonstration + redaction attestation + stop-condition matrix + verification fingerprint). Minimal pointer only (surgical).

**Phase 04 Prompt 09 (2026-05-28):** Sensitive routing and redaction proof — declarative parity between the per-entity normalizer routing shipped in Prompts 04–08 and `resources/config/procore_sensitive_routing_rules.yaml`: appended five family-scoped rules (`procore-rfi-legal-or-contractual`, `procore-submittal-financial-or-legal`, `procore-observation-safety`, `procore-meeting-sensitive-topic`, `procore-daily-log-personnel-pii`) covering the same trigger keywords the normalizers already use. New `mask_pii_in_excerpt()` helper in `src/hb_assistant/procore/redaction.py` (mask-then-truncate; emails → `[email-redacted]`, US-shaped phones → `[phone-redacted]`, ≥20-char alphanumeric/JWT-like tokens → `[token-redacted]`) — used only by the evidence artifact, not the normalizers (`_hash_summary` boundaries unchanged). New `PHASE_04_SENSITIVE_TEXT_BLOBS` + `SENSITIVE_SYNTHETIC_{EMAIL,PHONE,TOKEN}` fixtures (`example.invalid` / `555-010-0001` / `syntheticfixturetoken*`). New parameterized test `tests/test_procore_sensitive_routing_proof.py` proves per-family: `review_required=True`, non-empty `routing_reason`, observation `safety_route=True`, hash-summary block present, and zero raw blob / email / phone / token leakage into the serialized record. `procore validate` extended to 25 checks (new `sensitive_routing_rules_cover_phase_04_families` asserts each of {rfi, submittal, observation, meeting, daily-log} appears in a rule_id). See evidence `docs/evidence/construction-intelligence-phase-04/sensitive-routing-proof.md` (routing table + invariant attestations + masked-excerpt demonstration + static-scan attestation + stop-condition matrix + verification fingerprint). Minimal pointer only (surgical).

**Phase 04A Prompt 01 (2026-05-28):** Live-readiness hardening — first functional pass of Phase 04A before any transport wiring. New `src/hb_assistant/procore/live_gate.py` exposes `LIVE_ENV_VAR="HB_PROCORE_LIVE"`, `LIVE_ENV_ENABLER="1"`, `LiveEnvNotSet(ProcoreAPIError)`, `live_env_active()`, `require_live_env(*, command)`, and `assert_live_mapping_strict(registry, target_keys)`. The env-var gate is *exact-match* (any value other than `"1"` is treated as inactive — no truthy parsing). Wired into the live branches of `procore audit execute --confirm` (after confirm verification; exit 2 on `LiveEnvNotSet`) and `procore sync run --apply --confirm` (after confirm verification, before any HTTP; exit 2 on `LiveEnvNotSet`, exit 3 on strict-mapping violation). Dry-run paths (default `sync run`, `audit dry-run`, `tools list`, `mapping validate`, `obsidian preview`, etc.) are untouched. `procore validate` extended to 28 checks (new `live_env_gate_module_present` import-check + `live_commands_require_env_var` AST-check); `tests/test_procore_cli_validate.py` assertion bumped 26 → 28. New `tests/test_procore_live_gate.py` (20 tests): helper semantics, parametrized non-enabler values, strict-mapping rejection of pending/unknown targets, CLI negative-path exit codes, dry-run unaffected attestation. `pyproject.toml` narrowly extends the strict mypy override to `hb_assistant.procore.live_gate` only — broader `hb_assistant.procore.*` strict-coverage remains a separate hardening project. Endpoint catalog (10/3/1/2 = 16), `mapping_consistent` (Phase 03 residual, 05-C), and historical Phase 04 evidence files unchanged. Pytest 851 passed + 1 skipped; ruff/mypy/compileall green; sensitive-scan + offline-boundary + client-secret-isolation PASS. Manifest stays at 1.3.0. See evidence `docs/evidence/construction-intelligence-phase-04a/01-live-readiness-hardening.md` (12-section attestation with sub-area reconciliation table + smoke verification). Minimal pointer only (surgical).

**Phase 04A Prompt 00 (2026-05-28):** Procore Live Enablement arc opens at Phase 04 closeout baseline `e90a5e2`. Verification-only audit — no functional code changes, no live calls, no surface or guardrail mutations. Establishes the canonical evidence directory `docs/evidence/construction-intelligence-phase-04a/` and attests current state: command surface (9 groups; no `live` group yet), HTTP transport posture (default raises `transport_not_injected`; UA `HB-Personal-Assistant/1.3.0 (GET-only)`), OAuth helper references (Keychain entry `hb-assistant-procore` + `PathPolicy().get_auth_dir() / procore_token.json`), validate trajectory (26 checks; 25 pass + `mapping_consistent` deferred), endpoint catalog (10 official_docs_verified / 3 candidate / 1 excluded / 2 deferred = 16), pilot mapping (4 pilot + 2 pending = 6). Pytest 831 passed + 1 skipped; ruff/mypy/compileall green; sensitive-scan + offline-boundary + client-secret-isolation regressions all PASS. Phase 04 deferrals carried forward as items 05-A (production-wired requests transport), 05-B (candidate-endpoint promotion), 05-C (`mapping_consistent`), 05-D (`{meeting_id}` placeholder generalization), 05-E (normalizer return-shape consolidation). Manifest stays at 1.3.0. See evidence `docs/evidence/construction-intelligence-phase-04a/00-rebaseline-readiness.md` (15-section attestation). Minimal pointer only (surgical).

**Phase 04 Prompt 08 (2026-05-28):** Daily log selection scope and dry-run — completes the Phase 04 entity arc (RFI / Submittal / Observation / Meeting / Daily Log) with a section-selection mechanism rather than a single-shape normalizer. `list-daily-logs` is already in the active contract as `official_docs_verified` / `is_live_eligible: true` (no contract changes). New `resources/config/procore_daily_log_selection.seed.yaml` declares three buckets: **selected_sections** (counts, weather, manpower, dcr, delivery — persist with whitelist canonical_fields + `review_required=False`); **review_only_sections** (notes — `review_required=True` + `daily_log_review_only_section` routing reason + SHA-256 hash-only `body_summary`); **routed_to_review_sections** (accident, injury, delay, safety_violation — `review_required=True` + `safety_route=True` + SHA-256 hash-only `body_summary`). Bucket assignment is **structural, not content-derived**, so the stop conditions ("broad sync persists all sections", "accident/injury/delay text enters normal rows", "notes bypass review") are structurally unsatisfiable. New `src/hb_assistant/procore/daily_log_selection.py` mirrors the existing loader pattern (`HB_PROCORE_DAILY_LOG_SELECTION` env override + `config/<name>.yml` repo override + Pydantic `ProcoreDailyLogSelection` model with cross-bucket id / payload_key / category uniqueness invariants). New `src/hb_assistant/procore/normalizers/daily_log.py` demultiplexes a daily log payload into per-section canonical rows; returns a `dict[str, list[dict]]` (category → rows) rather than a tuple (multi-section shape deviates from prior normalizers). `sync.py` registers `list-daily-logs` in `NORMALIZER_DISPATCH`, adds a parallel `apply()` branch that loads the selection scope and persists per-section rows with distinct `<section>-<parent>-<id>` stable keys, threads `daily_log_preview_payload` through `plan()` and `run_sync()`, surfaces a new `would_persist_sections_separately` flag in dry-run receipts. Receipt entry includes `records_by_category`, `review_required_count`, `safety_route_count`. Fixture `DAILY_LOG_SAMPLE_PAYLOAD` (2 daily logs spanning all 10 section types; second log carries entries in all four routed-to-review buckets) added to `construction/fixtures/procore.py`. `procore validate` extended to 24 checks (new `daily_log_selection_and_dispatch_present` covers dispatch wiring + scope loadability + all three buckets populated). New `tests/test_procore_daily_log_selection.py` (7 tests), `tests/test_procore_daily_log_normalizer.py` (10 tests), `tests/test_procore_cli_sync_daily_log_dry_run.py` (4 tests), `tests/test_procore_daily_log_sqlite_idempotency.py` (3 tests). Pytest 816/816 + 1 skipped, ruff/mypy/compileall green. See evidence `docs/evidence/construction-intelligence-phase-04/daily-log-selection-scope-proof.md` (markdown proof — Selected / Review-only / Routed-to-review tables + fixture-derived per-category counts + stop-condition attestation table + guardrails block + verification matrix). Minimal pointer only (surgical).

**Phase 06A (SharePoint/OneDrive File Intelligence) — Prompts 00–17 (2026-05-30) — CLOSED:** Complete read-only delegated-Graph file-intelligence track (sibling of the Phase 06 email track) under `hb-assistant graph files`: the `graph/files_endpoint_guard.py` no-writeback guard + `no-writeback-proof`; canonical V5 source projection; SharePoint site/drive + OneDrive discovery; user-provided link → canonical-ID resolution (read-only Shares API, no redemption); rich driveItem indexing, bounded crawl, hardened delta (delta links surface only as SHA-256 fingerprints); project matching; ingestion-eligibility policy; controlled drive-aware download + bounded redacted extraction; sensitive-file review routing; grouped marker-bounded Obsidian manifests/registers/receipts; and source-linked retrieval over bounded excerpts. Additive schema **V15–V19** (final). Closeout (P17) ran the full validation matrix: ruff/`mypy src`/compileall clean, `construction-agent validate` 4/4, `no-writeback-proof` 19 mutations blocked / 0 mutating calls; pytest 1698 passed with 12 **pre-existing email-track** failures handed to that track (0 files-track failures). Broad `Files.ReadWrite.All` permission tightening stays **deferred** (documented risk: `22-deferred-permission-tightening-record.md`, `02-graph-auth-permission-posture-deferred.md`). See `18-sharepoint-onedrive-file-intelligence-phase-06.md` and `docs/evidence/construction-intelligence-sharepoint-onedrive/` (00…22, 16–17). Minimal pointer only (surgical).

**Phase 04B (Prompts 02–12, 2026-05-29) — Procore Second-Brain Enrichment — CLOSED:** Turned the Phase 04A flat latest-state sync into a queryable second brain. V7 migration (additive; `apply()` → 7) added 18 tables: history (state-index / snapshots / change-events / timeline), cross-cutting enrichment (people/company/location/attachment/custom-field entities, record edges, action signals, text intelligence + Fernet text vault), and the inspection family. Per-family enrichment projections (`store/procore_{history,enrichment,inspection_projection,meeting_projection,rfi_projection,submittal_projection,punch_projection,observation_projection,schedule_projection}.py`) are wired as guarded post-upsert dispatch in `live_sync.py`; daily-log enrichment + the three required signals (`daily_delay_reported` / `daily_note_review_required` / `daily_manpower_anomaly`) live in `normalizers/daily_log_live.py`. Added a daily-log date-window capability (`--start-date`/`--end-date`; live-proved Tropical daily logs carry data with a date filter where a no-filter query returned 0). Read-only query commands `procore live history|changes|timeline|actions|coverage` (+ `time_window.parse_since`, `coverage.compute_payload_coverage`) never call Procore. `procore obsidian enriched` registers eight redacted views into one marker-bounded `01_Projects/<project>.procore-memory-register.md`. Closeout validation: pytest 1112 passed / 2 skipped, ruff/mypy/compileall green, `procore validate` 28/28, 27 endpoints live-verified, scan-sensitive clean; all acceptance criteria (snapshots / change-events / timeline / inspection-No-Response signal / coverage omitted-field detection / no-network query commands / no raw-body persistence) PASS. See arch doc `15-procore-second-brain-phase-04b.md` and evidence `docs/evidence/construction-intelligence-phase-04b/00-…12-…` (12-final-validation-closeout.md is the acceptance record). Minimal pointer only (surgical).

**Phase 07A Prompt 00 (2026-05-31):** Repo truth rebaseline + full SQLite table lifecycle inventory (V19, 82 user tables, 9 families + legacy pilot + misc evidence). Captured current HEAD `cb52666` / v1.3.0 / V19 (runtime confirmed). CLI surfaces inventoried (construction-agent ~35 cmds, procore ~45+ with live/obsidian/06B read models, graph mail+files ~20+ with no-writeback gates). Evidence tree (no 07a dir pre-existing); README caps at 06A, arch 00-README historical only, 05-store doc V5-era (major drift noted). Full schema family extraction via Grep on migrator + repositories legacy 4; reconciled vs package 03_ report (82/53/29, explicit empties, procore-on-tropical strength, calendar/embeddings/thread-summaries blockers). Classification into 7 contract statuses (operational_populated 53, 6 blocking for 07A/07B, 8 legacy_superseded, 1 placeholder_deferred, ~14 evidence_only). Evidence: `docs/evidence/construction-intelligence-phase-07a-data-quality/{00-repo-truth-rebaseline.md,01-table-lifecycle-inventory.{md,json}}` (machine-readable + human matrix with V#/report refs). No schema/runtime changes. Validation matrix executed post-write (green). Architecture drift captured; 05-store left as historical (update deferred). See 00- + 01- artifacts + package contract + 02_/03_ summaries. Minimal pointer only (surgical).

**Phase 07A Prompt 01 (2026-05-31):** Additive V20 migration (six new tables + eight indexes) + ConstructionStore adapters for data-quality runs, table lifecycle registry, source-system record map, relationship resolution queue, project coverage mart, and gate results. All tables carry the Phase 07A CHECK guardrails (raw_body_persisted=0, external_writeback=0, full_text=0). New focused test + proof evidence. Architecture drift note only; no CLI surface yet. All validation commands executed under activated `.venv`. See `docs/evidence/construction-intelligence-phase-07a-data-quality/02-v20-schema-and-repository-proof.md`. Minimal pointer only (surgical).

**Phase 07A Prompt 02 (2026-05-31):** Local-only canonical project identity backfill (6 projects from registry + Procore mapping + cross-source signals via email/Graph/Procore live). New `construction/data_quality/` package + `data-quality` CLI subgroup (`project-coverage --json`; dry-run default, --apply for upserts to V5 tables with conflict review flagging). Coverage matrix emitted (real counts for tropical etc.). No live calls, no destructive changes, conflicts never auto-resolved. New test + evidence (03- + matrix.json). Architecture note only. Validation green under venv. See `docs/evidence/construction-intelligence-phase-07a-data-quality/03-project-identity-population-proof.json`. Minimal pointer only (surgical).

**Phase 07A Prompt 03 (2026-05-31):** Local-only source-system record map (Prompt 03) for 7+ tables (procore_live + financials, email_messages + project_matches, construction_drive_items + ingestion_decisions, body_vault_refs) into V20 `source_system_record_map` using deterministic canonical IDs (`{system}:{table}:{stable_key}`) + Prompt 02 identities or row signals (project_number_detected etc.). New `source_record_map.py` + `data-quality source-record-map --dry-run/--apply --json` (explicit flags, no-flag=dry-run, mutual-exclusion error). 1828+ pilot records emitted as unmapped with reason codes (never silently ignored; "pilot_source_unmapped"). 1 reusable repo helper (list_procore_live_records) + direct SQL in builder. New test (temp DB + CLI modes) + 04-proof + unmapped-source-records.json. Architecture note only. All venv-prefixed validation green. See `docs/evidence/.../04-source-record-map-proof.json`. Minimal pointer only (surgical).
**Phase 07A Prompt 04 (2026-05-31):** Local-only relationship orphan and confidence diagnostics (Prompt 04) across Procore edges (action_signals + timeline/change events as record-to-entity/record-to-record), email relationship candidates, Graph file/project matches, and source-record-map cross links. New `construction/data_quality/relationships.py` + `data-quality relationships --json`. Classifies per exact 08_ categories; confidence from policy JSON; model_proposed/weak/sensitive always review_required=1 and never auto-promoted (hard guard + explicit test proof). Separate deterministic_orphan_rate and candidate_orphan_rate (never combined) per 08_ + 09_ expectations. 0 repo helpers (direct + existing public APIs). New test + 05-json + orphan-rate-report.md. Architecture note only. All venv-prefixed validation green. See `docs/evidence/construction-intelligence-phase-07a-data-quality/05-relationship-quality-diagnostics.json`. Minimal pointer only (surgical).

**Phase 07A Prompt 05 (2026-05-31):** Additive V21 migration (three new marts: source_record_summary_mart, relationship_quality_mart, cross_domain_context_readiness_mart; project one reused from V20) + `construction/data_quality/marts.py` (MartBuilder populating all four + latency instrumentation for the 8 target local-agent queries, target 500 ms). `data-quality marts --json` populates + emits counts + measured latencies. 3 new upsert methods in repositories. New test + 06-proof + query-latency-results.json (all 8 <500 ms). Architecture note only. All venv-prefixed validation green. See `docs/evidence/construction-intelligence-phase-07a-data-quality/06-agent-ready-query-marts-proof.json`. Minimal pointer only (surgical).

**Phase 07A Prompt 06 (2026-05-31):** Marker-bounded Obsidian data-quality outputs (Prompt 06). New `construction/data_quality/obsidian.py` (ObsidianDataQualityRenderer + 4 renderers using duplicated _ensure_markers/_replace_bounded/_atomic_write_text; queries direct on V20/V21 marts + source_system_record_map + relationship_resolution_queue using only redacted columns; frontmatter + HB-DATA-QUALITY-* markers). `data-quality obsidian --dry-run --json` (default) writes repo evidence preview + proof JSON (07-obsidian-output-preview.md, obsidian-data-quality-dry-run.json) with full guardrail attestation and stop-condition checks. Optional `--apply` writes the 4 notes to the local vault under "Construction Intelligence/Phase 07A Data Quality/" (atomic, preserves non-marker user text). 0 new repo helpers. New test with leakage assertions + CLI subprocess. Architecture note + evidence only. All venv-prefixed validation green. See the two new evidence files under `docs/evidence/construction-intelligence-phase-07a-data-quality/`. Minimal pointer only (surgical).

**Phase 07A Prompt 07 (2026-05-31):** Data Quality Gates and Phase Go/No-Go (Prompt 07). New `construction/data_quality/gates.py` (GateEvaluator loading thresholds from resources/json/data_quality_gate_thresholds.json, 13 gates computed defensively from V20/V21 marts + source_system_record_map + relationship_resolution_queue + project identity + latency + prior attestations; classification into pass/warning/fail_blocking/deferred_not_blocking/not_applicable with explicit future_phase assignments). Persists via the pre-existing insert_data_quality_gate_result (0 new repo helpers). `data-quality gates --json` returns rich report + phase_go_nogo (07B blocked by calendar/email, 07C by documents, 08B by financials; hard "meeting_prep_readiness_claim": "blocked" while dependent gates not pass). New test (classification matrix + stop-condition enforcement + CLI subprocess). Generated 08-*.json + .md evidence + one-paragraph arch note. All venv-prefixed validation green (including the new gates command in the 11_ matrix). Minimal pointer only (surgical).

**Phase 07A Prompt 08 (2026-05-31):** No Writeback / No Secret / No Raw Body Proof (Prompt 08). New `construction/data_quality/safety.py` (build_data_quality_no_writeback_proof that re-uses the shared `_scan_text_for_secrets` + patterns from the Procore no-writeback prover; 5 checks covering the exact 6 07A data_quality modules, the V20/V21 tables they touch, and the 07A evidence tree). Report-only CLI `data-quality no-writeback-proof --json`. All mutation/secret scans on the 07A modules and evidence passed cleanly. Honest reporting of DDL completeness findings on marts created via defensive IF NOT EXISTS (no hidden guardrail violations). New focused test + two evidence artifacts (09-*.md + no-writeback-proof.json) + one-paragraph arch note. All venv-prefixed validation green. Minimal pointer only (surgical).

**Phase 07A Prompt 09 (2026-05-31):** Final validation and closeout (Prompt 09). Executed the complete safe validation matrix (12 commands) + every 07A data-quality command (full outputs + exit codes captured). Confirmed all 00-09 evidence present and clean (zero raw bodies/tokens/secrets/signed URLs in final high-precision sweep across 18 files). Produced the three required deliverables: 10-final-validation-closeout.md (truthful matrix, guardrails attestation, known limitations with phase assignments, explicit statement that 07D/meeting-prep is not ready), phase-07a-validation-summary.json (aggregate metrics, gate results, proof flags), and phase-07b-07c-07d-handoff.md (structured recommendations for 07B/07C/07D + 08A/B + open decisions from 12_ register). One-paragraph append to this file only. Conventional prompt-scoped commit on main whose only final output is the commit message. All venv-prefixed commands. 07A closed with integrity; no overstatement of readiness. Minimal pointer only (surgical).

**Phase 07B (Prompts 00–13, 2026-05-31) — Calendar & Email/Thread Intelligence — CLOSED:** Built the calendar/email/thread/candidate intelligence stack on the redacted local-first foundation (additive schema V20→V23; no Microsoft 365 mutation, guard-enforced read-only at every layer). Calendar: a positive-allowlist endpoint guard + read-only Graph status, bounded `calendarView` event indexing into redacted V23 `calendar_event_index`/attendees/crawl-runs (body/join-URL-free; private events review-flagged), and deterministic-number-hash + heuristic name-token calendar→project candidates (candidates only). Email/thread: V14 `email_model_classifications` persistence (advisory-only, CHECK-locked), metadata-only `email_thread_summaries` materialization with controlled in-memory body-context + review routing, and time-window + organizer-domain calendar↔email relationship candidates (`meeting_email_relationship_candidates`, no auto-promotion). Surfacing: read-only project correspondence previews/warnings, and one marker-bounded, leak-scanned Obsidian calendar/email register per project (no one-note-per-item). Governance: manifest-driven 07B data-quality gates (calendar/email/thread/candidate presence + a structured `meeting_prep_readiness` prerequisite check, `auto_readiness_allowed=false`) and an extended no-writeback/no-secret/no-raw-body proof now covering the V11/V14/V23 modules, guard CHECK columns, persisted content, and 07B evidence (fail-closed). Closeout validation: compileall/ruff check/mypy/safe-pytest all green, every `--json` CLI probe + gates + no-writeback-proof exit 0; live, the four 07B gates pass while `meeting_prep_readiness.ready=false` (blocked by the 07C document gate + a relationship gate) — **07D/meeting-prep is explicitly not claimed ready**. See arch docs `20-29` and evidence `docs/evidence/construction-intelligence-phase-07b-calendar-email/00-…13-…` (13-final-validation-closeout.md is the acceptance record; phase-07c-07d-handoff.md scopes the next phases). Minimal pointer only (surgical). + Prompt 15 follow-up (mail discover batch + per-proj diags + lifecycle fix; calendar chunked larger-window apply + per-ev diags + checkpoints + safe final + partial status/ok; all outside MCP, bounded/scoped preserved, no raw/no-writeback). See appended subsections in 148/21/04/00.

**Phase 09 (Prompts 12–39+, 2026-06+) — Retrieval Memory Quality + LlamaIndex surfaces (ongoing):** Phase 09 retrieval (LlamaIndex optional layer for semantic/hybrid over approved manifest nodes from obsidian + memory + generated-outputs loaders; V38 substrate; policy-gated dry-run/apply with vectors outside SQLite + metadata receipts only; hybrid deterministic+advisory semantic; truthful readiness). Key surfaces: `second-brain retrieval llamaindex status|build|build-proof|build-apply-proof`, `hybrid status|search|proof`; embedding-policy; approved-sources etc. Core (`llama-index-core` via `retrieval` extra) for status/dry-run/proofs (MockEmbedding); local HF embedding via `retrieval-local` for default `local` provider on real `--apply`/semantic. Follow-up made readiness truthful: split core/local probes + runtime_ready in llamaindex_config; plan/status/hybrid carry `sdk_available` (core) + `local_embedding_available` + precise blockers (`sdk_not_available`, `local_embedding_not_ready`, `semantic_*` variants); apply/hybrid gates + HF imports guarded; tests cover both branches; docs updated (no contract/seed/schema change). Modeled on MCP state-aware truthful fix (121). See arch 132 (13), 137 (18), 138 (19), 139 (20), 133 (14 cross), 120/121/131, runbook `phase-09-retrieval-llamaindex-setup.md`, pyproject extras comment. Base install fully clean; --apply/semantic fail-closed on missing pieces with honest reasons. Prompt 34: review cluster usefulness without added operator burden (review_burden_mart: non-dedup SELECT now projects project/reason/category/status/conf/tier cols from email/construction/memory/relationship/document queues + improved pk/conf/reason extraction+norm so review_reason_code carries table values e.g. "routine follow-up" not "unknown"; splits email/unclassified/unknown etc; build_review_burden_proof: removed or-True so advisory_retrieval_allowed asserted truthfully; added explicit True/False INSERT fixture cases in policy+advisory tests; caps, high_impact always-visible summary, hash-only deduped top_examples, high-impact promotion block all preserved; no schema/CLI/daily_brief changes). See 151 append (modeled on 121 truthful precedent). Minimal pointer (surgical; ongoing phase).
