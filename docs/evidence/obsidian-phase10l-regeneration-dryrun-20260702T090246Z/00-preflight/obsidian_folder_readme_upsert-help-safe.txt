usage: obsidian_folder_readme_upsert.py [-h] --vault-path VAULT_PATH [--apply]
                                        [--confirm-vault-path CONFIRM_VAULT_PATH]
                                        [--backup-dir BACKUP_DIR]
                                        [--json-output JSON_OUTPUT]
                                        [--local-sensitive-dir LOCAL_SENSITIVE_DIR]

Upsert singleton folder READMEs + report duplicate variants (dry-run default).

options:
  -h, --help            show this help message and exit
  --vault-path VAULT_PATH
  --apply
  --confirm-vault-path CONFIRM_VAULT_PATH
  --backup-dir BACKUP_DIR
  --json-output JSON_OUTPUT
  --local-sensitive-dir LOCAL_SENSITIVE_DIR
