# 10 — Audit event proof

Audit directory: `/volume1/personal-assistant/app-support/audit/mcp`

- mode **700**, owner **personal-assistant-svc:users**
- JSONL files mode **600** after writes

Events recorded for allowed and denied MCP tool calls during tunnel probe (tool names, decisions, durations — metadata only).

Sample captured in apply session (redacted); no secrets in committable evidence.
