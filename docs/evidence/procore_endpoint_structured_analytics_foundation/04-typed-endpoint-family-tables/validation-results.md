# Validation Results

Structured endpoint-family tables are created by fresh migration and populated by copied-DB
legacy reprocessing. Targeted tests prove mapper idempotency, raw payload linkage, typed fields,
dimension rows, and the rule that raw JSON-only capture is not sufficient.
