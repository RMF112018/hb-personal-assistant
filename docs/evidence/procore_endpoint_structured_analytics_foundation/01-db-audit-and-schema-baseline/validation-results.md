# Validation Results

The copied DB passed `integrity_check` and `quick_check`. Source schema was V45 before package
migration. Current Procore volume was confirmed from the copied DB during coverage validation:
`30,059` `procore_live_records` rows.
