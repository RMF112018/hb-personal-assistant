# 02 Structured Endpoint Data Contract

The endpoint contract is implemented in `hb_assistant.procore.structured_analytics`.

- Endpoint count: `59`.
- Raw landing table: `procore_endpoint_raw_payloads`.
- Structured table count: `42`.
- Live verified endpoints missing structured table mappings: `0`.
- Explicit deferred endpoint: `budget-details`.
- Acceptance gate: `structured_endpoint_family_tables_required`.
- `raw_json_only_is_sufficient`: `false`.
