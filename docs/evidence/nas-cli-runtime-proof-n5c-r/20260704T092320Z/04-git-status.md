# 04 — Git Status

## At N5C-R finalize (local repo, not the NAS export)
- Branch: `ops/nas-copied-db-n3-20260704T060648Z`
- HEAD: `3735e35c` — `docs(nas): add N5C local evidence stack consolidation`
- Ahead / behind `origin/main`: **12 / 0**
- Working tree: this N5C-R evidence directory is **untracked / uncommitted**:
  ```
  ?? docs/evidence/nas-cli-runtime-proof-n5c-r/
  ```
- Push status: local branch only; never pushed; no PR.

## NAS N4C repo (separate, on the NAS — not a git clone)
- `<nas>/runtime/n4c-backend-smoke-<ts>/repo` has **no `.git`** → no commit/branch to report. It is a source export
  created by the operator; treated read-only this phase.

## Posture
Evidence written and **left uncommitted** pending separate authorization. Never push; no PR.
