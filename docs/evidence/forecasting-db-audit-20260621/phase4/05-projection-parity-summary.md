# Projection Parity Summary

Phase 4 additions:

- `mode` parameter (warn/strict) on `run_projection_parity_gate`
- Per-record checks: status, amount (`grand_total`), updated timestamp
- Expected PO drift: `expected_financial_only_keys` (info)
- Adjusted PO row-count comparison
- Design doc: `../phase4-projection-parity-design.md`