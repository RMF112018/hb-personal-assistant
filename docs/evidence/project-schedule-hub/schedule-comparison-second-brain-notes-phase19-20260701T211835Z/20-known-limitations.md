# Known limitations

- Note bodies embed memo export markdown when available; very large exports may produce long notes.
- Portfolio snapshot uses thin per-project trust slices (same limitation as Phase 18 dashboard).
- Live vault writes require explicit `--write-notes --confirm-vault-write` (not exercised in committed evidence).
- Optional Qwen advisory is not captured in evidence (gated; default off).
- Phase 19 does not link notes into Obsidian graph (deferred to Phase 20).
