# Frontend Proof — Phase 6

## Components

- `ScheduleControlsPanel.tsx` — posture, top controls, advisory footer, basis toggle
- `ProjectSchedulePage.tsx` — panel placement after trust banner; shared comparison basis

## Tests (fixture/mocked API)

- `ProjectSchedulePage.test.tsx` — controls panel, advisory text, Controls Trend Analytics heading, no raw ID in top controls, basis on driver links
- `scheduleApiAsOf.test.ts` — controls `as_of` + `comparison_basis` query params

Proof type: **mocked frontend API data** (not live backend)
