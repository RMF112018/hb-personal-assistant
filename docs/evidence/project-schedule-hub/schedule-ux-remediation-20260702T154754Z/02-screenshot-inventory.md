# 02-screenshot-inventory.md — Project Schedule UX Remediation

**Stamp**: 20260702T154754Z  
**Branch/Worktree/HEAD**: (see 00-baseline.md)  
**Status**: Pre-change + planning inventory. Actual remediation screenshots captured in Phase 6 (after code changes) using copied DB + tropical route.

## Pre-Remediation / Reference Screenshots (from repo truth)
No new screenshot package was supplied with this task. Baseline understanding draws from:
- Code inspection of current render (ProjectSchedulePage, nav, ReviewCueCard, visualizations, etc.).
- Prior evidence trees under `docs/evidence/project-schedule-hub/` (e.g.):
  - `baseline-pm-readiness-phase12-20260701T081419Z/screenshots/` (6 pngs: pm walkthrough, before/after visuals for schedule surfaces).
  - `driver-route-encoding-phase11-20260701T075049Z/screenshots/` (5 pngs showing driver detail + nav context).
  - `named-baseline-*-phase*` trees (multiple pngs of workbench, baseline selectors, controls, overview states).
  - `phase-*-schedule-workbench-*`, `schedule-controls-*`, `named-baseline-workbench-*` etc. (dense cards, badge examples, early nav shots).
- These prior shots illustrate the problems this remediation targets: flat nav, early technical panels, badge-heavy review cues, "Not yet available" states, WBS strings, etc.

## Planned / Required Post-Remediation Screenshots (to be captured in Phase 6)
Store under `screenshots/` in this evidence dir. Minimum set (8+) with what each proves:

1. `01-dropdown-closed.png` — Project page with Schedule trigger visible in subnav (closed state). Proves: Schedule still appears as a first-class item; no regression of other tabs.
2. `02-dropdown-open.png` — Dropdown expanded showing exactly: Overview, Import Schedule, Review Workbench, Driver Detail, Activity Drivers (with correct hrefs visible or inspected). Proves: all 5 nested pages directly discoverable from shell; keyboard/ARIA menu present.
3. `03-overview-top-pm-first.png` — Top of /schedule after reorg (Schedule Status/Story card + Primary Actions row with prominent "Import Schedule" CTA + baseline context). Proves: PM-facing content leads; import is primary action; technical (trust/controls) moved lower.
4. `04-import-reached-from-nav.png` — /schedule/import loaded via click from dropdown. Proves: Import Schedule route reachable from shell nav; dedicated page is the primary workflow surface.
5. `05-workbench-reached-from-nav.png` — /schedule/workbench loaded via dropdown. Proves: Review Workbench directly discoverable; active Schedule state on nested route.
6. `06-baseline-comparison-section.png` — Baseline / Comparison Context area (labels + explanatory copy for Current Contract / Previous Progress / Secondary + what changing basis affects). Proves: PM-friendly language and explanations present.
7. `07-workbench-card-polish.png` — Example review cue card(s) in workbench (reduced badges, clear distinction preview vs persisted, distinct "Show detail" / "Open Driver Detail" / disposition / Save notes controls). Proves: chip overload reduced; action hierarchy clearer.
8. `08-technical-collapsed.png` — Overview with Technical evidence (or advanced trends) collapsed/secondary. Proves: provenance moved out of primary PM visual flow.
9. (Bonus) `09-active-state-on-nested.png` or series showing Schedule trigger active while on import/workbench/driver-detail/drivers.
10. Any error/empty states (no-schedule with import CTA, drivers index empty state) if relevant for completeness.

**All screenshots will use the copied validation DB only** (see 00-baseline + 06-operator-validation-steps for exact copy + export + launch commands). No live DB, no imports performed for visuals. Static route states used only as fallback if data not present in copy.

## Capture Method
- Primary: manual browser (localhost:5173 after `npm run dev` + backend on 8000 with copied DB).
- Fallback/automation assist: adapt prior `capture_pm_walkthrough.py` (temp playwright via node in /tmp) + `frontend/e2e/helpers/scheduleLoadedState.ts` recipes if operator wants scripted shots. Record tool + viewport + any script used.
- Naming: descriptive + numbered as above. Include .sha256 or manifest if generated by script (as in prior phase12 evidence).

## Validation Caveats (to be updated post-capture)
- Browser / OS / viewport used.
- Whether data came from copied DB with tropical schedule data (preferred) or route-level mocks.
- Any visual differences due to font rendering, window size, or theme (dark default).
- Confirmation that Schedule group was active on every nested screenshot.

**Inventory template complete. Real shots + final list + proofs added during Phase 6 closeout.**
