# Phase 8 Language QA

## Controls named baseline
Existing `validate_controls_text` rules retained — named slot labels must appear in controls copy; no ambiguous "the baseline" phrasing.

## Workbench named preview
- Headline copy distinguishes live preview vs persisted queue
- Footer advisory notes named preview does not persist dispositions
- No causation/entitlement language introduced

## Status
Controls language QA passes for named baseline fixtures (covered by `test_controls_language_qa_passes_named_baseline`).
