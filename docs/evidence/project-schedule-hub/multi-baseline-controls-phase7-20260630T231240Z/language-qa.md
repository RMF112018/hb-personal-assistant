# Phase 7 Controls Language QA

- `validate_controls_text` allows full named slot labels.
- Flags ambiguous standalone phrasing `the baseline` when a named comparison basis is active.
- Named controls copy prefixes summaries with `Compared against {Slot Label}`.
- Forbidden causation/entitlement terms remain blocked.
