# Phase 7 Frontend Proof

Proof type: mocked frontend API (vitest)

- Four controls comparison choices render; default Prior Update.
- Selecting Current Contract Baseline calls controls API with `comparison_basis=current_contract_baseline`.
- Workbench driver basis toggles independently from controls basis.
- Baseline Anchors section renders three slots with missing-state copy.
- Controls Trend Analytics section remains present.
- `scheduleApiAsOf.test.ts` covers baselines GET/PUT and named controls query params.
