# Known Limitations

- **Scope is surgical hardening only.** No new Phase 10 candidate, no schema migration, no product
  scope. The corrected surfaces pre-existed in mature form.
- **Pre-existing broad-suite failures** on `main` (env-dependent: real (Dev) DB / Ollama embedding
  dim) are not addressed and remain red regardless of this branch. This branch adds 0 net new
  failures. Only the 4 changed modules were lint/type-checked (repo convention: partial scope).
- **Unrelated lint untouched** — e.g. `cli/procore.py` B008 findings (pre-existing) were not "fixed".
- **mcp-packet counts** reflect the deterministic daily-brief context builder; on an empty temp DB
  some counts (e.g. calendar) are non-zero by the builder's default behavior — this is pre-existing
  builder behavior, unrelated to the flag change, and is metadata/counts only (raw-free).
- **Field rename has no alias.** `text_hash` is gone from the file-parse read-model; any external
  consumer (none found in-repo) must read `text_excerpt_hash`.
- **Two untracked foreign planning dirs** remain in the working tree (generated by separate
  workstreams); intentionally left untracked, never committed by this branch.
- **PR not opened.** Branch is local-only; push/PR require explicit user go-ahead.
