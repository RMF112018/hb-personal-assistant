# 10 — cloudflared Log Proof (redacted) — HOLD

**Status: HOLD** — captured at live activation. Will record (redacted) `cloudflared-runner logs` showing successful tunnel registration + inbound request routing to `http://hb-personal-assistant-mcp:8765`, and the **resolved image digest**. cloudflared does not log its token; any tunnel/connector id or tailnet IP is redacted or kept in `local-sensitive/`.
