# Endpoint contract proof

- total endpoints in registry: **59**
- live-verified: **56**
- degraded/unverified: **3**
- degraded endpoint ids: `budget-change-line-items, budget-details, purchase-order-detail-line-items`

The monitor reports the contract status from the static endpoint registry (`procore.endpoints`); unverified endpoints are surfaced honestly rather than silently treated as available.
