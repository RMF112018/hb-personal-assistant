# Validation Matrix — Daily Brief Surface Convergence (Prompt 01)

| Area | Command / Method | Expected | Actual | Status |
|---|---|---|---|---|
| Compile | `python3.12 -m compileall <changed src>` | pass | `COMPILE_OK` | ✅ |
| Targeted tests | `pytest -q tests -k "daily_brief or daily_run or followup or email_followup or render"` | pass | 516 passed | ✅ |
| New regression | `pytest -q tests/test_phase_10_daily_run_pending_followup_convergence.py` | pass | 3 passed | ✅ |
| Lint | `ruff check <changed>` | pass | All checks passed | ✅ |
| Types | `mypy daily_run.py daily_run_html.py` | pass | no issues in 2 files | ✅ |
| DB migration | N/A | — | No schema change (V45 already exists) | ✅ N/A |
| Final output (no-row) | evidence generator | section absent | count 0, no card | ✅ |
| Final output (seeded) | evidence generator | section in HTML + Obsidian | card+section present (3 rows) | ✅ |
| Degraded path | `render_daily_run_html(degraded=True, …)` | card present | present + egress-clean | ✅ |
| Safety scan | forbidden-pattern scan over outputs | no findings | TOTAL_FINDINGS=0 | ✅ |
| Guard columns | sum of 13 CHECK(=0) columns on temp DB | zero | nonzero_sum=0 | ✅ |
| Production DB checksum | sha256 before/after | unchanged | UNCHANGED=True | ✅ |

## Notes

- All rendering/seeding ran on **disposable temp DB copies**; the production DB
  (`PathPolicy.get_db_path()`) was read once for the baseline checksum and never opened for write.
  See `09-production-db-unchanged-proof.txt`.
- The pending section appears with `synthesize_brief=False` (no local model at all) and on the
  degraded path — proving model synthesis is never required for it to appear (Prompt requirement 3).
- The redacted status file carries only the `pending_followup` counts/labels summary; the row-level
  enriched titles never appear in it (asserted in the regression test).
- **Concurrent external mutation:** two files under
  `docs/evidence/construction-intelligence-phase-07a-data-quality/` were regenerated by a background
  data-quality dry-run during this session (their `repo_sha` equals this branch's setup-commit HEAD).
  They are unrelated to this candidate and were excluded from the commit (explicit-path staging).
