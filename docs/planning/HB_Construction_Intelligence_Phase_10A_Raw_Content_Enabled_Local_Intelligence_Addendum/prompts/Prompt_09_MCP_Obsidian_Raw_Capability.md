# Prompt 09 — MCP and Obsidian Raw Capability

## Objective

Prepare raw-content downstream capability, config-gated.

## Tasks

1. Add raw-capable packet types.
2. Keep raw MCP disabled by default unless explicitly enabled.
3. Allow Obsidian raw export when explicitly enabled.
4. Add tests to prove config behavior.

## Acceptance

- Raw packets can be built locally.
- MCP/Obsidian raw exposure is explicit and visible.
