You are working with Bobby on `RMF112018/hb-personal-assistant`.

Execute the objective defined at:

`docs/planning/phase-10-full-candidate-implementation-package/README.md`

Important execution requirements:

1. Work from the live repo truth.
2. Start from clean `main`; Bobby's package-generation baseline was `0c75f4a77b5987cdbd2a058504f5423d69d225f1` with `main == origin/main`.
3. Create or use the implementation branch specified in the README.
4. Execute every prompt in `prompts/` in numeric order.
5. After each prompt is implemented, validated, evidenced, and committed, wait exactly 10 minutes before starting the next prompt.
6. Each candidate must generate end-to-end evidence files, including the intended final output for that candidate.
7. Do not skip safety scans, guard-column proofs, or final-output evidence.
8. Do not perform external writeback.
9. Do not persist or commit raw/private content.

Proceed until all prompts are complete or a README stop condition is reached.
