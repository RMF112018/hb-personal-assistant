# Evidence — <prompt number/title>

## Scope

Describe what was implemented or audited.

## Commands

See `validation-commands.txt`.

## Results

See `validation-results.md`.

## Changed files

See `changed-files.txt`.

## Safety scan

See `safety-scan-results.txt`.

## DB-copy proof

Include only safe paths, hashes, integrity/quick check status, row counts, and schema summaries. Do not include raw payloads or copied DB files.

## No-raw-leak proof

Document commands proving no raw Procore payloads, tokens, signed URLs, private URLs, raw HTML, raw email/calendar/document bodies, or secrets are present in repo evidence/output artifacts.
