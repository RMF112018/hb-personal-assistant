# 06 Settings Connections Daily Brief Audit

Generated: 2026-06-07T07:17:24.406486+00:00

Audit scope: `hb-personal-assistant` FastAPI / Vite React frontend analytics dashboard. Repository truth reviewed through the GitHub connector because the sandbox could not access `/Users/bobbyfetting/hb-personal-assistant` and network clone failed. No production source changes were made.


## Connection Management

Connection setup services preserve the important guardrails: preview is safe and local, save writes setup metadata only, first live sync remains pending admin approval, and admin approval does not trigger sync directly.

## Confirmed Requirements

- Procore homepage URLs such as `https://app.procore.com/2982068/project/home` are supported.
- SharePoint site/folder/share-link recognition is present.
- OneDrive all-folder scope requires explicit mode and warnings.
- Outlook/Calendar `project_matching_only` is optional and false by default.
- Project keyword policy excludes standard/template folder names.
- Admin-only first sync approval is preserved.
- Daily Brief remains external Markdown generated by an external agent; the app detects and presents only.

## Gaps

- Settings page still exposes raw JSON response panels.
- Several actions still use `alert()` on failure.
- Preference and admin patch routes are echo/stub-level.
- Daily Brief state computation has an operator-precedence bug.
- Hash navigation links should be replaced with React Router links.
- Project keyword management is informational only; no polished CRUD UI exists.
