Execute the objective defined at `docs/planning/phase-10-ollama-assisted-candidate-ranking-brief-assembly-package/README.md`.

Work from `/Users/bobbyfetting/hb-personal-assistant`.

This is a code implementation task. Follow the package exactly, update the package only if repo truth requires it, do not mutate production data or external systems, validate on `/tmp` DB copies only, and produce the final handoff in the format required by the README.
