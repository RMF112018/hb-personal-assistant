# Source Refresh and Scheduler Status

Status is read-only metadata. Dry-run previews without writes. Local/mock refresh updates dev/local data without live clients. Live refresh requires environment/config/confirmation gates and orchestrator validation. Scheduler status is read-only.
