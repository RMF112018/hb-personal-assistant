# Dev and Production UX Behavior

Dev: show Dev, local data mode, status from local metadata, local refresh, dry-run, and disabled/gated live refresh.

Production: show live-read enabled/disabled state, next scheduled refresh, last refresh receipt, and manual live refresh only with confirmation and config gates. Status page must not auto-run live reads.
