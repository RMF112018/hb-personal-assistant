# Frontend Failure Reconstruction

Capture current failure by navigating to Settings/Connections in Dev, recording console/network/backend logs, and comparing frontend endpoint expectations to FastAPI route truth.

Classify as missing endpoint, wrong endpoint, response-shape mismatch, base URL/CORS issue, unimplemented auth flow, placeholder button, or local/mock mode confusion.
