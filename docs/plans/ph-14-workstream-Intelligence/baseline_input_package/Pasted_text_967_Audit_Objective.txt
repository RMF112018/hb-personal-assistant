Exhaustive Repo-Truth Audit + Next-Phase Implementation Roadmap

You are auditing the live `RMF112018/hb-personal-assistant` repository.

## Objective

Conduct an exhaustive repo-truth audit of the current `hb-personal-assistant` implementation and propose the next phase of implementation steps required to progress toward the full targeted architecture.

The output should not be a generic roadmap. It must be grounded in actual repo truth: source code, tests, docs, evidence files, CLI behavior, validation outputs, architecture docs, and commit history. The goal is to determine what is genuinely complete, what remains conditionally blocked, what is missing, and what the next implementation phase should address.

## Current Context to Carry Forward

The project is the **HB Personal Assistant + Work Product Intelligence System**, a Bobby-only local-first assistant. Daily Brief generation is one workflow, not the entire product.

The target architecture includes:

- local-first Microsoft 365 delegated retrieval;
- Outlook mail and calendar retrieval;
- file and attachment metadata discovery;
- bounded file ingestion and parsing;
- SQLite state and source-link registry;
- Obsidian daily notes / AI output writing;
- operational memory;
- workstream/action intelligence;
- meeting prep;
- source-linked retrieval;
- safe evidence generation;
- launchd morning automation;
- eventual local model / Ollama-based routing and synthesis.

## Important Deferred Scope / Consent Context

A previous audit found delegated Graph proof was not complete. The sequence of findings is important:

1. Earlier evidence incorrectly suggested DNS/network was the sole remaining blocker.
2. A later `hb-assistant auth login --json` run showed the real blocker was initially an MSAL reserved-scope defect:
   - the app was passing `offline_access` into the delegated MSAL scope request;
   - MSAL rejected reserved scopes including `openid`, `offline_access`, and `profile`;
   - this required a patch to sanitize delegated scopes before MSAL login.
3. After that correction, auth login reached Microsoft, but delegated permissions are still pending tenant/admin approval.
4. Full delegated Graph evidence collection must therefore be deferred until admin consent is granted.

The current blocker should be classified as:

```text
CONDITIONALLY_ACCEPTED_WITH_EXTERNAL_ADMIN_CONSENT_BLOCKER

Do not classify the current blocker as DNS unless current repo evidence proves a live DNS failure. Do not classify it as a local code/path/DB issue unless current validation proves one.

The relevant latest user-provided auth evidence showed the earlier reserved-scope failure and confirmed Application Support paths were writable. The reserved-scope issue should now be treated as historical unless the current repo still reproduces it.

Required Audit Method

Start from repo truth only. Do not rely on prior summaries except as context to verify.

1. Establish Current Repo State

Run or inspect enough to document:

git remote -v
git branch --show-current
git rev-parse HEAD
git log --oneline -20
git status --short

Identify:

current branch;
current HEAD;
relationship to latest known commits;
whether the working tree is clean;
whether final evidence aligns with the actual current state.
2. Inspect Project Structure

Audit at minimum:

README.md
pyproject.toml
src/hb_assistant/
tests/
docs/architecture/
docs/decisions/
docs/evidence/
docs/evidence/remediation-addendum/
docs/plans/my-pa-phase-0/
CLI modules under src/hb_assistant/cli/
auth modules under src/hb_assistant/auth/
graph modules under src/hb_assistant/graph/
store modules under src/hb_assistant/store/
automation modules under src/hb_assistant/automation/
classification modules under src/hb_assistant/classification/
files/ingestion modules under src/hb_assistant/files/
Obsidian modules under src/hb_assistant/obsidian/
retrieval/model-routing modules, if present.
3. Validate Current Gates

Run or review evidence for:

python -m pytest
ruff check .
mypy src
hb-assistant --version
hb-assistant auth status --json
hb-assistant diagnostics env --json
hb-assistant diagnostics paths --json
hb-assistant diagnostics graph --safe --json
hb-assistant diagnostics proof delegated-graph --json
hb-assistant diagnostics automation --json
hb-assistant diagnostics scan-sensitive --repo . --json
hb-assistant files sample --json
hb-assistant files ingest --dry-run --json
hb-assistant run morning --dry-run --json

If any command cannot be run because the session lacks local runtime access, inspect committed evidence and clearly say that the finding is evidence-based, not freshly executed.

4. Audit the Consent-Deferred Graph State

Specifically determine:

whether delegated scope sanitization exists;
whether reserved scopes are removed before MSAL delegated login;
whether raw configured scopes and effective MSAL scopes are separately reported;
whether auth login reaches Microsoft consent;
whether the current blocker is genuinely admin consent;
whether delegated proof is deferred correctly;
what evidence must be collected once admin approval is granted.

Required post-consent proof plan:

hb-assistant auth login --json
hb-assistant auth status --json
hb-assistant diagnostics graph --safe --json
hb-assistant diagnostics proof delegated-graph --json
hb-assistant diagnostics scan-sensitive --repo . --json

The proof must eventually validate:

delegated token classification using scp;
/me;
mail metadata;
bounded body retrieval;
calendarView;
attachment metadata;
drive/file metadata;
controlled eligible file download, if allowed;
app-only rejection or non-use for runtime mail/calendar;
no Microsoft 365 mutation paths.
Areas to Audit for Completeness

Assess each area as:

Complete / Conditionally Complete / Partial / Not Started / Blocked

For each, cite specific repo files, commands, or evidence.

A. CLI Foundation

Check:

Typer structure;
canonical command groups;
JSON output consistency;
structured error handling;
help text;
compatibility shims, if any;
command coverage in tests.
B. Auth + Token Cache

Check:

delegated MSAL auth implementation;
scope sanitizer;
token cache location;
file permissions;
app-only rejection for runtime;
status/diagnostics behavior;
admin-consent blocker evidence.
C. Graph Clients

Check:

mail client;
calendar client;
drive/file client;
bounded paging;
retry/throttling behavior;
metadata-only defaults;
bounded body retrieval;
no write methods.
D. SQLite Store + Source Links

Check:

schema;
migrations;
source_records;
emails;
calendar_events;
attachments/files;
action_items;
source_links;
assistant_runs;
embeddings, if implemented;
DB readiness checks;
provenance constraints.
E. Email Classification + Body Mention Detection

Check:

bodyPreview fast path;
bounded full-body fallback;
raw body non-persistence;
redacted excerpt logic;
tests proving beyond-preview detection;
whether classifier and real retrieval pipeline are actually wired together.
F. Action / Work Product Intelligence

Check:

action extraction;
waiting-on detection;
action item schema;
stability/idempotency;
source-linked action records;
integration into brief generation.
G. File Ingestion + Parsing

Check:

metadata discovery;
eligibility rules;
size caps;
approval gates;
provenance requirements;
parser outputs;
redaction/excerpt limits;
whether real Graph file discovery is implemented or still mostly sample-driven.
H. Retrieval / Search / Embeddings

Check:

local retrieval implementation;
embedding storage;
vector search or lexical fallback;
source-linked query results;
Ollama/local model hooks;
whether semantic retrieval is production-shaped or scaffold-only.
I. Obsidian Output

Check:

daily note writing;
AI output writing;
marker-bounded preservation;
frontmatter handling;
idempotency;
source links;
dry-run behavior.
J. Morning Automation / launchd

Check:

run morning;
run ledger;
weekend/manual-only gate;
catch-up behavior;
launchd plist generation;
install/uninstall/kickstart commands;
logs;
evidence output;
failure isolation.
K. Security / Privacy / Evidence

Check:

sensitive scanner;
no secrets committed;
no full email/file content committed;
redaction rules;
token-cache exclusion;
public repo safety;
evidence consistency;
.gitignore adequacy.
L. Tests + CI Readiness

Check:

unit coverage;
integration coverage;
mocked Graph tests;
CLI runner tests;
security tests;
mutation lockout tests;
whether GitHub Actions/CI exists or should be added next.
Required Output Structure

Return a detailed audit with these sections:

1. Executive Assessment

Include:

current repo status;
acceptance classification;
whether local gates are green;
whether Microsoft delegated proof remains blocked;
whether the admin-consent blocker is external or code-related.
2. Repo-Truth Snapshot

Include:

branch;
commit;
version;
validation status;
evidence directories reviewed;
any inconsistency between docs and code.
3. Completion Matrix

Table with:

Area	Status	Evidence	Notes
4. Critical Findings

Separate:

P0 blockers;
P1 functional gaps;
P2 hardening improvements;
documentation/evidence corrections.
5. Deferred Admin Consent Plan

Provide exact steps to run after admin approval.

Include:

commands;
expected successful outputs;
what evidence to commit;
how to classify any new failures.
6. Next Phase Recommendation

Propose the next implementation phase en-route to the full target architecture.

The next phase should be practical and sequenced. It should not assume delegated Graph proof is available until admin consent lands.

Provide:

phase name;
objective;
scope;
non-goals;
implementation sequence;
acceptance criteria;
validation commands;
risk controls;
suggested commits.
7. Proposed Prompt Package Outline

Provide a sequence of prompts for the local agent to execute next.

Each prompt should include:

title;
objective;
files/modules likely touched;
key implementation requirements;
validation commands;
expected commit message.
8. Final Recommendation

State whether the repo should proceed to:

delegated proof closeout only;
next local-only implementation phase while consent is pending;
or no further work until consent is granted.
Important Constraints
Do not propose Microsoft 365 writeback.
Do not propose multi-user scope yet.
Do not persist full email bodies.
Do not persist full file contents.
Do not use cloud services for local runtime state.
Do not assume admin consent is already granted.
Do not classify consent-pending delegated proof as a code failure if the repo shows auth reaches Microsoft consent.
Do not classify DNS as the blocker unless current command evidence proves DNS failure.
Do not over-claim acceptance.