# Forecast-Model Remediation Plan — Execution Tracker

Canonical status ledger for the 10-phase forecast-model remediation. This is the single source of
**phase status**; the authoritative **phase specs + copy/paste prompts** live in the gap-validation
report.

- **Source-of-truth report:** `docs/evidence/forecast-model-gap-validation/20260623T080628Z/forecast-model-gap-validation-report.md` (PR #102).
- **Execution tooling:** the `forecast-remediation-driver` skill drives one phase per invocation and
  updates this file; the `forecast-remediation-tracker` agent audits this ledger against repo truth.
- **Status values:** `pending` · `in-progress` · `in-review` · `merged` · `blocked`.

## Common guardrails (every phase)

Copied-DB evidence only (never mutate the live DB); no Procore/live/external calls; no external
writeback; no `raw_json`/source-path in user-facing API output; no `git add .`; one concern per PR
(never mix phases); confirm behavior in code/tests/DB, not docs. Each phase runs in a fresh worktree
off `origin/main` and opens one PR. Sensitive ops (migrations / live-DB / schema) gate through
`hb-sensitive-operation-gate`; plans gate through `hb-plan-gate-review`; tests select via
`hb-verification-router`; landed PRs close out through `hb-post-execution-closeout`.

## Recommended order

P1 & P4 (production blockers) → P2, P3, P6, P10 → P5, P8, P9 → P7.

## Phase ledger

| Phase | Title | Gaps | Severity | Status | PR | Evidence | Date | Blocked-by |
|---|---|---|---|---|---|---|---|---|
| P1 | Forecast output header totals + prior-run deltas | 1, 7 | production_blocker | merged | #104 | `docs/evidence/forecast-remediation/P1-header-totals-20260623T092610Z/` | 2026-06-23 | — |
| P4 | Multi-project generalization (remove tropical-only guards) | 4 | production_blocker | merged | #105 | `docs/evidence/forecast-remediation/P4-multi-project-20260623T101104Z/` | 2026-06-23 | — |
| P2 | Operator assumptions as consumed model inputs | 2 | high | merged | #107 | `docs/evidence/forecast-remediation/P2-assumptions-consume-20260623T141418Z/` | 2026-06-23 | — |
| P2b | Operator-assumption dollar value-overrides (output projection) | 2 | high | merged | #108 | `docs/evidence/forecast-remediation/P2b-value-overrides-20260623T162613Z/` | 2026-06-23 | — |
| P3 | DB-native model input accessors (reduce package-as-source) | 3 | high | merged | #109 | `docs/evidence/forecast-remediation/P3-db-native-inputs-20260623T193155Z/` | 2026-06-23 | — |
| P6 | Model registry / versioning / weighting / calibration governance | 6 | high | merged | #115 | `docs/evidence/forecast-remediation/P6-model-registry-governance-20260624T114500Z/` | 2026-06-24 | — |
| P10 | Forecast-correctness & isolation test hardening | 10 | high | merged | #110 (PR1) + #111 (P10c) + #113 (PR2) | `docs/evidence/forecast-remediation/P10-PR2-correctness-values-20260624T070456Z/` | 2026-06-24 | — |
| P5 | Maturity / data-availability / confidence completion | 5 | medium | merged | #117 | `docs/evidence/forecast-remediation/P5-maturity-availability-20260624T140000Z/` | 2026-06-24 | — |
| P8 | Explainability / audit trail (narratives, model-version, override, QA, sha256 chain) | 9 | medium | merged | #119 | `docs/evidence/forecast-remediation/P8-explainability-audit-trail-20260624T131731Z/` | 2026-06-24 | — |
| P9 | UI/API readiness + operator workflow | 1, 5, 6, 7, 9 (surface) | medium | in-review | (PR pending) | `docs/evidence/forecast-remediation/P9-ui-api-readiness-20260624T141415Z/` | 2026-06-24 | — |
| P9b | Durable gated live-write backup relocation (CFR Phase-14) | 9 (durability) | low | pending | — | — | — | split from P9 (sensitive-op) |
| P7 | External forecast eval completion (XLSX, multi-project discovery) | 8 | low | pending | — | — | — | — |

## Phase summaries (see the report for full specs/prompts)

- **P1** — aggregate per-code recommended costs into `forecast_outputs` header (EAC/CTC/variance_to_budget);
  compute `variance_to_prior_forecast` + `prior_run_id` from the prior run. Files:
  `construction/forecast/output_projection_engine.py`. No schema change.
- **P4** — replace the 25 `project_key != SUPPORTED_PROJECT_KEY` guards (19 CFR files) with a
  project-eligibility check; parameterize the `twn_cost_forecast_json_package` source name.
- **P2** — consume `forecast_operator_assumptions`/`forecast_required_assumptions` as confidence
  modifiers + a required-satisfaction gate in `decision_support_engine`, behind a default-off flag.
  Dollar value-overrides delivered in **P2b**.
- **P2b** — operator dollar value-overrides in `output_projection_engine`: reserved `assumption_type`s
  (`projected_cost_override` / `cost_to_complete_override`) override per-code typed columns + re-aggregate
  the EAC/CTC header; auditable `operator_value_override` change rows; raw_json kept as source echo
  (parity-safe). Default-off `HB_FORECAST_ASSUMPTION_OVERRIDES_ENABLED`. No schema change.
- **P3** — DB-backed model-input accessors (v59/runtime) with packages as fallback; prove parity.
  Default-off `HB_FORECAST_DB_BACKED_INPUTS_ENABLED` routes `forecast_run_service.start_run` through
  the existing CFR controlled workflow in `db` mode (3 covered domains from a NON-LIVE v59 DB
  read-only); fail-closed before the workflow on a live/unconfigured db_path; flag-off byte-identical.
  No schema change. ADR 303.
- **P6** — DB-backed model registry (migration v72+); versioned estimators/weights/thresholds; persist
  `forecast_method_eligibility`/`forecast_model_selection_decisions` with rationale; per-run model-version metadata.
- **P10** — two PRs. **PR1 (suite hardening, in-review):** fixed the red suite via Group D (the P4b
  relative-import regression in the 4 bare-file generators → absolute), Group E (P4 "unsupported
  project_key" → "not eligible" message stragglers), Group B (6 stale `schema_version == 61` →
  `LATEST_SCHEMA_VERSION`; left the intentional v61 backward-compat fixture), Group C (2
  `unconfigured->503` isolation leaks), Group A (evidence-script bare `python3` → venv). `-k forecast`
  88→35, 53 fixed, 0 regressions; residual 35 are pre-existing, unmasked, 4 distinct root causes
  (`project_display_name`/`_run_comprehensive` project_key/proof perturb-callback/launcher env)
  documented for a focused follow-up. ADR 304. **PR2 (gap-only, ADR 306):** forecast-correctness
  VALUE tests for the two genuinely-unasserted behaviors — probability band value/ordering round-trip
  and second-project (non-tropical) value isolation. The other spec bullets (header == per-code
  Decimal sum, prior-delta, override re-aggregation, assumption consumption, floor-to-actuals) were
  already covered by P1/P2/P2b + the CFR probability suite and are attested in ADR 306, not
  re-tested. **P10c (#111):** greened the 35 residual (ADR 305).
- **P5** — M5/closeout tier + `lifecycle_signal`; package/output-aware availability; populate
  `completeness`/`mapping_quality`/`score`; add changes/assumptions/procore/risk/probability domains.
- **P8** — populate `forecast_output_narratives`; add model-version metadata, human-override history,
  source-QA rationale, package-sha256 chain.
- **P9** — surface header totals, prior deltas, assumption impact, maturity/availability, model rationale
  in read-model + API + Run Center; multi-project selector; durable live-write backup.
- **P7** — XLSX ingest + multi-project discovery for external-forecast evaluation.

## Changelog

_Append one line per phase transition (date · phase · status · PR · note)._

- 2026-06-23 · P1 · in-review · (PR pending) · header EAC/FAC/CTC/variance_to_budget aggregated from per-code rows; variance_to_prior_forecast + project-level `current_vs_prior` change row from the prior run; no schema change; evidence `P1-header-totals-20260623T092610Z`.
- 2026-06-23 · P1 · merged · PR #104 · landed to main at `1052f563`.
- 2026-06-23 · P4 · in-review · (PR pending) · 26 tropical-only guards → CFR-local stdlib `common/project_eligibility.py` (env allowlist + `forecast_projects` registry + {tropical, fixtureproj} default); `package_resolution` prefixes derived per project; `source_package_name(project_key)` replaces 5 hardcoded `EXPECTED_SOURCE_PACKAGE_NAME`; `_project_rowcount`/proof `gen.generate` threaded with project_key; synthetic `fixtureproj` proof; no schema change; CFR imports no hb_assistant. Deferred to **P4b**: `generate_forecast_context_package.py` detropicalization + live second-project E2E. Evidence `P4-multi-project-20260623T101104Z`.
- 2026-06-23 · P4 · merged · PR #105 · landed to main at `d131a28d`.
- 2026-06-23 · P4b · in-review · (PR pending) · detropicalize the context/analysis/crosswalk-v2/mapping/comprehensive generators + CLI to read project values from `config/projects/<key>.json` via new stdlib `common/project_config.py` (CFR_PROJECT_KEY env, default tropical); removed the `cmd_run_generator` tropical-only refusal (eligibility-gated; passes CFR_PROJECT_KEY to the subprocess); added `procore_export_folder`/cutoffs/`row_count_expectations`/`project_display_name` to `tropical.json` + new `fixtureproj.json`. Tropical byte-parity proven (config-equivalence tests + full CFR suite 670 passed incl. the real-generator proof tests); CFR imports no hb_assistant; no schema change. Deferred to **P4c**: synthetic `fixtureproj` data root + live second-project run. Evidence `P4b-generators-detropicalize-20260623T131334Z`.
- 2026-06-23 · P4b · merged · PR #106 · landed to main at `d9523fa7`; closeout audit (hb-commit-diff-auditor) PASS (+497/−41, 16 files, no reformat churn, tropical parity intact, no hb_assistant source change). Deferred to **P4c**: synthetic `fixtureproj` data root + live second-project run.
- 2026-06-23 · P2 · in-review · (PR pending) · consume operator/required assumptions in `decision_support_engine` behind default-off `HB_FORECAST_ASSUMPTION_CONSUMPTION_ENABLED`: new consume-only `forecast/assumptions_repository.py` (project-scoped `run_id IS NULL` reads of the v66 tables); confidence modifiers (`confidence_impact` raises/lowers/neutral → booster/penalty/neutral factor on the matching scorecard) + required-assumption gate (penalty factor + warning per unsatisfied required assumption); assumptions read read-only from the live managed DB (or explicit `assumptions_db_path`) and pre-hydrated into the planner (engines stay DB-decoupled); flag-off = byte-identical output. No schema change; no live-DB write. Dollar value-overrides deferred to **P2b**. Evidence `P2-assumptions-consume-20260623T141418Z`. ADR 301.
- 2026-06-23 · P2 · merged · PR #107 · landed to main at `d633d5e8`; closeout audit (hb-commit-diff-auditor) PASS; zero forecast-suite regressions.
- 2026-06-23 · P2b · in-review · (PR pending) · operator dollar value-overrides in `output_projection_engine` behind default-off `HB_FORECAST_ASSUMPTION_OVERRIDES_ENABLED`: reserved `assumption_type`s `projected_cost_override`/`cost_to_complete_override` (require budget_code_key + parseable value) override the per-code typed columns (raw_json kept as original source echo → parity-safe) and re-aggregate the EAC/FAC/CTC/variance header; one auditable `operator_value_override` change row per override; null-key/unmatched/unparseable → skip + warning; guarded post-pass so flag-off / no-override = byte-identical. Read read-only (`mode=ro`) from the live managed DB (or explicit `assumptions_db_path`) via a private engine hydrator (assumptions_repository stays conn-accepting). No schema change; no live-DB write. Evidence `P2b-value-overrides-20260623T162613Z`. ADR 302.
- 2026-06-23 · P2b · merged · PR #108 · landed to main at `398c6140`.
- 2026-06-23 · P3 · in-review · (PR pending) · DB-native model inputs behind default-off `HB_FORECAST_DB_BACKED_INPUTS_ENABLED`: `forecast_run_service.start_run` routes the run through the existing CFR controlled workflow in `db` mode (the 3 covered domains — budget_details/cost_entries/monthly_actuals — sourced from a NON-LIVE v59 DB read-only) when the flag is on; flag resolved locally (lazy import → no circular dep); fail-closed BEFORE the workflow on a live/default or unconfigured `db_path` (recorded as a failed run; workflow never called — `resolve_db_path()` defaults to the live DB, so a run never silently reads it); `_summarize_report` derives `no_live_writes` from `work_root_outside_live_root` (both modes); `record["mode"]` branched at both sites. File package remains default + fallback; flag-off byte-identical. No new DB adapter; no wiring into output/decision engines; no schema/CFR-source change; no live-DB write. Acceptance proven by 7 routing/flag/fail-closed tests + Phase 6 file-vs-DB parity; full heavy E2E gated by the pre-existing Group D env condition (CFR not importable → P10). Evidence `P3-db-native-inputs-20260623T193155Z`. ADR 303.
- 2026-06-23 · P3 · merged · PR #109 · landed to main at `c49ca031`.
- 2026-06-23 · P10 · in-review · (PR1 pending) · suite hardening. Group D: the P4b regression — 4 bare-file generators (`generate_forecast_analysis_package.py:77`, `generate_forecast_context_package.py:109`, `generate_forecast_analysis_crosswalk_v2.py:80`, `generate_mapping_discrepancy_workpaper.py:67`) ran a relative `from ..common.project_config import` under `__main__` (subprocess), which fails → converted to absolute (matching each file's sys.path bootstrap). NOTE: the P3-changelog "CFR not importable" framing was imprecise — the real cause was this relative import, not an env/install issue. Group E: 7 tests' `match="unsupported project_key"` → `"not eligible"` (P4 message drift). Group B: 6 forecast tests `schema_version == 61` → `LATEST_SCHEMA_VERSION` (left the intentional v61 backward-compat fixture `test_forecast_runtime_config.py:219`). Group C: 2 `unconfigured->503` tests monkeypatch the source-module resolver to None. Group A: evidence script bare `python3` → `${VENV_PYTHON:-.venv/bin/python}` + tests pass `sys.executable`. `-k forecast` 88→35 failing, 53 fixed, 0 regressions. Residual 35 pre-existing + unmasked (4 root causes: `project_display_name` KeyError, `_run_comprehensive` missing project_key, proof perturb-callback project_key, launcher `HB_FORECAST_EVAL_ROOT`) documented for a focused follow-up. No schema/hb_assistant-product change; Group D = 4× one-line CFR-source bug fix. Evidence `P10-PR1-suite-hardening-20260623T205408Z`. ADR 304.
- 2026-06-23 · P10 · PR1 merged · PR #110 · landed to main at `42d3ce0a`.
- 2026-06-24 · P10c · in-review · (PR pending) · greened the suite by fixing the 35 residual (pre-existing, unmasked by PR1's Group-D fix). RC2: thread `project_key` into `forecast_db_config_backed_core.py` (both `descriptor.run()` sites + the probability inner `_run` closure that the plan-gate review caught + the descriptor comment). RC3: 6 monkeypatched perturb callbacks (phase18 ×2, phase18a, phase19, phase20 ×2) accept/forward `project_key`. RC1: phase20 + phase20_stability synthetic configs mirror the real `tropical.json` `project_name` + `project_display_name` (comprehensive generator reads them via `load_project_config` and embeds them in manifest/README; file-backed reads real config, db-backed the synthetic snapshot → must match for byte-exact parity; not a `.get()` mask). RC4: launcher empty `_forecast_default_env()` is intentional (commit `7223dba6`) → updated the 3 stale launcher tests (assert no `HB_FORECAST_*` injection) + corrected the stale `_child_env` docstring (no behavior change). `-k forecast` 35→0, 35 fixed, 0 regressions (P10 PR1+P10c: 88→0). 8 files; only source change = RC2 project_key threading. Evidence `P10c-residual-green-20260624T023541Z`. ADR 305.
- 2026-06-24 · P10c · merged · PR #111 · landed to main at `548d55d2`.
- 2026-06-24 · P10 · PR2 in-review · PR #113 (commit `469dc725`) · forecast-correctness VALUE tests (gap-only). New `tests/test_forecast_correctness_values_p10.py` (6 tests) covering the two genuinely-unasserted behaviors: (1) probability band VALUE correctness — `p10/p50/p90` round-trip exactly through projection → `forecast_output_probability` → read layer, `p10 ≤ p50 ≤ p90` ordering, surfaced via both `output_repository.read_output_probability_from_db` (raw-json echo) and `ForecastRunReadModelService.read_output` (typed); (2) second-project (non-tropical, `harbor`) VALUE isolation — per-project header aggregation (no cross-sum), project-scoped prior-run delta (later tropical run NOT picked up as harbor's prior), `list_outputs` project filtering. The other spec bullets (header == per-code Decimal sum, prior-delta, override re-aggregation, assumption consumption, floor-to-actuals) were already covered by P1/P2/P2b + the CFR probability suite (`test_fp_simulate`/`test_reconcile`/`test_fp_distributions`) → attested in ADR 306, not re-tested. The new tests SURFACED a pre-existing production defect: `SQLiteMigrator.apply()` (store/migrator.py) opened two WAL `get_connection()` connections and closed neither (contract: caller closes), so a leaked connection's `-wal` checkpointed into the main DB only on GC — non-deterministically flipping phase17's raw-byte canary `test_live_db_and_source_config_not_mutated` (deterministic with the new tests via GC timing; benign — read-only intent intact, only `_setup`'s own writes relocate from -wal to main). Fix: `apply()` reads the version on the existing connection and closes it before returning (both leaks gone; WAL checkpoint deterministic; canary passes WITHOUT touching the canary). One store-layer production line beyond tests/docs; no schema/count change, no live-DB write. Incorporated origin/main (FF `548d55d2`→`8703f7c`) which added the fast bundles `scripts/test-forecasting.sh`/`scripts/test-schedule.sh`; the bundle docs EXCLUDE the phase17 byte-canary as "not stable under the current local runtime" (corroborates the leak diagnosis; fix kept for root-cause/full-suite benefit). Validated via the bundles (cross-cutting migrator → both): `scripts/test-forecasting.sh` 0 failing (the new test file was added to its target list; 6 new tests pass) + `scripts/test-schedule.sh` 0 failing (migrator/schema tests green → apply() fix safe cross-domain). Perturbation sanity check confirmed the new assertions are non-vacuous. Evidence `P10-PR2-correctness-values-20260624T070456Z`. ADR 306.
- 2026-06-24 · P10 · PR2 merged · PR #113 · landed to main at `e5e3aa8e` (squash). P10 complete (PR1 #110 + P10c #111 + PR2 #113).
- 2026-06-24 · P6 · in-review · PR #115 (commit `d04a27075`) · model-registry / versioning / weighting / calibration governance (Gap 6). Migration **v72** adds three additive `operational_empty_expected` tables (`store/forecast_model_registry_tables.py`): `forecast_model_versions` (one immutable row per methodology, deduped by `methodology_sha256` — estimator order, reliability weights, thresholds, backtest cohort), `forecast_run_model_versions` (per-run linkage `run_id -> forecast_runs.run_id`, with the accuracy-package stamp as a separate provenance field), and `forecast_calibration_weights` (per-run, per-method calibration provenance; `calibration_source` distinguishes the four `BACKTEST_METHODS` = `backtest` from the cohort-omitted `schedule_etc` = `not_backtested` and the ERP baselines = `reliability_only`). Lifecycle contract `table_count` 433→436, all 19 hardcoded asserts bumped in lockstep. CFR `generate_forecast_accuracy_package.py` additively emits a **deterministic, path-free** `model_methodology.json` (methodology constants + `methodology_sha256`) so hb_assistant versions the methodology without duplicating CFR constants (CFR imports no hb_assistant; e2e byte-determinism stays green). New `construction/forecast/model_registry_repository.py` reads that descriptor + `audit/calibration_snapshot.json` and persists the three tables (idempotent, dedup-by-sha); wired into `project_decision_support` behind default-off `HB_FORECAST_MODEL_GOVERNANCE_ENABLED`, persisting in the same transaction after `apply_plan`, **fail-closed** before any write if `run_id`/`accuracy_package`/`model_methodology.json` is missing; flag-off is byte-identical (no rows, no `model_provenance`). The existing `_emit_method_rollups` already populates `forecast_method_eligibility`/`forecast_model_selection_decisions` when an accuracy package is threaded — unchanged. **Scope refinement:** per-run model version is recorded in `forecast_run_model_versions` (the projection-run home), NOT the `forecast_db_config_run_service` JSON record (a distinct config-snapshot run concept) — see ADR 307. Accuracy-package tropical-coupling deferred to P4c/P4d. Tests: `tests/test_migrator_v72_forecast_model_registry.py` + `tests/test_forecast_model_registry_p6.py` (added to the forecasting bundle). No live-DB write; temp/copied-DB only. Evidence `P6-model-registry-governance-20260624T114500Z`. ADR 307.
- 2026-06-24 · P6 · merged · PR #115 · landed to main at `01e3c472` (squash).
- 2026-06-24 · P5 · in-review · PR #117 (commit `2b2ad981`) · maturity / data-availability / confidence completion (Gap 5). Pure derivation logic in `construction/forecast/decision_support_engine.py` — **no migration** (all V66 columns already existed), no CFR change, **no flag** (metadata-only completion, temp-DB only, no dollar values → default-on). (1) `_maturity_tier` gains an **M5 closeout** tier driven by output evidence (header `cost_to_complete / estimated_final_cost <= M5_CLOSEOUT_CTC_FRACTION` = 0.005, a ratio not absolute dollars); `lifecycle_signal` populated via the `_LIFECYCLE_SIGNAL` coded enum (pre_start/mobilizing/in_progress/mature/closeout) instead of hardcoded None. ACCEPTED RISK (ADR 308): a stalled-and-exhausted project shows the same near-zero CTC and would be labeled closeout — no source signal distinguishes it. (2) **Output-aware availability**: commitment/schedule/changes/risk/probability/staffing flip from always-"unavailable" to "available" when the run's v63 output tables have rows, counted via a two-hop join (`child.output_id → forecast_outputs`, scoped by BOTH run_id AND project_key, helper `_count_output_rows`); assumptions counted run-scoped from the v66 tables; owner+procore stay "unavailable" (no forecast backing table — procore lives in the schedule source domain). (3) `completeness`/`mapping_quality`/`maturity`/`score` populated with deterministic count-derived ratios (`mapping_quality` = mapped÷domain codes; `completeness` = mapped÷budget-code universe; `maturity` = project tier; `score` = availability-gated completeness — a single count ratio, NOT a multi-metric blend). `GUARDRAILS["new_scoring_math"]` updated False→`"deterministic_count_derived_availability_score_only"` to name exactly this. Confidence scorecard numeric score stays None (deferred, accuracy/P8 concern). New `tests/test_forecast_p5_maturity_availability.py` (7 tests; added to the forecasting bundle). No live-DB write; temp/copied-DB only; coded values path-free. Evidence `P5-maturity-availability-20260624T140000Z`. ADR 308.
- 2026-06-24 · P5 · merged · PR #117 · landed to main at `b7bee688` (squash).
- 2026-06-24 · P8 · in-review · (PR pending) · explainability / audit trail (Gap 9). Derivation + wiring only — **no migration**, `LATEST_SCHEMA_VERSION`/`table_count` unchanged (72/436), no hb_assistant schema touch, no external systems, no live-DB write → no sensitive-op gate. New default-off flag `HB_FORECAST_EXPLAINABILITY_ENABLED` (`forecast_runtime_config.py`, mirrors P6) resolved in `project_run_output` and threaded as an explicit `explainability_enabled` param into the pure planner. New pure/deterministic `construction/forecast/output_narrative_builder.py` (no LLM, no CFR import) populates the previously-empty `forecast_output_narratives` (V63) with five `scope`s — `project` (header EAC/FAC/CTC/variance + counts), `budget_code` (per-code numbers/action/confidence/risk-count/overridden), `human_override` (one row per `operator_value_override` change — derived, NOT a new table), `source_qa` (null/zero/dup budget-code checks + `forecast_period` staleness), `lineage` (context→analysis→output package-sha256 chain). `output_repository.py` gains `upsert_output_narrative` (idempotent on `UNIQUE(output_id,scope,narrative_key)`) + `read_output_narratives_from_db`, registered in `_WRITERS` + the `_prove_parity` readers. `output_projection_engine.py` sets the long-empty `forecast_outputs.source_sha256` (analysis sha) and builds the `lineage` row in the apply txn after `apply_plan` (output sha = content hash excluding narratives + volatile path/timestamp keys → deterministic; folds in V72 `methodology_sha256`/`accuracy_package_stamp` when a `forecast_run_model_versions` row exists for the run + `prior_run_id` from the `current_vs_prior` change). Flag-off ⇒ zero narrative rows, `source_sha256` null, byte-identical. **Override "history table" → derivation** (deferred V73 table; only driver was the spec's one word). **Repo-truth correction:** the merged-P5 availability QA columns are already populated, so P8 leaves `decision_support_engine.py` untouched and meets source-QA via the output-side narrative (per ADR 309). New `tests/test_forecast_output_narratives_p8.py` (6) + `tests/test_forecast_lineage_completeness_p8.py` (4) + `tests/test_forecast_source_qa_p8.py` (2), all added to the forecasting bundle. Forecasting bundle 895 passed/0 failed (P5 baseline 883 + 12); schedule bundle 113/0 (schema canary, no drift). Evidence `P8-explainability-audit-trail-20260624T131731Z`. ADR 309.
- 2026-06-24 · P8 · merged · PR #119 · landed to main at `52c6f577` (squash).
- 2026-06-24 · P9 · in-review · (PR pending) · UI/API readiness + operator workflow (surfaces Gaps 1/5/6/7/9). Read-only UI/API only — **no schema/migration/`table_count` change, no live-DB write, no external system, no CFR source change → no sensitive-op gate**. The "durable live-write backup" spec item is a sensitive CFR live-write-path change, **split to P9b** (separate gated PR). Read-model `forecast_run_readmodel.py`: `read_narratives(output_id)` curates the P8 `forecast_output_narratives` per scope (`_curate_narrative` whitelist; `json.loads(raw_json)`, never raw_json verbatim) — stamp-format structured fields (`forecast_period`/`accuracy_package_stamp`/`prior_run_id`) dropped by omission, `applied_utc`→friendly display, and the P8 free-text `narrative` (which itself embeds stamps: source_qa "forecast period <stamp>", lineage "prior_run=<stamp>") scrubbed via `_redact_stamps` (`\d{8}_\d{6}`→`[redacted]`); sha256 hex surfaced; unknown scope→None (fail-safe). Plus `list_projects()` (DISTINCT project_key + counts) and `variance_to_prior_forecast` added to `list_outputs`. API `analytics/api.py`: viewer-role `GET /api/forecast/db/projects` (declared before the parameterized `/db/projects/{project_key}/…`) + `GET /api/forecast/db/outputs/{output_id}/narratives`, reusing the existing `_forecast_readmodel_service()`/`_forecast_readmodel_call()` (404-on-unknown). Frontend: new `ForecastNarrativesPanel` (project totals + human-override audit table + source-QA advisory + lineage sha chips); `ForecastDecisionSupportPanel` + `ForecastOperatorAssumptionsPanel` prop-ified (`project: string`, replacing hardcoded `tropical` incl. query keys + operator-write mutation calls); `ForecastRunCenterPage` multi-project selector (shown when >1 project) threading `project` into all three panels; `api.ts` client fns/interfaces + prior-delta on the summary type. Extended `tests/test_fastapi_forecast_run_readmodel.py` (+`test_list_projects`, `test_read_narratives` with stamp bait in BOTH structured fields and free-text + `find_redaction_leaks==[]`, `test_narratives_unknown_output_404`, prior-delta assertion); new vitest `ForecastNarrativesPanel.test.tsx` (3) + page selector tests (2); existing panel tests updated for the `project` prop. Forecasting bundle 898 passed/0 failed (P8 baseline 895 + 3); frontend `tsc -b` clean; vitest 16 passed/0 (4 forecast files). `api.py`'s 35 ruff errors are PRE-EXISTING (identical on origin/main; none on the new routes). Evidence `P9-ui-api-readiness-20260624T141415Z`. ADR 310.
