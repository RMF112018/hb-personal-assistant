# HB Personal Assistant + Work Product Intelligence System

Bobby-only local-first MVP for Microsoft 365 delegated access, source-linked retrieval, action intelligence, meeting prep, file review, and Obsidian Daily Brief output.

**The Daily Brief is a module, not the project name.**

## Repository Status

**Phase 01 (scaffold completion).** Construction-intelligence scaffold landed across Phase 01 Prompts 00–11 ending at SHA `34728c1`: Pydantic source registry + YAML loaders; V1–V4 additive SQLite schema (`construction_source_resolutions`, `construction_drive_item_inventory`, review queue, model decisions); Graph delta crawler with redacted error envelopes; Obsidian manifest / receipt / projection layer with the delta-link-fingerprint never-leaks invariant; deterministic review-queue policy with controller-overrides-model router; Ollama classification with `--mock-output` offline mode; Procore endpoint contract + auth-status stub; full operator CLI surface; baseline 240 passing tests. See `docs/evidence/construction-intelligence-phase-01/`.

**Phase 02 (corrective alignment).** Phase 02 Prompts 00–10 landed at HEAD `9564ee2`: source registry expanded from 3 sources / 2 projects to 14 sources / 6 projects via a Pydantic alias bridge (Phase 01 records preserved verbatim); V5 canonical SQLite schema with 10 additive tables + hard `CHECK` constraints on `read_only` / `mailbox_writeback_allowed` / `persist_full_body` invariants; folder-scoped Graph delta resolution + Hilltop ProjectHome linked-source discovery (no deep-index escape); Tropical baseline-comparison primitive against the canonical 8921-item / 39.78 GB inventory; OneDrive inventory-first policy + 4 PII review rules (12 → 16); Procore mapping corrected (`tropical: 23-435-01` → `2525840`) with `^\d{2}-\d{3}-\d{2}$` HB-number-shape validator and seed expanded 2 → 6 projects; Obsidian output hardening (`raw_delta_link_redacted` attestation, canonical `source_id` frontmatter alias, 7-output guardrail fence covering body-text / delta-link / token-shaped-secret); Ollama live-readiness probe via new `construction-agent ollama status` (offline-CI-safe, no live inference); email-intelligence deferred-foundation policy with Pydantic `Literal[False]` / `Literal[True]` guards and mailbox-mutation lockout static scans; closeout test count 413, ruff clean. See `docs/evidence/construction-intelligence-phase-02/`.

**Remaining external validation.** The following live validations were not exercised in any Phase 02 prompt and remain explicitly blocked or stubbed: live MSAL-backed Graph delta crawl against Tropical (requires interactive shell); live Procore OAuth + `/vapid/projects` round-trip (Procore HTTP client intentionally absent — `test_procore_module_imports_no_http_client` enforces this); live Ollama daemon presence (the readiness probe is exercised via mocked `requests.get` plus one offline `ollama status` invocation reporting `daemon_unreachable`); live mailbox metadata fetch — mailbox stays read-only at four layers (YAML policy, MSAL scope, Python adapter, SQLite CHECK), and although `Mail.ReadWrite.All` is granted at the tenant level, `IdentityConfig.delegated_scopes` continues to request only `Mail.Read`. The 4 pre-existing `test_obsidian_writer.py` failures noted in the Phase 01 final-closeout summary persist; they predate Phase 02 and are out of scope.

**Phase 05 (Procore Contracts & Financials) — Closed.** Phase 05 Prompts 01–12 landed at closeout SHA `6d77d35` (closeout commit follows): the Procore subsystem extends into the contract / financial-control surface (owner contracts, commitments, purchase orders, subcontractor invoices, RFQs, change events, budget) via the `procore/endpoints.py` registry (59 endpoints = 27 Phase 04A/04B operational + 32 financial), per-family normalizers + redaction utilities, additive **V9** SQLite schema (15 `procore_financial_*` projection tables + amount-facts ledger; V9 added billing periods + subcontractor invoices), the fail-closed live gate, generalized N+1 parent→child orchestration with parent-id tagging, 7 SQLite-only financial query commands + an Obsidian financial register, and a probe-first live-promotion pass against the `tropical` pilot (procore_project_id 2525840, company 5280, GET-only) that promoted **29** financial endpoints. Posture: **56 / 59 endpoints live-verified**, **3 fail-closed** (`purchase-order-detail-line-items` per-PO 404 data condition, `budget-change-line-items` 403 permission grant, `budget-details` unresolved-path sentinel) — documented as a deferred remediation list. Closeout validation green (pytest exit 0; ruff / `mypy src` / compileall clean; `procore validate` 28/28; no-secret probe over 2209 financial rows = 0 findings, `raw_body_persisted=0` / `redaction_applied=1`). No Microsoft 365 or Procore writeback. See `docs/evidence/construction-intelligence-phase-05-financials/` (Prompts 00–12) and `docs/architecture/16-procore-financials-phase-05.md`.

**Phase 06A (SharePoint / OneDrive File Intelligence) — Closed (Prompts 00–17).** A read-only delegated-Graph file-intelligence surface under `hb-assistant graph files`: operator `status` + `no-writeback-proof`; canonical V5 source projection (`sources`); SharePoint site/drive + OneDrive discovery (`sites`, `site resolve`, `drives`, `onedrive`); user-provided-link → canonical-ID resolution via the read-only Shares API (`link resolve`, no sharing-link redemption); rich driveItem metadata indexing, bounded baseline crawl, and hardened incremental delta sync (`index`, `crawl`, `delta`); project-aware file matching (`project-match`); pre-fetch ingestion-eligibility policy (`ingestion-policy`); controlled drive-aware download + **bounded redacted** extraction (`extract`, requiring explicit `--download`/`--extract`); sensitive-file review routing (`review-queue`); grouped marker-bounded Obsidian manifests/registers/review-summaries/receipts (`obsidian`); and source-linked retrieval over bounded excerpts (`retrieve`). Additive **V15–V19** SQLite schema (rich driveItem index, link resolution, project-match fields, `construction_file_ingestion_decisions` with the `review_required → no extraction` CHECK, download-receipt + extraction-run tables with `full_text_persisted = 0` / `source_file_copied_to_vault = 0` / `raw_download_url_persisted = 0` CHECKs). Guardrails: **dry-run is the default** for every write-capable command; side effects require explicit `--apply`/`--download`/`--extract`; no Microsoft 365 writeback; no source files copied into Obsidian; no full document text, signed URLs, `@microsoft.graph.downloadUrl`, or raw delta links persisted (delta links surface only as SHA-256 fingerprints); review-routed files cannot extract. **Deferred:** broad `Files.ReadWrite.All` consent is retained and **not** tightened in this phase — a documented risk (`docs/evidence/construction-intelligence-sharepoint-onedrive/22-deferred-permission-tightening-record.md`); the runtime stays read-only at four layers. An end-to-end pilot over the matrix (`resources/json/phase_06a_files_validation_matrix.json`) produced a behavior-level no-writeback / no-secret / no-vault-copy proof (`no-writeback-proof`: 19 mutation attempts blocked, 0 mutating calls across 39 files; `diagnostics scan-sensitive`: redacted indicators only, no matched secret values emitted) — `docs/evidence/construction-intelligence-sharepoint-onedrive/16-no-secret-no-writeback-proof.md`. Operator runbook: `docs/runbooks/phase-06a-operational-sharepoint-onedrive-workflows.md`. **Closeout (P17):** the full validation matrix ran end-to-end — ruff / `mypy src` (142 files) / compileall clean, `construction-agent validate` 4/4 (schema **V19**, final), `no-writeback-proof` 19 mutations blocked / 0 mutating calls across 39 files; pytest **1698 passed / 2 skipped** with **12 pre-existing email-track failures** (`upsert_email_model_classification` on `ConstructionStore`) that are out of scope for this files track (0 files-track failures) and handed to the email track. Broad `Files.ReadWrite.All` consent retained — tightening deferred. See `docs/evidence/construction-intelligence-sharepoint-onedrive/` (`17-final-validation-closeout.md`) and `docs/architecture/18-sharepoint-onedrive-file-intelligence-phase-06.md`.

**Phase 06B (Procore Operational Intelligence) — Closed (Prompts 00–16).** Turns the Procore sync/projection layer (Phases 04A/04B/05) into end-user **operational intelligence** via deterministic, **read-only** read models over the existing local SQLite tables — **no new schema (stays V19)**, every read model derived on demand. Hardening: endpoint rebaseline + ledger reconciliation, live-sync path/query hardening, N+1 / rate-limit hardening, held-endpoint final dispositions (3 Phase 05 endpoints preserved fail-closed), endpoint coverage + payload contracts (P00–P05). Read models + operator surface under `hb-assistant procore live`: `project-health`, `stale`, `overdue` (action queue), `financial exposure` (cost) + `schedule exposure` (decimal-safe amounts, never summed), `responsible-party-gaps` + `relationship-quality` (owner/assignee/BIC/vendor/location coverage, orphans, linkage, PO/commitment dupes), `risks`, `digest` (with optional `--since`), and a `retrieval-ready` **fact manifest** (redacted, source-linked facts across records / action signals / timeline events / exposure / amount facts; raw free text + change values never embedded; review-required records blocked). Marker-bounded `hb-assistant procore obsidian project-health / meeting-prep / daily-digest` notes (**dry-run default**, explicit `--apply --confirm` to the local vault; review-flagged content diverted to a warning banner). Guardrails: no Microsoft 365 writeback; no Procore writeback; no raw payload bodies persisted; no tokens/secrets/signed-URLs/PEMs; **no legal/claims/financial/safety/entitlement/schedule determinations** (intelligence/review aids only). An **executable** no-writeback/no-secret/no-raw-body proof (`hb-assistant procore live no-writeback-proof`, `proof_passed: true`) statically scans the 8 Phase 06B modules for mutating calls + HTTP-client imports + secret patterns, probes the 24 `raw_body_persisted` CHECK(=0) guardrail tables (distinct ⊆ {0}), and scans the evidence outputs. **Closeout (P16):** full matrix — `ruff check .` / `mypy src` (148 files) / compileall clean, `procore validate` 28/28, all operator commands operational, endpoint ledger current, Obsidian marker-bounded; pytest **1836 passed / 1 skipped** with **28 documented-unrelated failures** (17 migration version-assertions from a concurrent `project_identity` V20 migration committed to `main` after the P15 commit — Phase 06B added no migration; 7 pre-existing email-track `upsert_email_model_classification`; 4 date-dependent weekend automation), **0 Phase 06B operational failures**. Operator runbook: `docs/runbooks/phase-06b-operational-procore-workflows.md`. See `docs/evidence/construction-intelligence-phase-06b-procore-operational-intelligence/` (`16-final-validation-closeout.md`) and `docs/architecture/17-procore-operational-intelligence-phase-06b.md`.

**Phase 07C (Document Intelligence Promotion) — Closed (Prompts 00–13).** Promotes the read-only SharePoint/OneDrive file intelligence into source-linked, review-controlled **document intelligence** on the additive **V24** SQLite schema (six `construction_document_*` tables — cards + classification / project-match / relationship candidates + intelligence previews + projection runs — each with hard guard `CHECK(… = 0)` columns for `raw_document_text_persisted` / `raw_payload_persisted` / `signed_url_persisted` / `download_url_persisted` / `source_file_copied_to_vault` / `raw_prompt_persisted` / `raw_response_persisted` / `external_writeback_performed`). A deterministic-first, candidates-only pipeline under `hb-assistant graph files`: `materialize-document-cards` (283 hashed/redacted cards, all project `tropical`), `classify-document-cards` (283 advisory type candidates; 67 typed / 216 `unknown_needs_review`), `match-document-projects` (283 deterministic project matches), `evaluate-extraction-eligibility` (per-card disposition — 273 manual_approval_required / 5 metadata_only / 5 blocked / **0 eligible**; review-required files cannot extract), `build-document-relationships` (23 heuristic Procore document→record candidates; email/calendar arms deferred — those records are not yet `project_key`-aligned), `build-document-previews` (one review-controlled project preview), and `document-obsidian` (grouped marker-bounded document register + review notes, **dry-run preview by default** — the real vault was not written). Six **07C data-quality gates** added to `construction-agent data-quality gates` (classification/project-match/extraction/relationship coverage + source-scope + a document-intelligence safety scan); the **no-writeback / no-secret / no-raw-document-text proof** (`construction-agent data-quality no-writeback-proof`) extended over the nine 07C modules, the six V24 tables (guard CHECK + persisted-content scan), the 07C evidence tree, and the 07C Obsidian output base — `proof_passed=true`, fail-closed. Guardrails: no Microsoft 365 / Procore writeback; no raw document text, full paths, signed/download/tokenized URLs, tokens, or secrets in SQLite, evidence, or vault output; no auto-promotion of any candidate (all review-required); no legal/claim/financial/personnel/safety determinations. **Readiness honesty — 07D / meeting-prep remains BLOCKED:** `meeting_prep_readiness.ready=false`, blocked by `document_source_scope_compliance` (the live source registry has a source that is not scope-compliant under the document source policy — the OneDrive selected-folder allowlist must be formalized) and `review_required_routing_presence`; `auto_readiness_allowed=false`. **Closeout (P13):** ruff / `mypy src` (176 files) / compileall clean; `construction-agent validate` 4/4 (schema **V24**); full CLI matrix all exit 0; `no-writeback-proof` `proof_passed=true`; pytest **2064 passed**; gate map + proof verdict identical across `PYTHONHASHSEED` 1/2/3. **Deferred:** email/calendar relationship arms, Obsidian `--apply` to the real vault, and source-scope compliance (config/policy fix); the 06A `construction_drive_item_inventory` raw staging layer stays disclosed out-of-scope (its derived cards are hashed/redacted and proven clean). See `docs/evidence/construction-intelligence-phase-07c-document-intelligence/` (Prompts 00–13 + `phase-07d-08a-08b-handoff.md`) and `docs/architecture/30-42`.

**Phase 07D (Cross-Source Relationship & Meeting Prep) — Closed (Prompts 00–14).** Repo-truth audit + rebaseline at observed HEAD `733ffed` (schema **V24**, package `1.3.0`) ahead of any 07D implementation. **Prompt 00 is audit-only — no schema migration, no new CLI surface, no runtime behavior change.** It confirms ancestry (07A `3cf1652` → 07B `748ed7e` → 07C `733ffed`=HEAD, all ancestors), re-runs the full validation matrix at HEAD (ruff / `mypy src` (176 files) / compileall clean; `construction-agent validate` 4/4 schema 24; `procore validate` 28/28; all 9 CLI `--json` commands exit 0; `data-quality no-writeback-proof` `proof_passed=true`; pytest **2064 passed**), and reconciles gap **G9** — the static 07C closeout markdown recorded `repo_sha b65f3c0` while the audited closeout commit and live runtime stamping are `733ffed` (a docs-after-evidence artifact; the historical 07C evidence is left immutable and annotated, not rewritten). It classifies all G1–G10 blockers: G1 (`document_source_scope_compliance`) + G2 (`review_required_routing_presence`) are **must-fix-before-build**, deferred to Prompt 01; G3–G8 implement inside 07D; G9 resolved here; G10 deferred to 08C financial readiness. Guardrails: local-first, read-only against Microsoft 365 / SharePoint / OneDrive / Outlook / Calendar / Procore; no external writeback or write scopes; no raw email/document/calendar content, signed/download URLs, tokens, or secrets persisted; no auto-promotion of weak/model/sensitive/high-impact relationships. **Prompt 01 (07C remediation preflight)** resolves G1/G2 additively (no migration, schema stays **V24**): an explicit per-source OneDrive **all-folders** opt-in (`SourceLocation.allow_all_folders` + policy `allow_explicit_all_folders`, honored only on recognized OneDrive root kinds via a *local* evaluator set — the shared inventory-first scope set is untouched), with implicit/ambiguous root-wide indexing still fail-closed; and a broadened `review_required_routing_presence` gate that reconciles `review_required` evidence across the document/relationship/email/calendar review queues. On the live registry + current local SQLite, `graph files scope-compliance` is `all_compliant=true` (4 OneDrive roots `all_folders_explicit`, 0 blocked) and both gates flip to **pass** (tally 17 pass / 3 warning / 1 n/a / 0 deferred); `meeting_prep_readiness.ready=true`, `blocked_by=[]`, with `auto_readiness_allowed=false` (07D is never auto-claimed). pytest **2072 passed**; `no-writeback-proof` `proof_passed=true`. **G1/G2 resolved; Prompt 02 (relationship schema & contracts) may proceed**; G3–G8 remain 07D build work. **Prompt 02 (relationship schema & contracts)** lays down the cross-source substrate on the additive **V25** SQLite schema: ten tables shipping **empty** — `cross_source_relationship_candidates` + `cross_source_relationships` (deduped by a `UNIQUE` source/target/relationship edge key; candidates default `review_required=1`), `source_evidence_trails`, `meeting_prep_brief_runs` + `meeting_prep_brief_sections`, `project_issue_history_items`, `project_risk_digest_items`, `aging_exposure_report_items`, `cross_source_intelligence_obsidian_runs`, and `phase_07d_validation_runs` — each with the eight hard guard `CHECK(… = 0)` columns (`raw_email_body_persisted` / `raw_document_text_persisted` / `raw_calendar_payload_persisted` / `raw_prompt_persisted` / `raw_response_persisted` / `signed_url_persisted` / `download_url_persisted` / `external_writeback_performed`). Eight machine-readable JSON contracts (relationship / evidence-trail / brief / issue-history / risk-digest / aging-exposure + 07D data-quality-gates + validation-matrix) install under `resources/json/` and five policy seeds (relationship promotion, review-required relationship rules, brief policy, risk-digest policy, aging/exposure thresholds) under `resources/config/`, read by a pure `construction/relationships/contracts.py` loader; the relationship contract + promotion seed forbid local auto-promotion of weak/model/sensitive/high-impact edges. The ten tables are registered in the table-lifecycle contract (`table_count` 110→120, `phase_owner: 07D`, `operational_empty_expected`) so `data-quality table-inventory` classifies them from contract (none unmapped). **This prompt is schema + contracts + loader + tests only — no builders, no new CLI surface, no gate wiring, no external writeback.** ruff / `mypy src` (177 files) / compileall clean; `construction-agent validate` 4/4 (schema **V25**); full CLI matrix all exit 0; both no-writeback proofs `ok/proof_passed=true`; `table-inventory` reports schema_version 25 / contract_table_count 120 with all ten 07D tables `source=contract`; pytest **2135 passed**. **Prompt 03 (cross-source relationship substrate) may proceed.** **Prompt 03 (cross-source relationship substrate)** makes the V25 substrate live (no migration): new `ConstructionStore` methods (`upsert/list/count` for `cross_source_relationship_candidates`, `source_evidence_trails`, and `cross_source_relationships`) + a normalization engine (`construction/relationships/cross_source_substrate.py`) that reads the three existing edge-shaped candidate tables (document→record V24, calendar↔email V23, email→project/procore/calendar V11) and writes unified candidates + redacted evidence trails with **deterministic-hash idempotent IDs** (matching the table UNIQUE edge key), a confidence-class mapping to the V25 enum (`model_proposed`→`model_proposed`, `deterministic`→`deterministic`, score≥0.8→`strong_heuristic`, else `weak_heuristic`), and review/sensitive routing from `review_required_relationship_rules.seed.yaml`. A new `construction-agent relationships` CLI sub-app (`build` — dry-run default / `--apply` / `--project`; `status`) drives and inspects it. **`build` writes candidates + evidence trails only — it never promotes, so `cross_source_relationships` stays empty and weak/model/sensitive edges always route to review.** Refs are local IDs / existing hashes; JSON fields carry hashes/enums/booleans only; the eight guard `CHECK(… = 0)` columns stay 0. Prompt 04 will add the Procore-native + source-record-map arms, cross-family `project_key` alignment + dedup, the first live `--apply`, and policy-gated promotion. ruff / `mypy src` (178 files) / compileall clean; `construction-agent validate` 4/4 (schema **V25**); full CLI matrix + the two new `relationships` commands all exit 0 (live `build` dry-run plans 209 edges — calendar 117 / email 69 / document 23 — and writes nothing; live substrate left empty); both no-writeback proofs `ok/proof_passed=true`; `table-inventory` 25 / 120 (no new tables); pytest **2145 passed**. **Prompt 04 (document/email/calendar/Procore/source-record-map relationship normalization) may proceed.** **Prompt 04 (relationship normalization + deterministic promotion)** completes the source coverage (no migration): two new engine adapters — `_procore_edges` over the Procore-native edge graph (V7; record→record and record→entity, deterministic at confidence ≥ 0.999) and `_resolution_queue_edges` over the Phase 07A `relationship_resolution_queue` (V20; carrying its `confidence_class` verbatim via a new `confidence_class_override`) — plus three read-only store readers (`list_procore_record_edges`, `list_relationship_resolution_queue`, `resolve_source_record_project_key`). `build()` now backfills a missing `project_key` from `source_system_record_map` before filtering (a counted no-op while that map is empty; alignment never changes the dedup hash). A new `relationships promote` CLI command + `promote()` method perform **policy-gated deterministic promotion** into `cross_source_relationships` per `cross_source_relationship_policy.seed.yaml` — only `deterministic`, non-`sensitive_high_impact`, non-`review_required` candidates are promoted (`promoted_by='deterministic'`, idempotent `relationship_id`); weak/strong/model/sensitive are **never** auto-promoted. **First live `--apply`:** `relationships build --apply` wrote **1,880** candidates + **1,880** evidence trails (procore 1,671 / calendar 117 / email 69 / document 23; 159 review-required), and `relationships promote --apply` landed **1,671** deterministic Procore relationships (209 non-deterministic skipped; 0 sensitive/review promoted). Refs stay local IDs / existing hashes; `metadata_json` / `evidence_redacted` free-text are never read into substrate rows; the eight guard `CHECK(… = 0)` columns stay 0. ruff / `mypy src` (178 files) / compileall clean; `construction-agent validate` 4/4 (schema **V25**); full CLI matrix + the three `relationships` commands all exit 0; both no-writeback proofs `ok/proof_passed=true` **over the now-populated substrate**; `table-inventory` 25 / 120 (no new tables); `meeting_prep_readiness` unchanged (substrate population alone does not flip it); pytest **2150 passed**. **Prompt 05 (meeting-prep prerequisite gates) may proceed.** **Prompt 05 (meeting-prep prerequisite gates)** wires the 07D meeting-prep prerequisite gate set into the existing `construction-agent data-quality gates` evaluator (no migration, no new CLI command, schema stays **V25**): five new `GateEvaluator` gates (`future_phase="07D"`) over the V25 substrate — `cross_source_relationship_candidate_coverage` (substrate built), `deterministic_relationship_quality` (deterministic edges well-formed; malformed ⇒ `fail_blocking`), `evidence_trail_completeness` (1:1 evidence trails; gap ⇒ `fail_blocking`), `weak_model_sensitive_review_routing_accuracy` (every model/sensitive/weak candidate is review-routed; misrouted ⇒ `fail_blocking`, reported only — never promoted), and `meeting_prep_prerequisite_status`, a **source-scope 3-way** that reuses the 07C scope evaluator (explicit OneDrive all-folders stays compliant) and adds the meeting-prep-specific `deferred_not_blocking` case (no OneDrive sources in scope, or no document-card inputs) distinct from `fail_blocking` (implicit/ambiguous root). `meeting_prep_readiness` now merges the 07B manifest prerequisites with the 07D set (a `deferred_not_blocking` 07D gate keeps readiness blocked; `auto_readiness_allowed` stays `false`) and adds a `prerequisite_categories` breakdown (calendar / email / document / relationship / review / safety / source_scope); the readiness claim gains an honest `needs_07d_data` value between `needs_07b_07c_data` and `ready`. Gates copy only counts / enum classes / boolean flags / source-keys — never decoded `signals_json`/`source_reference_json`. On the live populated substrate all five gates **pass** (candidates 1,880; deterministic 1,671 / 0 malformed; evidence trails 1,880; weak/model/sensitive 158 / 0 misrouted; OneDrive 4 roots all-folders-explicit / 0 blocked; 283 document cards) and `meeting_prep_readiness.ready=true` honestly with `auto_readiness_allowed=false`; on an empty substrate the gates correctly **defer** and readiness stays blocked. ruff / `mypy src` (178 files) / compileall clean; `construction-agent validate` 4/4 (schema **V25**); full CLI matrix all exit 0; both no-writeback proofs `ok/proof_passed=true` over the populated substrate; `table-inventory` 25 / 120 (no new tables); pytest **2165 passed**. *(The `phase_07d_validation_matrix` `data-quality phase-07d-gates` / `meeting-prep status` commands are intentionally deferred to the later 07D gates-wiring prompt — meeting-prep prerequisites are validated through `data-quality gates`.)* **Prompt 06 (meeting-prep brief materialization) may proceed.** **Prompt 06 (meeting-prep brief materialization)** materializes source-linked, review-controlled meeting-prep briefs into the two V25 tables `meeting_prep_brief_runs` + `meeting_prep_brief_sections` (shipped empty in Prompt 02) via a new `construction-agent meeting-prep build/status` sub-app (no migration, schema stays **V25**). A `MeetingPrepBriefBuilder` (`construction/meeting_prep/brief_builder.py`) writes one brief run per project with the eight policy sections (`meeting_context`, `project_context`, `open_items`, `aging_items`, `recent_activity`, `risk_exposure_watchlist`, `review_required_warnings`, `confidence_and_stale_unknown_warnings`), each carrying a redacted compact summary (counts/enums/local-ids only), a confidence class, an optional evidence-trail ref, a review flag, and stale/unknown flags. Deterministic-hash `brief_run_id`/`section_id` make `--apply` idempotent; dry-run is the default and writes nothing. Sourcing is honest to live-data truth: `construction_project_identity` is empty (projects enumerated from the substrate; `project_context` degrades to `unknown_project_identity`, never copies `project_name_raw`), live calendar events are unmatched (`meeting_context` reports 0 project-matched + a flagged unmatched-upcoming count), and `aging_items`/`risk_exposure_watchlist` are emitted as **deferred** placeholders (`deferred_source`, no fabricated items) pending Prompts 07–09. The brief is **prerequisite-gated**: per `require_prerequisite_gates` it reads the Prompt 05 `meeting_prep_readiness` and, when not ready, writes a `status="blocked"` run with **zero sections** (a blocked brief is an honest outcome, not an error; a `readiness` injection seam — never set by the CLI — lets tests exercise materialization). Six new store methods (a promoted-relationship reader + brief run/section upsert/list/count) mirror the substrate readers; the eight guard `CHECK(… = 0)` columns are never written. Advisory only — no final legal/contractual/claim/safety/financial determination; weak/model/sensitive relationships stay review-required and nothing is auto-promoted. ruff / `mypy src` (180 files) / compileall clean; full CLI matrix all exit 0; live `meeting-prep build --apply` materialized one `tropical` run with all eight sections (3 review-required surfaced), dry-run wrote nothing, `meeting-prep status` reflects 1 run / 8 sections / 1 materialized; no-raw-content scan clean over the apply + status payloads; both no-writeback proofs `ok/proof_passed=true`; `table-inventory` 25 / 120 (no new tables); `meeting_prep_readiness_claim="ready"` unchanged; pytest **2173 passed**. **Prompt 07 (project issue history) may proceed.** **Prompt 07 (project issue history)** materializes `project_issue_history_items` (shipped empty in Prompt 02) by grouping the cross-source substrate into per-issue families using **deterministic + strong-heuristic relationships only**, via a new `construction-agent issue-history build/status` sub-app (no migration, schema stays **V25**). An `IssueHistoryBuilder` (`construction/issue_history/issue_history_builder.py`) groups eligible candidates (`confidence_class ∈ {deterministic, strong_heuristic}`, excluding `sensitive_high_impact`/`model_proposed` — weak/model/sensitive are never grouped or promoted) **one family per distinct anchor source record** `(source_family, source_record_ref)` — a bounded, deterministic unit (the natural RFI / change order / commitment), deliberately **not** a transitive connected component (which would collapse the project into mega-families through shared entities like vendor/created_by/category). Each family carries `source_families_json` (distinct families touched), `confidence_class` (deterministic iff all member edges deterministic, else strong_heuristic), `review_required` (true for any review-flagged edge or any strong-heuristic family), a representative `evidence_trail_id`, and `issue_kind` (procore endpoint segment, else source record type). `latest_activity_utc`/`age_days`/`status` are **best-effort honest**: a procore anchor resolves against `procore_live_records` (record_key `project|endpoint|parent|id`, 557/562 refs match) for a real `updated_at_utc` + computed `age_days` + a **normalized bounded status token** (Procore dict-string statuses like `{'id':…,'name':'Open','mapped_to_status':'open'}` are parsed and never persisted raw); an unresolved anchor gets NULL activity + a `stale_unknown` flag — never fabricated. Deterministic-hash `issue_family_id` makes `--apply` idempotent; dry-run is the default and writes nothing; three new store methods mirror the brief readers and the eight guard `CHECK(… = 0)` columns are never written. No prerequisite gate / no policy seed (objective is materialization); the meeting-prep brief is untouched (`aging_items` stays a deferred placeholder for Prompt 09). ruff / `mypy src` (182 files) / compileall clean; full CLI matrix all exit 0; live `issue-history build --apply` materialized **598** `tropical` families (562 deterministic / 36 strong review-required; 522 activity-resolved / 76 stale-flagged; status tokens all bounded), dry-run wrote nothing, `issue-history status` reflects `items=598`; no-raw-content scan clean (no raw status payload); both no-writeback proofs `ok/proof_passed=true`; `table-inventory` 25 / 120 (table already registered); `meeting_prep_readiness_claim="ready"` unchanged; pytest **2182 passed**. **Prompt 08 (project risk digest) may proceed.** **Prompt 08 (risk digest)** materializes review-controlled risk digests into `project_risk_digest_items` (shipped empty in Prompt 02) via a new `construction-agent risk-digest build/status` sub-app (no migration, schema stays **V25**). A `RiskDigestBuilder` (`construction/risk_digest/risk_digest_builder.py`) classifies local risk indicators into the four policy `risk_source_class` values: **source_stated** (open `procore_action_signals` grouped by signal_type — deterministic), **inferred_candidate** (risk-bearing `project_issue_history_items` — status overdue/void/rejected/out_for_pricing or `age_days ≥ 31` open — grouped into overdue/void/rejected/aging_open indicators, strong_heuristic), **review_required** (review-required / weak / `sensitive_high_impact` candidates grouped by relationship_type, weak_heuristic), and **model_proposed** (`model_proposed=1` candidates, model_proposed). The digest is **bounded** — one item per `(risk_source_class, risk_indicator_type)` with a count + ≤5 safe sample references, not one row per record — with a deterministic-hash `risk_digest_id` (idempotent `--apply`; dry-run default writes nothing). The `review_required` flag is set when the indicator's mapped category ∈ `review_required_categories` (financial/cost_impact/schedule_impact/safety/claim/contractual/personnel/legal via a keyword map) OR the class ∈ {review_required, model_proposed} OR an underlying edge/family is review-required; weak/model/sensitive indicators stay review-required and are never auto-promoted. No double-count (procore record status risk is already reflected through issue history). Four new store methods mirror the issue-history readers (`list_procore_action_signals` selects safe identifier/enum columns only — never title/summary/metadata free-text); the eight guard `CHECK(… = 0)` columns are never written; items persist only counts/enums/category tokens/endpoint names/ids. No prerequisite gate / no policy-gated readiness (objective is materialization); the meeting-prep brief is untouched (`risk_exposure_watchlist` stays a deferred placeholder). ruff / `mypy src` (184 files) / compileall clean; full CLI matrix all exit 0; live `risk-digest build --apply` materialized a bounded **44**-item `tropical` digest (34 source_stated / 4 inferred / 6 review_required; 30 review-required; 0 model — none live), dry-run wrote nothing, `risk-digest status` reflects `items=44`; no-raw-content scan clean (no title/status payload); both no-writeback proofs `ok/proof_passed=true`; `table-inventory` 25 / 120 (table already registered); `meeting_prep_readiness_claim="ready"` unchanged; pytest **2190 passed**. **Prompt 09 (aging / exposure report) may proceed.** **Prompt 09 (aging & exposure reporting)** materializes `aging_exposure_report_items` (shipped empty in Prompt 02) via a new `construction-agent aging-exposure build/status` sub-app (no migration, schema stays **V25**). An `AgingExposureBuilder` (`construction/aging_exposure/aging_exposure_builder.py`) classifies one row per `procore_live_records` record across all record families (UNIQUE project_key/record_family/record_ref): `record_family = endpoint_id`, `record_ref =` reconstructed record_key `project|endpoint|parent|id`, real `age_days` from `updated_at_utc`, assigned an aging `threshold_band` from the `aging_exposure_thresholds` seed (current 0–7 / monitor 8–14 / aging 15–30 / stale 31–60 / critical_review 61–∞, honoring `record_family_overrides`); a missing timestamp → `threshold_band="unknown"` + `confidence_class=NULL` (never overstated). `stale_flag` = stale/critical band; `missing_status_flag` = null/empty status; `status` is normalized to a bounded token (Procore dict-strings parsed, never persisted raw). **"Exposure" is aging-based and "financial boundaries" means financial record families (budget/commitment/invoice/change-order/billing/prime/purchase-order) are recognized and a financial-family record in a stale/critical band is flagged review-required and counted in a `financial_exposure` summary — while no raw financial amounts are persisted (the table has no amount column; that is the boundary).** `review_required` = critical_review band OR (financial family AND stale) OR the source record's `review_required`/`sensitive_reason`; sensitive records stay review-required and are never auto-promoted. Deterministic-hash `aging_item_id` makes `--apply` idempotent; dry-run default writes nothing; three new store methods mirror the risk-digest readers and the eight guard `CHECK(… = 0)` columns are never written; `evidence_trail_id` links to the substrate when the record participates (else NULL — `record_ref` is itself the source reference). No prerequisite gate / no policy-gated readiness (objective is materialization); the meeting-prep brief is untouched (`aging_items` stays a deferred placeholder). ruff / `mypy src` (186 files) / compileall clean; full CLI matrix all exit 0; live `aging-exposure build --apply` materialized **1780** `tropical` rows across 39 families (bands current 38 / monitor 8 / aging 41 / stale 40 / critical_review 1053 / unknown 600; stale 1093; missing_status 854; financial_exposure total 875 / critical 437), dry-run wrote nothing, `aging-exposure status` reflects `items=1780`; no-raw-content scan clean (no status payload, no amounts); both no-writeback proofs `ok/proof_passed=true`; `table-inventory` 25 / 120 (table already registered); `meeting_prep_readiness_claim="ready"` unchanged; pytest **2200 passed**. **Prompt 10 (correspondence context) may proceed.** **Prompt 10 (review-controlled correspondence context)** ties email thread summaries to the records they relate to — meetings, RFIs, submittals, changes, commitments, daily-log issues, inspections, and documents — **only where relationships already exist**, via a new **read-only** `construction-agent correspondence context/status` sub-app (no schema change, **no V25 table, no persistence** — extends the existing read-only `construction/correspondence/` module). A `CorrespondenceContextBuilder` (`construction/correspondence/correspondence_context.py`) anchors on each `email_thread_summaries` row: meetings come from `meeting_email_relationship_candidates` matched on `thread_key_hash == hash_value(thread_key)`; record ties come from the email-source `cross_source_relationship_candidates` rolled up message→thread via `email_messages` (only message_id/thread_key are read — never web link/body), each target classified into one of the 8 categories (with `project_match` surfaced as a separate `project_confirmed` flag). Only threads with ≥1 tie are linked; a thread/tie is `review_required` if any contributing edge is review-required/weak/model/sensitive, and nothing is auto-promoted (read-only). Output carries only bounded `summary_redacted` (metadata-only), counts, local record refs/hashes/endpoint names, confidence classes, and evidence-trail ids. No store or schema changes; reuses existing readers + `hash_value`; inventory stays 120. ruff / `mypy src` (187 files) / compileall clean; full CLI matrix all exit 0; live `correspondence context` tied all **19** `tropical` thread summaries to records (meetings 117 / rfis 4 / daily_log_issues 2 / commitments 1; `documents=0` honestly — those email edges belong to the 47 of 66 threads that 07B did not summarize, so there is no thread summary to anchor them), `correspondence status` mirrors the summary; no-raw-content scan clean; both no-writeback proofs `ok/proof_passed=true`; `table-inventory` 25 / 120 (no new table); `meeting_prep_readiness_claim="ready"` unchanged; pytest **2206 passed**. **Prompt 11 (Obsidian cross-source outputs) may proceed.** **Prompt 11 (Obsidian cross-source intelligence outputs)** projects the six 07D read models into **marker-bounded Obsidian notes without raw content**, via a new `construction-agent cross-source obsidian/status` sub-app (no migration, schema stays **V25**), mirroring the Phase 07A `data-quality obsidian` / 07C `document-obsidian` renderers. An `ObsidianCrossSourceRenderer` (`construction/obsidian/cross_source.py`) renders six `HB-CROSS-SOURCE-<KIND>:START/END` marker-bounded notes — Cross-Source Relationships, Meeting-Prep Readiness, Project Issue History, Risk Digest, Aging & Exposure, Correspondence Context — sourced from the six redacted read-model `*_status()` summaries (so only counts/enums/bands/review-counts reach a note; no raw content can leak). Every rendered note passes an output-fence (rejects token/URL/PEM/full-text markers) and an atomic marker-bounded replace preserves user content + frontmatter outside the markers (idempotent re-apply). Dry-run is the default (writes `11-obsidian-cross-source-output-preview.md` + a proof JSON to the 07D evidence folder; **no vault**); `--apply` (mutually exclusive) writes the six notes under `Construction Intelligence/Phase 07D Cross-Source Intelligence/` in the local vault, degrading gracefully when no vault is configured. A run record persists to the V25 `cross_source_intelligence_obsidian_runs` audit table (idempotent on a deterministic `obsidian_run_id`; counts/enums only; the eight guard `CHECK(… = 0)` columns are never written). Three new store methods; review-required items are surfaced but never presented as authoritative and nothing is auto-promoted. ruff / `mypy src` (189 files) / compileall clean; full CLI matrix all exit 0; live `cross-source obsidian --dry-run` rendered the six sections (`notes_written=0`, `applied_to_vault=false`, aggregate `review_required_count=2027`) and wrote the committed preview/proof artifacts, `cross-source status` reflects the run (`by_mode={dry_run:1}`); **`--apply` was not run against the operator's real vault** (outward side effect — apply is unit-tested against a temp vault, consistent with the 07A/07C precedent); no-raw-content scan clean over notes/report/artifacts; both no-writeback proofs `ok/proof_passed=true`; `table-inventory` 25 / 120 (table already registered); `meeting_prep_readiness_claim="ready"` unchanged; pytest **2213 passed**. **Prompt 12 (07D gates wiring / no-writeback proof) may proceed.** **Prompt 12 (07D data-quality gates)** completes the 07D gates wiring (no migration, schema stays **V25**): a new `construction/data_quality/phase_07d.py` (`evaluate_phase_07d_data_quality_gates`) assembles the full **twelve-field** `phase_07d_data_quality_gates` conformance report — the five Prompt-05 meeting-prep prerequisite gates (reused via `evaluate_data_quality_gates`, no duplication), four V25 output-coverage gates (meeting-prep brief / issue-history / risk-digest / aging-exposure — `pass` when populated, `deferred_not_blocking` on empty so readiness is never overstated), `obsidian_output_safety` (guard-column scan of the obsidian-run audit table), `stale_unknown_warning_coverage`, and a 07D-scoped `no_writeback_no_secret_no_raw_content_proof` (guard-column sum + forbidden-pattern scan over all ten V25 tables). It is exposed via a new read-only `construction-agent data-quality phase-07d-gates --json` (the command the `phase_07d_validation_matrix` referenced). A `gates.source_scope_safe_counts(sources)` helper + a refinement to `_gate_meeting_prep_prerequisite_status` emit four **named safe counts** — `onedrive_explicit_subset_sources` / `onedrive_explicit_all_folders_sources` / `onedrive_implicit_root_blocked_sources` / `sharepoint_approved_all_nested_sources` — **counts only, never folder names / paths / web URLs / drive IDs / item IDs**, with explicit OneDrive all-folders selection counted as compliant (never misclassified as blocked). The contract gains an additive `source_scope_safe_counts` field list (required_fields/guardrails unchanged; inventory stays 120). ruff / `mypy src` (190 files) / compileall clean; full CLI matrix all exit 0; live `phase-07d-gates` reports `ok=true`, all twelve fields **pass**, `required_fields_covered=true`, `phase_07d_intelligence_ready=true`, safe counts {all_folders 4 / implicit_blocked 0 / sharepoint 10}, and the 07D no-writeback proof passes over all 10 V25 tables; `data-quality gates` `meeting_prep_prerequisite_status` carries the safe counts; no-raw-content scan clean (no folder/path/URL/id); both no-writeback proofs `ok/proof_passed=true`; `table-inventory` 25 / 120; `meeting_prep_readiness_claim="ready"` unchanged; pytest **2221 passed**. **Prompt 13 (07D no-writeback proof command) may proceed.** **Prompt 13 (no-writeback / no-secret / no-raw-content proof — 07D arm)** extends the formal `build_data_quality_no_writeback_proof` (`construction/data_quality/safety.py`) with a Phase 07D arm so the existing `construction-agent data-quality no-writeback-proof` command now proves no external writeback / no secret / no raw content across all of 07D as well as 07A/07B/07C (no migration, schema stays **V25**, no new command — the objective is to *extend* the proof). Mirroring the 07C arm from reusable scan helpers, the 07D arm scans the **nine** 07D modules (mutation verbs via regex + AST, dangerous imports, value-shaped secrets), the **ten** V25 tables (the eight guard `CHECK(… = 0)` columns + a forbidden-pattern/secret content scan over the safe text columns), the 07D evidence tree, and the 07D Obsidian output subdir; each contributes a boolean AND-chained into `proof_passed` (fail-closed — any finding flips it false), with seven `checks_detail` entries, `scanned_modules_07d`, an extended `phase` string, and `no_raw_values_persisted` (now AND `guards_07d_ok`/`content_07d_ok`) + scope/note covering the 07D surfaces. Findings are pattern labels + `table.column` / file locations only — never the offending value. A clean 07D pass is meaningful: the scanners are test-proven non-vacuous (a synthetic `import requests` + `client.post(...)` module is flagged; injecting a `?sig=…` tokenized URL into a V25 text column fails the content scan closed, value never echoed), the nine modules carry no mutation verb / dangerous import, the value-shaped secret patterns leave the 07D evidence files clean despite quoting the `_LEAK` documentation literal, and the live V25 tables show 0 guard violations / 0 content findings. ruff / `mypy src` (190 files) / compileall clean; full CLI matrix all exit 0; live `data-quality no-writeback-proof` `proof_passed=true` now covering 07D (phase string includes "Phase 07D Prompt 13"; 9 modules; all seven 07D checks pass with empty findings; scope includes `phase_07d_cross_source_meeting_prep`); no-raw scan over `checks_detail` clean; `phase-07d-gates` `ok=true` with its own `no_writeback_proof.proof_passed=true`; `graph files no-writeback-proof` `ok=true`; `table-inventory` 25 / 120; pytest **2226 passed** (+5 new; existing 07A/07B/07C proof tests pass with one scope assertion broadened). **Prompt 14 (07D final closeout) may proceed.** See `docs/evidence/construction-intelligence-phase-07d-cross-source-meeting-prep/` (`00-repo-truth-rebaseline.md`, `01-07c-remediation-preflight.md`, `02-relationship-schema-and-contracts.md`, `03-cross-source-relationship-substrate.md`, `04-document-email-calendar-procore-relationship-normalization.md`, `05-meeting-prep-prerequisite-gates.md`, `06-meeting-prep-brief-materialization.md`, `07-project-issue-history.md`, `08-risk-digest.md`, `09-aging-and-exposure-reporting.md`, `10-review-controlled-correspondence-context.md`, `11-obsidian-cross-source-intelligence-outputs.md`, `12-07d-data-quality-gates.md`, `13-no-writeback-no-secret-no-raw-content-proof.md`) and `docs/architecture/43`–`56`. **Prompt 14 (07D final closeout & 08A handoff)** clears the last carried-over drift before Phase 08A runtime work (no migration, schema stays **V25**): the `phase_07d_validation_matrix` command name `data-quality phase-07d-no-writeback-proof` is now a real **read-only CLI alias** delegating to the same `build_data_quality_no_writeback_proof` builder (the matrix resolves to an implemented surface, matching the `phase-07d-gates` first-class precedent — the canonical `no-writeback-proof` is unchanged); the meeting-prep `aging_items` / `risk_exposure_watchlist` deferral is confirmed an **intentional honest placeholder** (real integration is routed to the Phase 08A context builder, which will consume the issue-history / risk-digest / aging-exposure read models directly with explicit warnings — no brief rebuild); and an intermittent test-isolation flake in the `_LEAK` no-raw-content guard regex (a bare `eyJ` false-positive under `IGNORECASE`) is fixed across the ten 07D test files by length/structure-bounding the JWT alternative to `eyJ[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{6,}` (no production redaction change). Re-validated at HEAD: ruff / `mypy src` (190 files) / compileall clean; `construction-agent validate` 4/4 (schema **V25**); `data-quality no-writeback-proof` + the new `phase-07d-no-writeback-proof` alias + `phase-07d-gates` (`ok=true`) all exit 0 with `proof_passed=true`; `table-inventory` 25 / 120; pytest **2227 passed**. **Phase 07D is closed; Phase 08A (local-first second-brain runtime) may proceed** from this remediation baseline. See `docs/evidence/construction-intelligence-phase-08a-second-brain-runtime/` (`00-repo-truth-rebaseline.md`, `01-phase-07d-gap-audit.md`, `phase-07d-final-closeout-and-08a-handoff.md`).

**Phase 08A (Local-First Second-Brain Runtime) — Active (Prompts 02–15; Prompt 09 deferred).** A deterministic, local-first, mock-first internal-agent runtime under `hb-assistant second-brain`, additive on the **V26** SQLite schema (**141** contract tables / 137 live; schema unchanged across the phase). **Prompt 02** lands the additive V26 second-brain tables + foundational contracts; **03** the dependency/config surface + the mock/live Claude adapter boundary (model-profile enforcement + metadata-only model-call receipts); **04** the retrieval policy, context budget, and the allowlisted **Retrieval Broker (A03)** (the only path to model-bound context — no raw SQL, no excluded families); **05** approved marker-bounded Obsidian indexing (read-only); **06** the thirteen allowlisted read-only SQLite query tools (no arbitrary SQL); **07** the **Retrieval Orchestrator (A01)** + **Research Packet Agent (A02)** (research-before-synthesis; insufficient context degrades or blocks, never overstates); **08** the interactive query CLI + **Answer Synthesis Agent (A04)** (Tier-3 / high-impact never a final conclusion); **10** the **Memory Curator (A07)** + **Operator Preference (A08)** (source-linked, review-controlled, preferences never override safety); **11** the **Daily Brief Context Builder (A06)** + **Review Triage Agent (A09)** (five card kinds + a source-linked delivery-handoff input); **12** daily-brief generation + the **Output Evaluation Agent (A05)** (apply blocked unless evaluation passes) writing approved marker-bounded Obsidian output to `Work/HB Personal Assistant/12_Daily_Brief/` + a local-only Phase 08B delivery-handoff payload; **13** the launchd scheduling runbook + dry-run install preview (20:00 → following day's brief; no plist written, no `launchctl` invoked); **14** the `second-brain data-quality phase-08a-gates` evaluator (pass/warning/fail_blocking/deferred_not_blocking; offline/mock synthesis → warning, unimplemented → deferred, readiness never overstated); **15** the `second-brain data-quality no-writeback-proof` (51 modules + the 18 V26 tables' guard columns + evidence tree + generated brief/handoff outputs + metadata-only model receipts, fail-closed). **Prompt 09 (Chat Session Memory) is deferred** — its `interactive_chat_sessions` substrate exists but no agent was built (classified, not overstated). Guardrails: external systems read-only; no source-system writeback; no raw prompts/responses/bodies/document-text/calendar/URLs/secrets persisted (metadata-only receipts, guard `CHECK(… = 0)` columns); dry-run defaults for write-capable local ops; the only outbound call is the lazy, opt-in Anthropic `messages.create` model boundary (receipts metadata-only). **Prompt 16 closeout:** ruff / `mypy src` (242 files) / compileall clean; `construction-agent validate` 4/4 (schema **V26**); `table-inventory` 26 / 141; legacy + second-brain `no-writeback-proof` both `proof_passed=true`; `phase-08a-gates` `ok=true` (8 pass / 1 warning / 0 fail / 3 deferred); pytest **2535 passed**. Handoffs are explicit — 08B (automation hardening + HTML brief + notifications), 08C, 08D (MCP: "expose workflows only; never expose stores"), 09 (embeddings behind the retrieval broker + deferred chat-session memory). See `docs/evidence/construction-intelligence-phase-08a-second-brain-runtime/` (`final-validation-closeout.md`, `phase-08a-final-handoff-to-08b-08c-08d-09.md`, and the per-prompt proofs) and `docs/architecture/57`–`72`.

**Phase 08B (Automation Delivery & Observability) — Closed.** Phase 08B Prompts 00–14 delivered the local automation/delivery/observability substrate and the Automation Execution Completion Addendum (Prompts 00–09) closed the remaining `automation_execution` gate. The phase now provides a local-only scheduled daily brief workflow with LaunchAgent integration, first-run-after-wake/catch-up handling, no-overlap file locking outside the repo, run registry, bounded retry/backoff, safe replay, failed-run diagnostics, job health, last-good-run tracking, polished local HTML delivery, macOS notification, delivery/open receipts, and proof-backed observability. **Closeout (P10):** validation green — `python -m compileall -q src` exit 0; `ruff check .` clean; `mypy src` no issues across **255** files; `pytest -m "not integration and not live and not manual"` **2834 passed / 1 deselected**; `construction-agent validate` 4/4 (schema **V34**, additive history only); `second-brain status` 34/34 with guardrails `external_writeback=false` / `raw_content_persisted=false`; `second-brain automation health` `overall_status=ok` (`RUN_OK`, 0 degraded); `second-brain data-quality phase-08b-gates` `ok=true` with **16 pass / 0 warning / 0 fail_blocking / 0 deferred** (`automation_execution=pass`, `required_fields_covered=true`); `second-brain data-quality no-writeback-proof` `proof_passed=true` (`no_external_writeback`, `no_raw_values_persisted`, `no_live_call_performed`, `executor_modules_ok`, `executor_08b_evidence_ok` all true). Final gates report `automation_execution=pass` and **`readiness_overstated=false`**; no external-system writeback, no external delivery services, no raw source content, no raw prompts/responses, no signed/download URLs, and no external HTML assets are introduced (the LaunchAgent install and daily-brief vault apply remain operator-gated `--apply --confirm`, exercised against temp/fake surfaces in tests). Handoffs are explicit — **08C** (financial readiness), **08D** (MCP: "expose workflows only; never expose stores"), **09** (embeddings behind the retrieval broker + deferred chat-session memory). See `docs/evidence/construction-intelligence-phase-08b-automation-hardening/` (`phase-08b-final-validation-closeout.md`, `session-handoff.md`, and the per-prompt proofs) and `docs/architecture/73`–`96`.

**Phase 08C (Financial Readiness) — Closed (Prompts 00–14).** A deterministic, local-first, advisory-only financial-readiness substrate under `hb-assistant second-brain financial …`, additive on the **V35/V36** SQLite schema (ten V35 financial tables + the V36 review-item confidence label; money as canonical decimal TEXT + integer minor units, never binary float). Delivers amount normalization, currency / WBS-cost-code / source-coverage completeness, the financial source-coverage matrix, advisory exposure marts, the Financial Fact Readiness Agent, forecast-readiness gates, deterministic review-required routing (seven trigger categories → reason code / review tier / confidence), the operator CLI surfaces (`readiness`, `coverage`, `exposure-summary`, `review-items`, `no-writeback-proof`, plus `data-quality phase-08c-gates` and `data-quality phase-08c-no-writeback-proof`), the data-quality gate evaluator, and the no-writeback / no-raw-financial-output safety proof. **Closeout (P14):** validation green — `python -m compileall src tests` exit 0; `ruff check .` clean; `mypy src` no issues across **259** files; `pytest -m "not integration and not live and not manual"` **2895 passed / 0 failed**; `construction-agent validate` exit 0 (schema **V36**, additive history only); all seven 08C CLI surfaces exit 0; `second-brain data-quality phase-08c-gates` `proof_passed=true` (**21 pass / 1 warning / 0 fail_blocking / 0 deferred_not_blocking**, `readiness_overstated=false`); `financial no-writeback-proof` and `data-quality phase-08c-no-writeback-proof` both `proof_passed=true`. Guardrails: local-first; no external-system writeback; Procore/M365 read-only; no raw payloads/prompts/responses/bodies/document-text/calendar/signed-or-download-URLs/secrets persisted in any V35/V36 table or evidence; money never binary float; all financial outputs are advisory review aids only — not approvals, claims, entitlements, determinations, or forecasts. **Deferred carry-forward:** three not-yet-live-verified Procore endpoint shells (`purchase-order-detail-line-items`, `budget-details`, `budget-change-line-items`) keep `forecast_readiness` `source_coverage` at `deferred_not_blocking` (a deferred external dependency; forecasting is out of Phase 08C scope). Handoffs are explicit — **08D** (MCP: "expose workflows only; never expose stores"), **09** (embeddings behind the retrieval broker + deferred chat-session memory). See `docs/evidence/construction-intelligence-phase-08c-financial-readiness/` (`final-validation-closeout.md` and the per-prompt proofs) and the `docs/architecture/construction-intelligence-phase-08c-*` records.

**Phase 08D (Local MCP Bridge) — Closed, operational (Prompts 00–15).** A local-first, stdio-only, fail-closed Model Context Protocol bridge under `hb-assistant second-brain mcp …` that exposes the existing **read-only** second-brain workflows to a local MCP client (e.g. Claude Desktop) on the additive **V37** schema (two metadata-only receipt tables — tool-call + denial — each with twenty guard `CHECK(… = 0)` columns). The bridge: nine allowed workflow-wrapper tools behind a deny-first **ToolBroker** (canonicalize → deny-first → allowed-registry → argument-validation → wrapper → bounded no-raw output → metadata-only receipt), five safe resources, five advisory prompts, a fail-closed permission policy (8 `allow_*` flags all false), stdio-only transport, the Claude Desktop config-preview (preview-only, never auto-written), the permission/audit surface, the data-quality gate evaluator, and the no-raw / no-writeback guard proofs (Prompts 00–14). **Prompt 15 (operational closeout)** installs the optional `mcp` SDK (`pip install -e ".[mcp]"`, mcp 1.27.2) and wires a real low-level stdio MCP server (`mcp/sdk_server.py` → `serve_stdio` → `serve_stdio_loop`) over the existing safe broker/resources/prompts — `second-brain mcp serve --stdio` now serves a live local session (the broker stays the single deny-first authority via `validate_input=False`); flips the last gate (`validation_matrix`) to a live, static, **SDK-agnostic** proof (contract + dual-tree parity + closeout-critical evidence) so `phase-08d-gates` reports **14 pass / 0 deferred**; and makes `ready_to_serve` truthful — derived after `serve_blockers`, with the optional-SDK blocker appended only when the SDK is absent (true when installed/operational, false otherwise; no overstatement). The 08A `mcp_exposure` gate and the `mcp_implemented` markers in 08A-era proofs are left as **historical phase-08A attestations** (the same convention by which 08B's automation hardening leaves 08A's `automation_hardening` gate `deferred_not_blocking`); the authoritative operational status lives in `phase-08d-gates` (`ready_to_serve=true`), `mcp status`, and the operational serve proof. Guardrails: local-first; stdio-only (no network listener); every tool call (allowed or denied) emits a metadata-only receipt (hashes/counts/reason codes — never raw args/results/prompts/responses/SQL/tokens/URLs); no raw stores, arbitrary SQL, raw files/Obsidian, direct Graph/Procore, email/calendar/source-system writeback, or signed/download URLs; advisory only — no final determinations. **Operational proof:** an in-process MCP client↔server round trip (`tests/test_phase_08d_mcp_serve_operational.py`) plus an end-to-end subprocess over real stdio pipes — initialize, 9 tools / 5 resources / 5 prompts, allowed `hb_status` + receipt, denied tool + denial receipt, bounded resource/prompt payloads, no raw leakage, receipts persisted (1 allow + 1 deny). Fail-closed preserved: without the SDK or on a below-V37 DB, `serve` refuses (`served=false`). **Closeout (P15):** full matrix — `compileall src tests` exit 0; `ruff check .` clean; `mypy src` no issues across **272** files; `construction-agent validate` exit 0 (schema **V37**); `second-brain mcp status` `ready_to_serve=true` / `serve_blockers=[]`; `phase-08d-gates` **14 pass / 0 deferred** (`readiness_overstated=false`); `mcp no-raw-access` + `mcp no-writeback` `proof_passed=true`; 08A/08B/08C gates green with warnings preserved; pytest **3011 passed / 4 skipped / 0 failed**. Attaching the server to Claude Desktop is a one-time manual paste of the config preview. Handoffs are explicit — **09** (embeddings behind the retrieval broker + deferred chat-session memory; remote MCP transport + desktop fleet rollout remain out of scope). See `docs/evidence/construction-intelligence-phase-08d-mcp-bridge/` (`final-validation-closeout.md`, `mcp-operational-serve-proof.md`, and the per-prompt proofs) and `docs/architecture/106`–`119`.

**Phase 09 (Retrieval / Memory / Quality) — Preflight remediation (in progress).** Phase 09 will add semantic retrieval (LlamaIndex / embeddings / a vector index behind the existing Retrieval Broker) plus memory-quality / consolidation agents — but only after a preflight sequence (Prompts 01–11) closes the data-quality gaps left open at the end of Phase 08D. **Prompt 00 (repo-truth audit & rebaseline)** is **audit-only** — no runtime code, schema, or `pyproject` change, and **no LlamaIndex/embeddings/vector/semantic-retrieval code** is introduced (the explicit preflight boundary). It rebaselines current `main` at HEAD `23e6d87` (verified **2 commits ahead** of the Phase 08D target `a24f2a7`, behind 0 — the two commits are the 08D Prompt-15 operational closeout and a Procore live-sync runtime change), confirms schema **V37** / version **1.3.0** / an accurate README ledger, and re-runs the full validation matrix fresh: `compileall` exit 0; `ruff` clean; `mypy src` no issues across **272** files; `construction-agent validate` 4/4 (schema **V37**); `data-quality table-inventory` 171 contract / 167 live with **0 unmapped live tables**; legacy + 08A/08B/08C/08D gate and no-writeback / no-raw proofs all `proof_passed=true` / `ok=true` with `readiness_overstated=false`; pytest **3012 passed / 4 failed / 0 skipped** — the **4 failures are pre-existing Phase-08D SDK-presence test assertions** (the Prompt-13/14 SDK-absent assertions invert because Prompt 15 installed the optional `mcp 1.27.2` extra in this venv; not introduced by Phase 09 and not a safety stop-condition — the no-raw/no-writeback proofs pass and `ready_to_serve=true` is the documented SDK-present state). The audit **resolves gap G-09** (repo baseline emitted as safe literal values — branch / HEAD SHA / target equality / ahead-behind compare / repo-relative dirty paths) and **classifies G-01…G-08, G-10 as carried-forward Phase-09 blockers** (owned by preflight Prompts 03–11), with G-11 routed to Prompt 02. No Microsoft 365 / Procore / external writeback; no raw content, tokens, signed/download URLs, or PEMs; no semantic retrieval bypassing Research Packet / Evaluation; advisory only. No stop condition triggered. **Prompt 01 (08D closeout gap verification)** verifies Phase 08D's MCP runtime, receipts, gates, no-raw, no-writeback, config, and evidence against current repo truth (audit-only except a tests-only fix; schema stays **V37**, no retrieval code): `mcp status` `ready_to_serve=true` / `serve_blockers=[]` / SDK present (9 tools / 27 denied / 5 resources / 5 prompts); `phase-08d-gates` 14 pass with `readiness_overstated=false`; `mcp no-raw-access` + `mcp no-writeback` `proof_passed=true` (7 surfaces each); `config-preview` preview-only and safe; the 08D evidence bundle complete. It **resolves** the central 08D closeout gap — the four Prompt-13/14 tests that hard-asserted the SDK-absent posture (`ready_to_serve is False`) are made **SDK-state-aware** (branch on `find_spec("mcp")`, covering both branches), so the full suite is now **green (3016 passed / 0 failed / 0 skipped)** versus 3012/4 at Prompt 00. It **classifies** gap **G-02** — the operator DB has **0 MCP tool-call / 0 denial receipts** (substrate + operational serve proof verified; operator-DB receipt/denial smoke owned by `Prompt_04`). No Microsoft 365 / Procore / external writeback; no raw content/tokens/URLs/PEMs; no stop condition. **Prompt 02 (attached audit package gap verification)** re-measures every attached-audit gap (the Phase 08D agent data-quality evaluation packet) against live operator-DB/vault truth (read-only; no runtime/schema/test change; no retrieval code) and **resolves G-11**. G-01/02/05/06/07 confirmed empty (generated-output / MCP-receipt / memory / automation / Obsidian-linkage tables all 0); G-04 (currency 100% null, period 98.7% null, wbs/cost 3.4% null of 85,521 amount facts) and G-10 (corpus procore/financial-heavy) confirmed; G-08 relationship population present (1880 candidates / 1671 promoted) but no quality mart. **Key finding — G-03 reframed:** `second_brain_financial_review_required_items` is an **append-only per-run ledger** (run_id-tagged) — 101,490 rows across 112 routing runs collapse to **≈801 distinct review items** (guard columns all 0), so the attached audit's "66,466 review items" is largely a row-count artifact; the `phase-08c-gates` command appends ~1,299 ledger rows per run (a non-pruning local ledger, disclosed and routed to Prompt 05 + an 08C hygiene follow-up). **G-11 resolved:** 5 compact summary companions (e.g. 4.5 MB → 979 B) for the oversized field-profile/completeness files, each carrying the original's SHA-256 (originals unchanged). pytest **3016 passed / 0 failed**; all 08A/B/C/D gates + no-raw/no-writeback proofs pass, schema V37, 0 unmapped tables. G-01..G-08, G-10 carried to owning Prompts 03–11. No raw content/writeback; no stop condition. **Prompt 03 (generated output & research packet preflight)** resolves **G-01** (generated-output tables empty) by demonstrating the Phase 08A generation pipeline produces controlled, source-linked, confidence-labeled, **guard-clean** outputs **offline** — written to a **labeled proof-DB copy** of the operator DB (`run_daily_brief` mock/dry-run/no-vault + an `interactive_query` research packet): **820 rows** (2 research packets / 1 daily-brief run / 408 source refs / 408 handoff lines / 1 evaluation run), **all guard `CHECK(=0)` sums 0**, source-linked (`retrieval_receipt_id` / `research_packet_id` / `source_family`+`source_ref`), confidence/review-tier labeled, no-raw scan clean. The **operator DB and real Obsidian vault stay pristine** (operator generated tables remained 0; proof DB deleted after measurement). A reusable read-only `generated_output_proof.build_generated_output_population_proof` helper + 4 tests (normal / empty / no-raw fail-closed / stale-schema) are committed; `parser_outputs` classified out of scope (Phase 06A file-extraction). No CLI command, no schema, no retrieval code; mypy 273 files; pytest **3020 passed**; all 08A/B/C/D gates + no-raw/no-writeback proofs pass, schema V37, 0 unmapped. No stop condition. **Prompt 04 (MCP runtime receipt & denial smoke)** resolves **G-02** (V37 MCP receipt tables present but operator DB 0/0) by running an **in-process deny-first broker smoke** (`build_default_broker` — the nine real workflow wrappers; **no MCP SDK / serve / external call**) into a **labeled proof DB outside the repo**: `hb_status` (allowed) + `arbitrary_sql`/`graph_api_call`/`email_send`/`source_system_writeback`/`hb_delete_everything` (denied) → **1 allowed + 5 denied** metadata-only receipts, **both guard `CHECK(=0)` sums 0**, reason codes present (`action_denied_by_policy`, `tool_not_allowed`), receipts hashes/counts/enums only, structural no-raw/no-writeback pass. The **operator DB stays pristine** (before==after==0/0; proof DB deleted after measurement). A reusable read-only `mcp.receipt_smoke_proof.build_mcp_receipt_smoke_proof` helper + 5 tests (normal / missing-wrapper / unsafe-output fail-closed / empty / stale-schema) are committed. No CLI command, no schema, no retrieval code; mypy 274 files; pytest **3025 passed**; all 08A/B/C/D gates + `mcp no-raw-access`/`mcp no-writeback` proofs pass, schema V37, 0 unmapped. No stop condition. **Prompt 05 (review load & human-in-loop)** resolves **G-03** with a read-only **review-load triage mart** + a **fail-closed review-required promotion gate** (no new schema — derived read model; reuses the risk-digest impact classifier + 8 high-impact categories; reads the operator DB read-only, verified unmutated). It **reframes** the "66,466 review items" headline: `second_brain_financial_review_required_items` is an append-only per-run ledger (Prompt 02), so the mart counts **distinct** items — **111,213 raw rows → 2,733 distinct review items** (financial 109,284 raw → **804 distinct** across 118 runs; relationship-candidates 1,880; email 22; construction 26; document 1; memory 0), 1,254 high-impact, `review_not_performed=true`. The fail-closed gate **blocks all 2,733 from promotion** (`promotable=0`, `unresolved_high_impact_promotable=0`) — the "unresolved high-impact entering an approved source manifest" stop condition cannot be hit. A reusable `review_load_mart` helper (`build_review_load_mart` / `evaluate_review_promotion_gate` / `build_review_load_proof`), a read-only CLI `second-brain data-quality review-load --json`, and 5 tests are committed; impact classification is advisory (review routing, never a determination); review tier/confidence/source refs preserved (`review-tier-preservation-proof.json`). No schema, no retrieval code; mypy 275 files; pytest **3030 passed**; all 08A/B/C/D gates + no-raw/no-writeback proofs pass, schema V37, 0 unmapped. No stop condition. **Prompt 06 (financial data completeness)** remediates **G-04** advisorily with a read-only **financial-completeness mart** (no new schema; derived read model; reads the operator DB read-only, verified unmutated). Because the guardrails forbid final financial determinations, currency fallback / period enrichment / WBS-cost-code reconciliation are emitted as **advisory recommendations + review labels only** — never assignments, never routed to the review ledger, money never echoed. Live profile: `procore_financial_amount_facts` (85,521) currency **100% null**, period **98.7% null**, WBS/cost-code orphan-or-missing **2,887 / 3,510**; `second_brain_financial_amount_facts_normalized` empty. **Key finding:** no non-null currency exists anywhere, so a project-default currency is **not data-derivable** → surfaced as `project_default_currency_required` (3 projects; eligibility for an evidence-backed default = false), with period → `period_context_required` and WBS → `wbs_cost_code_context_required`. The proof attests `no_determination` (08C guard columns `financial_determination_performed`/`payment_decision_performed`/`claim_or_entitlement_decision_performed` = 0). A reusable `financial_completeness_advisory` helper, a read-only CLI `second-brain financial completeness-advisory --json`, and 5 tests are committed. No schema, no retrieval code; mypy 276 files; pytest **3035 passed**; all 08A/B/C/D gates + no-raw/no-writeback proofs pass, schema V37, 0 unmapped. No stop condition. **Prompt 08 (automation delivery receipt)** resolves **G-06** (automation/delivery receipt tables empty) by running an **in-process, no-external** controlled population (`run_daily_brief` mock + the 08B delivery/notification/HTML/open/health/launchd agents) into a **labeled proof DB outside the repo** — temp vault/HTML dirs, notification **explicitly gated** with an injected **fake notifier** (no real osascript; title stored as a hash), open **fail-closed disabled**, launchd **dry-run** (no plist/launchctl): **10 metadata-only receipts** (delivery 1 channel `obsidian_vault` / notification 1 channel `local_macos` / html 1 / agent-run 7), **all guard `CHECK(=0)` sums 0**, channels pinned, `external_writeback_performed` total 0. The **operator DB and the real vault/HTML/launchd stay pristine** (before==after==0; proof DB deleted after measurement). A reusable read-only `automation_delivery_proof.build_automation_delivery_proof` helper + 4 tests (controlled population / notification policy-off fail-closed / empty / stale-schema) are committed. No schema, no CLI, no retrieval code; mypy 277 files; pytest **3039 passed**; `phase-08b-gates` 16 pass and all 08A/C/D gates + no-raw/no-writeback proofs pass, schema V37, 0 unmapped. No stop condition. **Prompt 09 (approved Obsidian linkage)** resolves **G-07** (the approved-index `obsidian_index_entries` table is empty and the indexer counted `[[` wikilinks without validating them) by adding **canonical, redacted link metadata** to the existing approved indexer (additive `note_name_hash` + `link_target_hashes` in the already-`TEXT` `source_refs_json` — **no schema migration**; hashes of normalized note/target stems, never raw names/paths) plus a read-only **linkage proof with a broken-link check**. `build_obsidian_linkage_proof` (read-only `mode=ro`) verifies, for the latest manifest, that the `obsidian_index_manifests` guard `CHECK(=0)` columns sum to 0, canonical refs are preserved (`content_hash`/`section_marker`/`confidence_class`/`review_status` + `review_tier`/`approved_root_label`), **only approved-root notes are indexed** (an out-of-policy `approved_root_label` is a hard failure), and each wikilink is classified **resolved / broken / stale-unknown** (broken/stale are advisory source-coverage warnings, never a determination). Demonstrated on a **controlled proof DB** (throwaway fixture vault → the existing `build_index`): 2 entries / 1 excluded, guard sum 0, **3 links → 2 resolved / 1 broken**, raw-clean, `proof_passed=true`; the **operator DB stays pristine** (1 pre-existing dry-run manifest, **0 linked entries**, unchanged before==after). A reusable `obsidian_linkage_proof` helper (`build_obsidian_linkage_proof` + temp-dir `write_linkage_fixture_vault`), a read-only CLI `second-brain index linkage-proof --json`, and 7 tests (normal resolved+broken / empty / missing-policy fail-closed / stale-schema / unapproved-note hard-fail / no-raw fail-closed / broken-link advisory) are committed; no retrieval code. mypy **278** files; pytest **3046 passed**; `phase-08c-gates` deliberately skipped (its append-only ledger writes the operator DB — disclosed in Prompts 02/05, unrelated surface), all other 08A/B/D gates + no-raw/no-writeback proofs pass, schema V37, 0 unmapped. No stop condition. **Prompt 10 (relationship quality)** resolves **G-08** (the cross-source relationship substrate is populated — 1,880 candidates / 1,671 promoted / 1,880 evidence trails — but had **no quality mart**) with a read-only **relationship-quality mart** (no new schema; derived read model over the V25 `cross_source_relationship_candidates` / `cross_source_relationships` / `source_evidence_trails` tables; reads the operator DB read-only, verified unmutated). It reports **link ratios** (promotion rate **0.889**, review/determinism/human-promotion shares), the **confidence distribution** (candidates: deterministic **1,671** / strong_heuristic **51** / weak_heuristic **158**) and `confidence_score` spread, and **orphan / duplicate** counts — emitting quality signals + source-coverage warnings only, never a promotion or determination. **Key finding:** a naive "relationship has no evidence trail" check would flag all **1,671** promoted edges, but every one is fully traceable via `candidate_id → candidate.evidence_trail_id`, so the mart counts a true evidence-orphan only when a relationship reaches a trail by **neither** path (**0** live) and surfaces the missing *direct* link as **informational denormalization** (1,671), not a warning; the only advisory warning is **46 multi-edge near-duplicate pairs** (exact dups blocked by the UNIQUE edge constraint). The proof attests `no_determination` + guard-clean (the 8 `CHECK(=0)` guard columns on each table sum to 0) + raw-clean. A reusable `relationship_quality_mart` helper (`build_relationship_quality_mart` / `build_relationship_quality_proof`), a read-only CLI `second-brain data-quality relationship-quality --json`, and 5 tests are committed; no retrieval code. mypy **279** files; pytest **3051 passed**; `phase-08c-gates` deliberately skipped (append-only ledger writes the operator DB — disclosed Prompts 02/05, unrelated surface), all other 08A/B + no-raw/no-writeback proofs pass, schema V37, 0 unmapped. No stop condition. **Prompt 11 (corpus balance & source coverage)** resolves **G-10** (Prompt 02 classified the corpus `confirmed_imbalance` — procore/financial-heavy at raw ingestion) with a read-only **corpus-balance gate + source-family coverage proof** (no new schema; derived read model over the **retrieval corpus** — the `ALLOWLISTED_SOURCE_FAMILIES` the broker may read, reusing `retrieval/policy.py` + `retrieval/readers.py`; reads the operator DB read-only, verified unmutated) plus a committed threshold policy seed (`resources/config/phase_09_corpus_balance_policy.seed.yaml`). **Key finding:** measured at the *retrieval-corpus* level the imbalance is more precise than the raw-ingestion claim — **5 of 7** reader-backed families are covered and well-distributed (evidence-trails **1,880** / aging **1,780** / relationships **1,671** / issue-history **598** / risk **44**; dominant share **0.31** ≤ 0.6), and the real gap is the **2 empty families** `approved_obsidian_generated_outputs` + `accepted_long_term_memory` (owned by sibling Prompts 09/07). The fail-closed gate (policy requires all 7 reader-backed families covered) returns the honest **`imbalanced`** verdict (`too_few_covered_families:5<7`); the proof attests `no_determination` + guard-clean + raw-clean and `proof_passed=true` **independently** of the balance verdict (it validly measures the imbalance). A reusable `corpus_balance_mart` helper (`build_corpus_balance_mart` / `evaluate_corpus_balance_gate` / `build_corpus_balance_proof` + `load_corpus_balance_policy`), a read-only CLI `second-brain data-quality corpus-balance --json`, the policy seed, and 5 tests are committed; no retrieval code. mypy **280** files; pytest **3056 passed**; `phase-08c-gates` deliberately skipped (append-only ledger writes the operator DB — disclosed Prompts 02/05, unrelated surface), all other 08A/B + no-raw/no-writeback proofs pass, schema V37, 0 unmapped. No stop condition. **Prompt 12 (schema contracts & table lifecycle)** begins the additive build: it lands the **V38** migration adding **nineteen** metadata-only retrieval/memory/agent substrate tables (`second_brain_retrieval_*` ×14, `second_brain_memory_*` ×3, `second_brain_agent_performance_feedback_runs`, `second_brain_phase_09_validation_runs`), each carrying the **full twenty-three guard `CHECK(=0)` columns** (the twenty no-raw/no-writeback/no-direct-api/no-determination guards plus the three Phase 09 guards `unsupported_claim_performed` / `raw_vector_content_persisted` / `semantic_retrieval_bypassed_policy`) and **shipping empty** — **still no LlamaIndex/embeddings/vector/semantic-retrieval code** (the substrate is populated by later Phase 09 prompts 13–39). The tables are registered in the lifecycle contract (`table_lifecycle_status_contract.json` **171→190**, all nineteen `placeholder_deferred` / `phase_owner 09` / `V38`, **0 unmapped**) and a new richer **`phase_09_table_lifecycle_contract.json`** maps each table to its Phase 09 lifecycle state + owning prompt. A read-only **`second-brain data-quality phase-09-schema-status --json`** CLI + `phase_09_schema` helper (`build_phase_09_schema_status_report` / `load_phase_09_lifecycle_contract`, fail-closed) verify schema head **V38**, all nineteen tables present with all twenty-three guards, every table empty, and the contract loaded — `overall_status=ready`; the helper opens the DB `mode=ro` and never mutates it. The migration is additive (V1–V37 untouched, idempotent); the operator DB advanced schema **37→38** adding the nineteen empty tables with **0 data rows**. 13 new tests (V38 migration: tables/guards/CHECK-rejects-nonzero/idempotency/metadata-only/lifecycle-classification; status: ready/stale-schema/missing-contract-fail-closed/CLI-exit-codes/no-mutation); the twelve existing `contract_table_count` asserts bumped 171→190. compileall/ruff clean; mypy **281** files; pytest **3069 passed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 contract / 0 unmapped; `phase-09-schema-status` ready; `phase-08c-gates` deliberately skipped (append-only ledger writes the operator DB — disclosed Prompts 02/05), all other 08A/B/D gates + no-raw/no-writeback proofs pass. No stop condition. **Prompt 13 (LlamaIndex dependency & config surface)** declares the **optional** retrieval dependency and a lazy-import config/status surface — **still no embeddings / vector index / semantic-retrieval runtime**. New `pyproject` extras `retrieval = ["llama-index-core>=0.12"]` + `retrieval-local` (adds `llama-index-embeddings-huggingface`), declared but **not installed** (the base install + full suite run with LlamaIndex absent — the local-first default). A JSON contract (`phase_09_llamaindex_config_contract.json`: allowed providers `local`/`mock`, index `vector_store`, vector store `simple`, deferred external providers, no-raw/no-writeback/lazy-import posture) + a metadata-only YAML seed (`phase_09_llamaindex_config.seed.yaml`: local embedding label, chunk params, a `persist_dir_label` **label** — never a path) + a `PHASE_09_CONTRACT_FILES` loader. A read-only helper `retrieval/llamaindex_config.py` (`build_llamaindex_config_status`, fail-closed; SDK probed via import-free `find_spec("llama_index")`; stable `config_hash`; opens the DB `mode=ro`, persists nothing) and a new two-level CLI **`second-brain retrieval llamaindex status --json`**. Live (SDK absent — expected): `sdk.available=false`, `config_valid=true`, `schema_ready=true`, `ready_to_index=false`, blockers `["llama_index_not_installed"]`, exit 0; operator `…_llamaindex_config_snapshots` stays **0 rows** (status writes nothing). 11 new tests (normal / missing-contract / missing-seed fail-closed / stale-schema / unsafe-deferred-provider / metadata-only / no-mutation / SDK-state-aware / CLI exit 0·3 / contract-failure). compileall/ruff clean; mypy **282** files; pytest **3080 passed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped (append-only ledger writes the operator DB — disclosed Prompts 02/05), all other 08A/B/D gates + no-raw/no-writeback proofs pass. No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/` (`00`–`06-*` + `08-*`–`13-*` evidence + `.json`, `02-live-gap-measurement.json`, `source-linked-retrieval-proof.json`, `review-tier-preservation-proof.json`, `obsidian-linkage-proof.json`, `broken-link-classification.json`, `relationship-quality-mart.json`, `corpus-balance-mart.json`, `attached-audit-summary-companions/`, `validation-outputs*/`), `docs/runbooks/phase-09-retrieval-llamaindex-setup.md`, and `docs/architecture/120`–`132`. **Prompt 14 (embedding & vector-store policy)** implements the embedding/vector-store **policy** and **no-raw guardrails** that the index build (Prompts 18–19) and approved manifest (Prompt 15) will enforce — **still no embeddings computed and no index built**. A JSON contract (`phase_09_embedding_vector_policy_contract.json`: allowed providers `local`/`mock`, deferred external providers, vector store `simple`, embedding-dim bounds, required + forbidden node fields, persistence rules — **vectors never persisted to SQLite**) + a metadata-only YAML seed (`phase_09_embedding_vector_policy.seed.yaml`: the 7-family embeddable allowlist = the policy maximum, `persist_dir_label` **label**) + a `PHASE_09_CONTRACT_FILES` entry. A new `retrieval/embedding_policy.py` provides the fail-closed no-raw guardrail `validate_embedding_candidate` (rejects EXCLUDED/non-embeddable families, missing metadata, forbidden raw fields, raw/secret/URL shapes via the shared `_FORBIDDEN` scanner, vector blobs, unresolved review), a read-only `build_embedding_vector_policy_status` (`mode=ro`), and a fail-closed `build_no_raw_vector_policy_proof`; two new CLI surfaces **`second-brain retrieval embedding-policy status|no-raw-proof --json`**. Live: status `config_valid=true`/`schema_ready=true`/7 embeddable families/governed tables **0 rows**; `no-raw-proof` `proof_passed=true` over 9 cases (safe passes, 8 planted-unsafe rejected); operator DB pristine (vectors never enter SQLite). 11 new tests (normal / missing-contract / missing-seed fail-closed / stale-schema / unsafe-candidates / no-raw-proof clean / proof-artifact / metadata-only / no-mutation / CLI exit codes). compileall/ruff clean; mypy **283** files; pytest **3091 passed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped (append-only ledger — disclosed Prompts 02/05), all other 08A/B/D gates + no-raw/no-writeback proofs pass. No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/` (`00`–`06-*` + `08-*`–`14-*` evidence + `.json`, `embedding-vector-policy-no-raw-proof.{json,md}`, `source-linked-retrieval-proof.json`, `review-tier-preservation-proof.json`, `obsidian-linkage-proof.json`, `broken-link-classification.json`, `relationship-quality-mart.json`, `corpus-balance-mart.json`, `attached-audit-summary-companions/`, `validation-outputs*/`), `docs/runbooks/phase-09-retrieval-llamaindex-setup.md`, and `docs/architecture/120`–`133`. **Prompt 15 (approved index source manifests)** builds the **approved source manifest** that enumerates which records from three categories — generated outputs (`second_brain_research_packets` accepted), approved Obsidian outputs (latest **apply-mode** index entries), reviewed memory (`accepted_long_term_memory`) — are eligible for indexing; it is the gate the index build (Prompts 18–19) reads. **Still no embeddings computed and no index built.** A JSON contract (`phase_09_approved_source_manifest_contract.json`: category→family mapping, approved vs excluded review statuses, `max_auto_approval_review_tier`, required/forbidden entry metadata, fail-closed posture) + a metadata-only YAML seed (`phase_09_approved_source_manifest.seed.yaml`) + a `PHASE_09_CONTRACT_FILES` entry. A new `retrieval/source_manifest.py` provides the fail-closed approval/no-raw guardrail `validate_manifest_entry` (excludes EXCLUDED families, non-accepted statuses, **unresolved high-impact** tier-3/`review_required`, missing metadata, forbidden fields, raw/secret/URL shapes via the shared `_FORBIDDEN` scanner), a read-only `build_approved_source_manifest` (`mode=ro`; **metadata-only** — per-family counts + a deterministic `manifest_hash`, no per-entry refs persisted), a guard-clean single-row `persist_approved_source_manifest`, and a fail-closed `build_approved_source_manifest_proof`; a new two-level CLI **`second-brain retrieval approved-sources build|proof --json`** (build dry-run by default). Live: operator build status **`empty`** (sources empty → honest source-coverage warnings); `proof` `proof_passed=true` over 10 cases (3 safe approved, 7 planted-unsafe excluded); a proof-DB test with one accepted memory yields `approved_ref_count=1` + a guard-clean persisted summary row; operator `…_approved_source_manifests` stays **0 rows** (no `--apply`). 10 new tests (normal+persist / missing-contract / missing-seed fail-closed / stale-schema fail-closed / operator-empty-honest / unsafe-candidates-excluded / proof clean / proof-artifact / no-mutation / CLI exit codes). compileall/ruff clean; mypy **284** files; pytest **3101 passed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped (append-only ledger — disclosed Prompts 02/05), all other 08A/B/D gates + no-raw/no-writeback proofs pass. No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/` (`00`–`06-*` + `08-*`–`15-*` evidence + `.json`, `approved-source-manifest-proof.{json,md}`, `embedding-vector-policy-no-raw-proof.{json,md}`, `source-linked-retrieval-proof.json`, `review-tier-preservation-proof.json`, `obsidian-linkage-proof.json`, `broken-link-classification.json`, `relationship-quality-mart.json`, `corpus-balance-mart.json`, `attached-audit-summary-companions/`, `validation-outputs*/`), `docs/runbooks/phase-09-retrieval-llamaindex-setup.md`, and `docs/architecture/120`–`134`. **Prompt 16 (approved Obsidian output loader)** adds the first per-category **node-preparation loader** (architecture layer 2): load **only approved, source-linked generated Obsidian notes** — the entries of the latest **`mode='apply'`** index manifest (dry-run/unapproved manifests are never loaded) — into safe metadata-only nodes for the future embed step. **Still no embeddings computed and no index built.** A new `retrieval/obsidian_loader.py` provides `load_approved_obsidian_nodes` (read-only `mode=ro`; strict `WHERE mode='apply'` gate — not the dry-run-fallback helper; validates every node via Prompt 14's `validate_embedding_candidate` — embeddable family + source-linked metadata + no-raw + tier-3/unresolved exclusion), a **metadata-only** `build_obsidian_loader_report` (counts + per-node hashes; the redacted heading rides only on in-memory nodes, never echoed), and a fail-closed `build_obsidian_loader_proof` (apply-mode fixture loads nodes; dry-run-only loads 0; guardrail rejects tier-3/raw/non-embeddable); a new two-level CLI **`second-brain retrieval obsidian-loader status|proof --json`**. Live: operator loader status **`empty`** (0 apply manifests → honest `no_approved_obsidian_notes` warning); `proof` `proof_passed=true` (apply=2, dry_run=0, all candidate cases excluded); operator obsidian tables unchanged (read-only). 9 new tests (normal apply load + metadata-only report / missing-policy / stale-schema fail-closed / dry-run-only loads 0 / guardrail excludes unsafe / proof clean / proof-artifact / no-mutation / CLI exit codes). compileall/ruff clean; mypy **285** files; pytest **3110 passed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped (append-only ledger — disclosed Prompts 02/05), all other 08A/B/D gates + no-raw/no-writeback proofs pass. No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/` (`00`–`06-*` + `08-*`–`16-*` evidence + `.json`, `approved-obsidian-loader-proof.{json,md}`, `approved-source-manifest-proof.{json,md}`, `embedding-vector-policy-no-raw-proof.{json,md}`, `source-linked-retrieval-proof.json`, `review-tier-preservation-proof.json`, `obsidian-linkage-proof.json`, `broken-link-classification.json`, `relationship-quality-mart.json`, `corpus-balance-mart.json`, `attached-audit-summary-companions/`, `validation-outputs*/`), `docs/runbooks/phase-09-retrieval-llamaindex-setup.md`, and `docs/architecture/120`–`135`. **Prompt 17 (reviewed memory loader)** completes the per-category **node-preparation loaders** (sibling of Prompt 16): load **only reviewed (accepted) long-term memory** — `long_term_memory_items` with `review_status='accepted'` (pending/rejected/superseded are never loaded) — into safe metadata-only nodes for the future embed step. **Still no embeddings computed and no index built.** A new `retrieval/memory_loader.py` provides `load_reviewed_memory_nodes` (read-only `mode=ro`; strict `WHERE review_status='accepted'` gate; validates every node via Prompt 14's `validate_embedding_candidate`; reports `source_ref_count`), a **metadata-only** `build_reviewed_memory_loader_report` (counts + per-node hashes; the redacted statement rides only on in-memory nodes, never echoed; `no_reviewed_memory` / advisory `unsourced_memory` warnings), and a fail-closed `build_reviewed_memory_loader_proof` (accepted fixture loads nodes; pending-only loads 0; guardrail rejects non-embeddable/raw/missing/unresolved); a new two-level CLI **`second-brain retrieval memory-loader status|proof --json`**. Live: operator loader status **`empty`** (0 accepted memory → honest `no_reviewed_memory` warning); `proof` `proof_passed=true` (accepted=1, pending=0, all candidate cases excluded); operator memory tables unchanged (read-only). 9 new tests (normal accepted load + metadata-only report / missing-policy / stale-schema fail-closed / pending-only loads 0 / guardrail excludes unsafe / proof clean / proof-artifact / no-mutation / CLI exit codes). compileall/ruff clean; mypy **286** files; pytest **3119 passed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped (append-only ledger — disclosed Prompts 02/05), all other 08A/B/D gates + no-raw/no-writeback proofs pass. No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/` (`00`–`06-*` + `08-*`–`17-*` evidence + `.json`, `reviewed-memory-loader-proof.{json,md}`, `approved-obsidian-loader-proof.{json,md}`, `approved-source-manifest-proof.{json,md}`, `embedding-vector-policy-no-raw-proof.{json,md}`, `source-linked-retrieval-proof.json`, `review-tier-preservation-proof.json`, `obsidian-linkage-proof.json`, `broken-link-classification.json`, `relationship-quality-mart.json`, `corpus-balance-mart.json`, `attached-audit-summary-companions/`, `validation-outputs*/`), `docs/runbooks/phase-09-retrieval-llamaindex-setup.md`, and `docs/architecture/120`–`136`. **Prompt 18 (vector index build dry run)** implements the **dry-run vector build** — a metadata-only **plan** over the approved manifest's loader nodes (Obsidian + reviewed memory) of what *would* be embedded/indexed, **computing no embeddings and writing no vector store**. The **approved manifest is the only input** and the build **rejects any node lacking review tier, confidence, source ref, freshness, or no-raw proof** (`_apply_build_rule` = the four required fields + Prompt 14's `validate_embedding_candidate`). A new `retrieval/vector_index.py` provides `build_vector_index_dry_run` (read-only `mode=ro`; per-family node counts, planned chunk count, `config_hash`/`index_plan_hash`, `ready_to_apply`, `vectors_persisted_to_sqlite: false`, `status='dry_run'`; persists nothing), a guard-clean single-row `persist_dry_run_record` (`status='dry_run'`; proof/test only), and a fail-closed `build_vector_index_dry_run_proof`; a new CLI **`second-brain retrieval llamaindex build [--dry-run] | build-proof --json`** (`--apply` is fail-closed `apply_not_enabled`, **deferred to Prompt 19**). Live: operator `build` status **`dry_run`** with `total_nodes=0` (0 approved sources → honest `no_approved_nodes` + `generated_outputs_loader_deferred` warnings); `build-proof` `proof_passed=true` (controlled fixture plans 3 nodes, the build rule rejects 6 planted-unsafe nodes, a guard-clean `dry_run` `vector_index_runs` row persists in the proof DB); operator `vector_index_runs`/`vector_index_items` stay **0 rows**. 8 new tests (normal dry-run + persist / missing-policy / stale-schema fail-closed / build-rule rejects unsafe / no-mutation / proof clean / proof-artifact / CLI apply-deferred + build-proof). compileall/ruff clean; mypy **287** files; pytest **3127 passed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped (append-only ledger — disclosed Prompts 02/05), all other 08A/B/D gates + no-raw/no-writeback proofs pass. No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/` (`00`–`06-*` + `08-*`–`18-*` evidence + `.json`, `vector-index-dry-run-proof.{json,md}`, `reviewed-memory-loader-proof.{json,md}`, `approved-obsidian-loader-proof.{json,md}`, `approved-source-manifest-proof.{json,md}`, `embedding-vector-policy-no-raw-proof.{json,md}`, `source-linked-retrieval-proof.json`, `review-tier-preservation-proof.json`, `obsidian-linkage-proof.json`, `broken-link-classification.json`, `relationship-quality-mart.json`, `corpus-balance-mart.json`, `attached-audit-summary-companions/`, `validation-outputs*/`), `docs/runbooks/phase-09-retrieval-llamaindex-setup.md`, and `docs/architecture/120`–`137`. **Prompt 19 (vector index build apply)** implements the **policy-gated apply build** — it embeds the approved manifest's loader nodes (Obsidian + reviewed memory) via LlamaIndex and writes a vector store **on the local filesystem under Application Support (never to SQLite)**, persisting **metadata-only receipts**: a `status='applied'` `vector_index_runs` row + one `vector_index_items` row per node (`source_ref` **hashed**, `content_hash`/`confidence_class`/`freshness_label`/`chunk_count` — no `text`/`embedding`/`vector`/`persist_dir`). The build re-asserts the build rule on every node before embedding and the **approved manifest is the only input**. The embed/vector-store writer is **injectable**: the default is `VectorStoreIndex` + `SimpleVectorStore` + `HuggingFaceEmbedding(BAAI/bge-small-en-v1.5)`; the proof/tests inject a `MockEmbedding` writer (same LlamaIndex pipeline) so the default-safe suite runs **fully offline**. Apply **fails closed** (`status='apply_blocked'`, persisting nothing) for `sdk_not_available` / `no_indexable_nodes` / `schema_not_ready` / `policy_unavailable` — so on the operator DB (0 approved sources) it honestly blocks with `no_indexable_nodes` and leaves the store **data-pristine** (`vector_index_runs`/`vector_index_items` stay **0 rows**, schema **V38**). The optional **`.[retrieval-local]`** extra (`llama-index-core` + `llama-index-embeddings-huggingface`) is installed to enable real local embedding (`pyproject` unchanged — extras declared Prompt 13). A new `retrieval/vector_index.py` surface provides `build_vector_index_apply` / `persist_apply_record` / `build_vector_index_apply_proof` (+ the `VectorStoreWriter` port and the apply contract/seed loaders); a new contract `phase_09_vector_index_apply_contract.json` + seed `phase_09_vector_index_apply.seed.yaml` (persisted-column allowlists, forbidden-persisted fields, status/blocker vocabulary, `external_filesystem` requirement); CLI **`second-brain retrieval llamaindex build --apply | build-apply-proof --json`**. Live: `build --apply` `apply_blocked: no_indexable_nodes` (exit 3, nothing persisted); `build-apply-proof` `proof_passed=true` (3 items, dim **384**, vectors written outside SQLite, guard-clean run + item rows over all **23** `CHECK(=0)` guards, blocked-no-nodes path); a real `bge-small` embed smoke applied 3 items and persisted the `SimpleVectorStore` JSON to disk. 9 new tests (normal apply / missing-policy / stale-schema fail-closed / unsafe-source blocks / no-raw+no-writeback+SDK-absent gate / apply-proof clean / proof-artifact / CLI proof + an `integration` real-HF embed smoke); an incidental one-line fix made the Prompt-12 `test_helper_does_not_mutate_db` assert row counts instead of flaky file size. compileall/ruff clean; mypy **287** files; pytest **3135 passed / 0 failed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped (append-only ledger — disclosed Prompts 02/05), all other 08A/B/D gates + no-raw/no-writeback proofs pass. No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/` (`00`–`06-*` + `08-*`–`19-*` evidence + `.json`, `vector-index-apply-proof.{json,md}`, `vector-index-dry-run-proof.{json,md}`, `reviewed-memory-loader-proof.{json,md}`, `approved-obsidian-loader-proof.{json,md}`, `approved-source-manifest-proof.{json,md}`, `embedding-vector-policy-no-raw-proof.{json,md}`, `source-linked-retrieval-proof.json`, `review-tier-preservation-proof.json`, `obsidian-linkage-proof.json`, `broken-link-classification.json`, `relationship-quality-mart.json`, `corpus-balance-mart.json`, `attached-audit-summary-companions/`, `validation-outputs*/`), `docs/runbooks/phase-09-retrieval-llamaindex-setup.md`, and `docs/architecture/120`–`138`. **Prompt 20 (hybrid retrieval broker)** combines the **deterministic** Retrieval Broker (A03 — the source of truth over the allowlisted families) with an **advisory semantic** path that queries the applied vector index, merged into one source-linked, guard-clean context re-bounded by the deterministic budget. **Source-of-truth discipline:** deterministic results are authoritative; semantic results are advisory "suggested context" only — source-linked back to the persisted `vector_index_items` receipts, **floored at review tier 2** (never auto-tier-1), review tier / confidence / freshness preserved, and re-validated with Prompt 14's `validate_embedding_candidate` before admission. The broker returns a merged context, **never a final answer** (`assembles_final_answer=false`; answer assembly stays in the Research Packet / Evaluation layers), so the `semantic_retrieval_bypassed_policy` guard stays **0**. The semantic read path loads the latest **applied** `SimpleVectorStore` from disk (`load_index_from_storage` + `as_retriever.retrieve`) with an **injectable embedder** (default `HuggingFaceEmbedding(bge-small)`; proofs/tests inject `MockEmbedding` — fully offline) and **fails closed** (semantic skipped, deterministic still returned) on `semantic_sdk_not_available` / `semantic_no_applied_index` / `semantic_disabled_mode`; the whole surface is V38-gated. A new `retrieval/hybrid_broker.py` (`build_hybrid_retrieval` / `build_hybrid_status` / `persist_hybrid_query_record` / `build_hybrid_retrieval_proof`) + contract `phase_09_hybrid_retrieval_contract.json` + seed `phase_09_hybrid_retrieval.seed.yaml`; CLI **`second-brain retrieval hybrid status | search "<q>" [--mode hybrid|deterministic-only] | proof --json`**. `hybrid search` is **read-only** — it emits a metadata-only summary (counts, per-family + origin split, tier distribution, score buckets, warnings) and **persists nothing**; the raw query is **never persisted** (only `query_hash`); vector/semantic search is **not** exposed via MCP. Live: `hybrid status` deterministic-ready, semantic-blocked `semantic_no_applied_index` on the operator DB; `hybrid search` returns deterministic results with an empty semantic set (no persist); `hybrid proof` `proof_passed=true` (3 deterministic + 3 advisory semantic merged, guard-clean run + result rows over all **23** `CHECK(=0)` guards, raw query not persisted, no-applied-index / deterministic-only / unsafe-node paths exercised); a real `bge-small` hybrid query produced **high/medium** score buckets. Operator DB pristine (`hybrid_query_runs`/`hybrid_query_results` **0 rows**, schema **V38**); hybrid broker adoption by A02/A04 is **wiring-ready but deferred**. 8 new tests (normal merge / missing-policy / stale-schema fail-closed / unsafe-source dropped / no-raw+no-writeback+SDK-absent gate / proof clean / proof-artifact + an `integration` real-HF hybrid smoke). compileall/ruff clean; mypy **288** files; pytest **3142 passed / 0 failed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped (append-only ledger — disclosed Prompts 02/05), all other 08A/B/D gates + no-raw/no-writeback proofs pass. No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/` (`00`–`06-*` + `08-*`–`20-*` evidence + `.json`, `hybrid-retrieval-proof.{json,md}`, `vector-index-apply-proof.{json,md}`, `vector-index-dry-run-proof.{json,md}`, `reviewed-memory-loader-proof.{json,md}`, `approved-obsidian-loader-proof.{json,md}`, `approved-source-manifest-proof.{json,md}`, `embedding-vector-policy-no-raw-proof.{json,md}`, `source-linked-retrieval-proof.json`, `review-tier-preservation-proof.json`, `obsidian-linkage-proof.json`, `broken-link-classification.json`, `relationship-quality-mart.json`, `corpus-balance-mart.json`, `attached-audit-summary-companions/`, `validation-outputs*/`), `docs/runbooks/phase-09-retrieval-llamaindex-setup.md`, and `docs/architecture/120`–`139`. **Prompt 21 (metadata filter enforcement)** adds a fail-closed **project / source / date / review / confidence / source-coverage** filter enforced **before** retrieval (constrain which allowlisted families/sources are queried; an explicitly requested **excluded** family fails closed) and **after** retrieval (drop merged items outside the requested project / family / date window / review-tier ceiling / confidence floor — recorded by reason — and emit source-coverage warnings). A new `retrieval/metadata_filter.py` provides the `MetadataFilter` spec, `normalize_filter` (pre) and `apply_metadata_filter` (post); it integrates into `build_hybrid_retrieval` via an optional `metadata_filter` param (pre sets the deterministic families/project; post runs on the merged items before `apply_context_budget`; the summary gains `filter_applied` + `filter_summary`). **Date filtering is family-aware** — only the six families whose `recency` is a parseable ISO date get a date window; `cross_source_relationships` and semantic items are kept with a `date_filter_not_applicable` warning, never silently dropped (confidence order `deterministic > high > medium > low > unknown`). **Source-of-truth discipline preserved:** review tier / confidence class / source references / freshness / coverage warnings ride through on kept items; no raw content is read or emitted (the raw query is never persisted — only `query_hash`); `assembles_final_answer=false`; **read-only, persists nothing, no new table** (schema stays **V38**). A new contract `phase_09_metadata_filter_contract.json` + seed `phase_09_metadata_filter.seed.yaml`; CLI **`second-brain retrieval metadata-filter status | apply "<q>" [--source a,b] [--date-from] [--date-to] [--max-review-tier] [--min-confidence] [--require-coverage] | proof --json`**. Live: `metadata-filter apply` on the operator DB → `status=ok`, `filter_applied=true`, `result_count=0`, `dropped_by_reason={review_tier_above_max:500}` (the operator issue items are tier 3, capped at 2 — correct enforcement), no persist; `metadata-filter proof` `proof_passed=true` (excluded family rejected pre-filter; post-filter drop matrix project/family/date/tier/confidence with reasons; date-incapable family kept with a coverage note; hybrid integration; raw query not emitted); a real `bge-small` filtered hybrid query enforced the tier ceiling (3 tier-1 + 3 tier-2, 0 tier-3). Operator DB pristine (`hybrid_query_*` **0 rows**, schema **V38**). 10 new tests (pre+post filter / hybrid integration / missing-policy / stale-schema fail-closed / unsafe-source excluded-family rejected / no-raw+no-writeback / date-window + date-incapable / min-confidence floor / proof clean / proof-artifact + an `integration` real-HF filtered smoke). compileall/ruff clean; mypy **289** files; pytest **3152 passed / 0 failed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped (append-only ledger — disclosed Prompts 02/05), all other 08A/B/D gates + no-raw/no-writeback proofs pass. No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/` (`00`–`06-*` + `08-*`–`21-*` evidence + `.json`, `metadata-filter-proof.{json,md}`, `hybrid-retrieval-proof.{json,md}`, `vector-index-apply-proof.{json,md}`, `vector-index-dry-run-proof.{json,md}`, `reviewed-memory-loader-proof.{json,md}`, `approved-obsidian-loader-proof.{json,md}`, `approved-source-manifest-proof.{json,md}`, `embedding-vector-policy-no-raw-proof.{json,md}`, `source-linked-retrieval-proof.json`, `review-tier-preservation-proof.json`, `obsidian-linkage-proof.json`, `broken-link-classification.json`, `relationship-quality-mart.json`, `corpus-balance-mart.json`, `attached-audit-summary-companions/`, `validation-outputs*/`), `docs/runbooks/phase-09-retrieval-llamaindex-setup.md`, and `docs/architecture/120`–`140`. **Prompt 22 (research packet integration)** establishes the sanctioned (and only) route for **semantic** (vector) retrieval context to enter answer generation: it builds the hybrid broker's merged `RetrievalEnvelope` (deterministic authoritative + advisory semantic) and routes it through `build_research_packet_from_envelope` (A02), producing a metadata-only `ResearchPacket` (`advisory_classification='advisory'`) — **never a final answer** (`route='research_packet_only'`, `synthesis_performed=false`, `assembles_final_answer=false`). To expose the merged items, `build_hybrid_retrieval` is refactored over a shared `_collect_hybrid` (output unchanged — Prompt 20/21 tests pass) and a new `build_hybrid_envelope` returns the envelope; a new `research/semantic_packet.py` (`build_semantic_research_packet` / `build_semantic_research_packet_proof`) is the bridge. **No semantic→answer bypass:** `synthesize_answer` (A04) is untouched (still deterministic-only) and the proof asserts the synthesis agent has **no** hybrid-broker reference — so semantic context cannot reach an answer except through the Research Packet / Evaluation layers. Review tier / confidence / source refs / freshness / coverage warnings are preserved (semantic advisory + tier-floored at 2); no raw content is read or emitted (the raw query is never emitted — only `query_hash`); **read-only by default** (`emit_receipt=False` persists nothing; the persisted packet receipt is metadata-only + guard-clean). Reuses the existing `second_brain_research_packets` table — **no migrator/schema change** (schema stays **V38**). A new contract `phase_09_research_packet_integration_contract.json` + seed `phase_09_research_packet_integration.seed.yaml`; CLI **`second-brain retrieval research-packet build "<q>" [--source a,b] [--max-review-tier] [--min-confidence] [--mode] | proof --json`** (distinct from the existing 08A `second-brain research-packet` command). Live: `research-packet build` on the operator DB → `route='research_packet_only'`, `synthesis_performed=false`, 408 deterministic / 0 semantic (no applied index → semantic skipped), no persist; `research-packet proof` `proof_passed=true` (semantic context in an advisory packet; returns a packet not an answer; guard-clean metadata-only persisted receipt; no semantic bypass; excluded family fail-closed; raw query not emitted); a real `bge-small` smoke routed 3 advisory semantic items into a tier-2 advisory packet without synthesizing. Operator DB pristine (`second_brain_research_packets` unchanged, schema **V38**); default-synthesis adoption deferred. 7 new tests (normal route / missing-policy / stale-schema fail-closed / unsafe-source excluded-family / no-raw+no-writeback+no-bypass / proof clean / proof-artifact + an `integration` real-HF smoke). compileall/ruff clean; mypy **290** files; pytest **3159 passed / 0 failed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped (append-only ledger — disclosed Prompts 02/05), all other 08A/B/D gates + no-raw/no-writeback proofs pass. No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/` (`00`–`06-*` + `08-*`–`22-*` evidence + `.json`, `research-packet-integration-proof.{json,md}`, `metadata-filter-proof.{json,md}`, `hybrid-retrieval-proof.{json,md}`, `vector-index-apply-proof.{json,md}`, `vector-index-dry-run-proof.{json,md}`, `reviewed-memory-loader-proof.{json,md}`, `approved-obsidian-loader-proof.{json,md}`, `approved-source-manifest-proof.{json,md}`, `embedding-vector-policy-no-raw-proof.{json,md}`, `source-linked-retrieval-proof.json`, `review-tier-preservation-proof.json`, `obsidian-linkage-proof.json`, `broken-link-classification.json`, `relationship-quality-mart.json`, `corpus-balance-mart.json`, `attached-audit-summary-companions/`, `validation-outputs*/`), `docs/runbooks/phase-09-retrieval-llamaindex-setup.md`, and `docs/architecture/120`–`141`. **Prompt 23 (output evaluation integration)** completes the doctrine boundary by routing **semantic retrieval outputs** through the **Output Evaluation (A05) layer + an unsupported-claim check + a source-linked proof + metadata-only receipts**, fail-closed. A new `synthesis/semantic_output_evaluation.py` builds the hybrid envelope → research packet → a **non-synthesized** context `AdapterResult` (`answer=""`, `synthesized=False`) → the **real** `build_evaluation_preview` (the A05 10-item checklist), then runs an unsupported-claim check + source-linked proof over the retrieved items. The context is evaluated for fitness but **no answer is synthesized** (`route='evaluation_only'`, `synthesis_performed=false`, `assembles_final_answer=false`). A retrieved item is a **supported, source-linked claim** iff it carries a `source_ref` + an allowlisted (non-excluded) `source_family`; any unsupported/unlinked item **fails closed** (zero tolerance), and the `unsupported_claim_performed` guard stays **0** (detect-and-block, never emit). `overall_passed = evaluation.passed AND unsupported==0 AND unlinked==0`. Metadata-only receipts persist to the existing V38 `second_brain_retrieval_source_linked_proof_runs` + `second_brain_retrieval_unsupported_claim_checks` tables (all 23 `CHECK(=0)` guards 0) — **read-only by default** (`emit_receipt=False` persists nothing; proven on a temp DB). Review tier / confidence / source refs / freshness / coverage warnings are preserved; no raw content is read or emitted (the raw query is never emitted — only `query_hash`; no answer/excerpt). **No migrator/schema change** (schema stays **V38**); no A04 rewiring. A new contract `phase_09_output_evaluation_integration_contract.json` + seed `phase_09_output_evaluation_integration.seed.yaml`; CLI **`second-brain retrieval output-eval run "<q>" [--source a,b] [--max-review-tier] [--min-confidence] [--mode] | proof --json`**. Live: `output-eval run` on the operator DB → `route='evaluation_only'`, `overall_passed=true` (10/10), 408 deterministic / 0 semantic (no applied index → semantic skipped), no persist; `output-eval proof` `proof_passed=true` (real A05 evaluation score 1.0; 0 unsupported / 0 unlinked; guard-clean metadata-only receipts in both V38 tables; unsupported-claim detection over synthetic items; no answer; raw query not emitted; excluded family fail-closed); a real `bge-small` smoke routed 3 advisory semantic + 3 deterministic items through the evaluation (score 1.0) without synthesizing. Operator DB pristine (both V38 tables **0 rows**, schema **V38**). 7 new tests (normal route / missing-policy / stale-schema fail-closed / unsafe-source detection + excluded-family fail-closed / no-raw+no-writeback+guard-clean receipts / proof clean / proof-artifact + an `integration` real-HF smoke). compileall/ruff clean; mypy **291** files; pytest **3166 passed / 0 failed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped (append-only ledger — disclosed Prompts 02/05), all other 08A/B/D gates + no-raw/no-writeback proofs pass. No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/` (`00`–`06-*` + `08-*`–`23-*` evidence + `.json`, `output-evaluation-integration-proof.{json,md}`, `research-packet-integration-proof.{json,md}`, `metadata-filter-proof.{json,md}`, `hybrid-retrieval-proof.{json,md}`, `vector-index-apply-proof.{json,md}`, `vector-index-dry-run-proof.{json,md}`, `reviewed-memory-loader-proof.{json,md}`, `approved-obsidian-loader-proof.{json,md}`, `approved-source-manifest-proof.{json,md}`, `embedding-vector-policy-no-raw-proof.{json,md}`, `source-linked-retrieval-proof.json`, `review-tier-preservation-proof.json`, `obsidian-linkage-proof.json`, `broken-link-classification.json`, `relationship-quality-mart.json`, `corpus-balance-mart.json`, `attached-audit-summary-companions/`, `validation-outputs*/`), `docs/runbooks/phase-09-retrieval-llamaindex-setup.md`, and `docs/architecture/120`–`142`. **Prompt 24 (retrieval quality eval set)** creates **source-linked retrieval evaluation cases from approved outputs**: a new `retrieval/eval_set.py` enumerates the approved Obsidian + reviewed-memory corpus (via `_gather_approved_nodes`) and builds **one source-linked eval case per approved node**, admitted iff it carries a `source_ref` + an allowlisted (non-excluded) `source_family`. Each case stores `expected_source_ref_hash` (hashed — **never the raw ref**), a `question_hash` (a deterministic query *seed* hash — no raw query text), and `confidence_class`; the eval set carries a hashed `name_hash`, `case_count`, a `review_tier` summary, and `status` (`built`/`empty`). Metadata-only receipts persist to the existing V38 `second_brain_retrieval_eval_sets` + `second_brain_retrieval_eval_cases` tables (all 23 `CHECK(=0)` guards 0) — **read-only by default** (`emit_receipt=False` persists nothing; proven on a temp DB). **No embeddings** are involved (pure metadata enumeration); review tier / confidence / hashed source refs / freshness are preserved; unsafe/unlinked/excluded-family nodes are excluded; **no migrator/schema change** (schema stays **V38**). Executing/scoring the set against the index (`eval_runs`) is a later prompt. A new contract `phase_09_retrieval_eval_set_contract.json` + seed `phase_09_retrieval_eval_set.seed.yaml`; CLI **`second-brain retrieval eval-set build [--project] [--name] | proof --json`**. Live: `eval-set build` on the operator DB → `status='empty'`, 0 cases (no approved outputs → honest `no_approved_outputs`), no persist; `eval-set proof` `proof_passed=true` (3 source-linked cases from a fixture; guard-clean metadata-only `eval_sets` + `eval_cases` receipts; unsafe/unlinked/excluded node excluded; no raw source ref emitted). Operator DB pristine (both V38 tables **0 rows**, schema **V38**). 7 new tests (normal builds source-linked cases / missing-policy / stale-schema fail-closed / unsafe-source excluded + all-unsafe empty / no-raw+no-writeback+guard-clean receipts / proof clean / proof-artifact). compileall/ruff clean; mypy **292** files; pytest **3173 passed / 0 failed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped (append-only ledger — disclosed Prompts 02/05), all other 08A/B/D gates + no-raw/no-writeback proofs pass. **Prompt 25 (deterministic vs semantic benchmark)** benchmarks the three retrieval modes — **deterministic** (authoritative), **semantic** (advisory), **hybrid** — against each other over the approved corpus and emits **comparative, bucketed, metadata-only** metrics: a new `retrieval/benchmark.py` runs the deterministic baseline once + one advisory `_semantic_query` per **runtime-only** probe (built from each approved node's already-redacted excerpt — never persisted/emitted) and writes seven `(metric, mode)` bands to the existing V38 `second_brain_retrieval_benchmark_runs` table (all 23 `CHECK(=0)` guards 0; `semantic_retrieval_bypassed_policy` 0). It is a **measurement leaf** — `assembles_final_answer=false`, semantic context never reaches an answer / Research Packet / Evaluation path — **read-only by default** (`emit_receipt=False` persists nothing) and **fail-closed** (semantic degrades to a `blocked` status without the SDK / applied index; the deterministic baseline is unaffected); **no migrator/schema change** (schema stays **V38**). A new contract `phase_09_retrieval_benchmark_contract.json` + seed `phase_09_retrieval_benchmark.seed.yaml`; CLI **`second-brain retrieval benchmark build [--project] [--name] | proof --json`**. Live: `benchmark build` on the operator DB → `status='empty'`, 0 probes (no approved outputs → honest `no_approved_outputs`), no persist; `benchmark proof` `proof_passed=true` (3 probes, all three modes compared, semantic available + floored tier 2, guard-clean metadata-only `benchmark_runs` receipts, blocked-semantic fail-closed path, unsafe nodes excluded; a real `bge-small` smoke compared all three modes at dim 384). Operator DB pristine (`benchmark_runs` **0 rows**, schema **V38**). 7 new tests (normal / missing-policy / stale-schema fail-closed / unsafe-source excluded + all-unsafe empty / no-raw+no-writeback+guard-clean receipts / proof clean / proof-artifact). compileall/ruff clean; mypy **293** files; pytest **3180 passed / 0 failed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped (append-only ledger — disclosed in Prompts 02/05), all other 08A/B/D gates + no-raw/no-writeback proofs pass. No stop condition. **Prompt 26 (project-specific retrieval benchmarks + coverage)** scopes the Prompt 25 benchmark **per project** and pairs each with a **coverage report**: a new `retrieval/project_benchmark.py` enumerates projects from the deterministic `RetrievalBroker` corpus and, per project, runs `build_retrieval_benchmark(project_key=P)` (advisory deterministic/semantic/hybrid) + the read-only `corpus_balance_mart` coverage report (per-family `covered`/`empty`/`deferred_no_reader`). It is an **orchestration leaf** — `assembles_final_answer=false`, the `semantic_retrieval_bypassed_policy` guard stays 0, semantic context never reaches an answer / Research Packet / Evaluation path. Per-project benchmark metrics reuse the existing V38 `second_brain_retrieval_benchmark_runs` table (project-distinct run ids; all 23 `CHECK(=0)` guards 0); coverage reports are **read-only** (never persisted); raw/excluded families never appear. **Read-only by default** (`emit_receipt=False` persists nothing); **no migrator/schema change** (schema stays **V38**). A new contract `phase_09_project_retrieval_benchmark_contract.json` + seed; CLI **`second-brain retrieval project-benchmark build [--project] [--name] | proof --json`**. Live: `project-benchmark build` on the operator DB → `status='built'`, **1 project** enumerated, **read-only, no persist** (that project's approved-node benchmark is honestly empty); `project-benchmark proof` `proof_passed=true` (2 projects ALPHA/BETA; per-project guard-clean `benchmark_runs` receipts; per-project coverage present; unsafe/excluded families excluded; no raw emitted; read-only default no-persist). Operator DB pristine (`benchmark_runs` **0 rows**, schema **V38**). 7 new tests (normal / missing-policy / stale-schema / unsafe-source-excluded-from-coverage / no-raw+no-writeback guard-clean / proof / proof-artifact). compileall/ruff clean; mypy **294** files; pytest **3194 passed / 0 failed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped (append-only ledger). `phase-08b-gates` exits 1 — a **pre-existing/environmental** `automation_execution_proof` assertion that **reproduces on a pristine checkout of clean HEAD `6c43844`** (operator Application-Support automation-state drift), unrelated to this change (which touches no automation code) and not introduced by it; documented in the evidence bundle. Architecture record renumbered **145 → 146** to avoid a doc-number collision with a concurrent in-flight prompt. No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/`**Prompt 27 (context budget optimization)** optimizes context packing while preserving source and warning metadata: the baseline `apply_context_budget` **breaks at the first overflowing item** (so a single large item can block smaller items behind it within the 24000-char budget), and a new `retrieval/context_budget.py` adds an **advisory best-effort packer** — same deterministic priority order + budget bounds, but it **skips an oversized item and continues**, recovering wasted budget. It **never exceeds** the budget, **preserves** every kept item's review tier / confidence / source ref / freshness, and **surfaces every drop** as a `budget_dropped:<family>` coverage warning (no silent loss). It is an **advisory leaf** — `assembles_final_answer=false`, the authoritative `apply_context_budget` is **unchanged** (consumed by `broker.py`/`hybrid_broker.py`; broker adoption deferred), and the builder **performs no DB writes**; **no migrator/schema change** (schema stays **V38**). A new contract `phase_09_context_budget_optimization_contract.json` + seed; CLI **`second-brain retrieval context-budget build [--project] | proof --json`**. Live: `context-budget build` on the operator DB → **2045 candidate items**, baseline kept **408** vs optimized **412** (**+4 source-linked items recovered**), within budget, metadata preserved, read-only (no writes); `context-budget proof` `proof_passed=true` (≥1 recovered, never exceeds budget, all metadata preserved, every drop warned, priority preserved, authoritative packer unchanged, build path no DB writes, no raw). 8 new tests (recovery+metadata / missing-policy / stale-schema / unsafe-source-denied / no-raw+no-writeback / all-oversized-never-exceeds / proof / proof-artifact). compileall/ruff clean; mypy **295** files; pytest **3202 passed / 0 failed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped; `phase-08b-gates` exits 1 — a **pre-existing/environmental** `automation_execution_proof` assertion that reproduces on a pristine checkout of clean HEAD `6c43844` (operator Application-Support automation-state drift), unrelated to this change (no automation code touched). Architecture record **147** (145 = the concurrent daily-brief prompt). No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/`**Prompt 28 (unsupported claim checks + review routing)** implements unsupported claim detection and review routing: a new `retrieval/unsupported_claim_checks.py` treats each retrieved item as a **claim** (supported iff it carries a source ref + an allowlisted non-excluded source family) and **routes unsupported claims to human review** — `review_required` (tier 3, with a reason code) so an unsupported claim is **never presented as fact**; a supported-but-review-flagged claim (tier 3 / stale / conflict) routes to `review_recommended`. It is **advisory only** — `assembles_final_answer=false`, `claim_determination_made=false`, and the `claim_or_entitlement_decision_performed` + `unsupported_claim_performed` guards stay 0 (it routes to a human, never decides a claim). **Read-only by default** (`emit_receipt=False` persists nothing); receipts go metadata-only + guard-clean to the existing V38 `second_brain_retrieval_unsupported_claim_checks` table (hashes, counts, review vocabulary, reasons — no raw claim text/source ref); **no migrator/schema change** (schema stays **V38**). A new contract `phase_09_unsupported_claim_checks_contract.json` + seed; CLI **`second-brain retrieval claim-checks build [--project] | proof --json`**. Live: `claim-checks build` on the operator DB → `status='clean'`, **408 claims, 0 unsupported** (all source-supported), read-only (no persist); `claim-checks proof` `proof_passed=true` (unsupported claim routed to `review_required`, flagged claim to `review_recommended`, no determination, guard-clean receipt, no raw). 7 new tests (detect+route / missing-policy / stale-schema / unsafe-source-routed / no-raw+no-writeback guard-clean / proof / proof-artifact). compileall/ruff clean; mypy **296** files; pytest **3209 passed / 0 failed**; `construction-agent validate` 4/4 schema **V38**; `table-inventory` 190 / 0 unmapped; `phase-08c-gates` deliberately skipped; `phase-08b-gates` exits 1 — a **pre-existing/environmental** `automation_execution_proof` assertion that reproduces on a pristine checkout of clean HEAD `6c43844`, unrelated to this change (no automation code touched). Architecture record **148**. No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/`No stop condition. See `docs/evidence/construction-intelligence-phase-09-retrieval-memory-quality/` (`00`–`06-*` + `08-*`–`28-*` evidence + `.json`, `unsupported-claim-checks-proof.{json,md}`, `context-budget-optimization-proof.{json,md}`, `project-retrieval-benchmark-proof.{json,md}`, `retrieval-benchmark-proof.{json,md}`, `retrieval-eval-set-proof.{json,md}`, `output-evaluation-integration-proof.{json,md}`, `research-packet-integration-proof.{json,md}`, `metadata-filter-proof.{json,md}`, `hybrid-retrieval-proof.{json,md}`, `vector-index-apply-proof.{json,md}`, `vector-index-dry-run-proof.{json,md}`, `reviewed-memory-loader-proof.{json,md}`, `approved-obsidian-loader-proof.{json,md}`, `approved-source-manifest-proof.{json,md}`, `embedding-vector-policy-no-raw-proof.{json,md}`, `source-linked-retrieval-proof.json`, `review-tier-preservation-proof.json`, `obsidian-linkage-proof.json`, `broken-link-classification.json`, `relationship-quality-mart.json`, `corpus-balance-mart.json`, `attached-audit-summary-companions/`, `validation-outputs*/`), `docs/runbooks/phase-09-retrieval-llamaindex-setup.md`, and `docs/architecture/120`–`148`.

## Quickstart (after clone)

```bash
# Use the phase-0 venv or create fresh
python3 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"

hb-assistant --version
hb-assistant --help
hb-assistant diagnostics env --json
```

## Key Paths (macOS)

- Application Support: `~/Library/Application Support/HB Personal Assistant/`
- Obsidian Vault: `/Users/bobbyfetting/Documents/Obsidian Vault/`

See `docs/architecture/` for implementation and remediation records, and `docs/plans/my-pa-phase-0/` for the original implementation package.

## Guardrails (Global)

- Delegated Bobby-user Microsoft Graph auth is the runtime default.
- Certificate-backed app-only is proof/admin only.
- **No** Microsoft 365 write-back.
- **No** tokens/keys/full bodies/PEMs logged or committed.
- Store auth/cache/SQLite/logs **outside** the repo.
- Dry-run before writes.
- Every output carries source traceability.

## Construction Intelligence Phase 02 Status

Phase 01 closed as a construction-intelligence scaffold / foundation. Phase 02 Prompts 00–10 landed corrective alignment across the source registry, the canonical V5 SQLite schema, folder-scoped Graph resolution + Hilltop linked-source discovery, the Tropical baseline-comparison primitive, the OneDrive inventory-first policy + PII review rules, the Procore project mapping (HB-number-shape rejected), the Obsidian output projection layer (redaction proof + `source_id` alias + 7-output guardrail fence), Ollama live-readiness, and the email-intelligence deferred-foundation policy.

- Phase 02 evidence: `docs/evidence/construction-intelligence-phase-02/` (12 files at closeout: Prompts 00–10 + this closeout).
- Local validation: 413 pytest passing; ruff clean across `src/hb_assistant/construction/`, `src/hb_assistant/procore/`, and the construction + procore CLI modules; `construction-agent validate --json` reports `ok` across schema / source_registry / review_rules / model_routing.
- Live external validation status: the **live SharePoint + OneDrive Graph delta crawl is now exercised** — a first-ever live end-to-end files-pipeline smoke (bounded ≤50 items/source over Tropical SharePoint + Business OneDrive) crawled 100 real driveItems into SQLite and wrote marker-bounded notes to the real Obsidian vault, with content-extract correctly skipping all sensitive/low-confidence items (0 downloads) and the no-writeback / no-secret proofs staying green; it also drove a delegated-scope minimization (drive sources no longer request the unconsented `Sites.Read.All`). See `docs/evidence/construction-intelligence-sharepoint-onedrive/23-live-end-to-end-smoke.md`. Live Procore OAuth, live Ollama daemon probe, and live mailbox metadata fetch remain pending — see *Remaining external validation* in the Repository Status block above. The granted-but-suppressed mailbox posture (Mail.ReadWrite.All consented at tenant level; `Mail.Read` is the runtime-requested scope) is documented at `docs/evidence/construction-intelligence-phase-02/10-email-intelligence-deferred-foundation.md`.

## Email Intelligence (Deferred)

`Mail.ReadWrite.All` has been granted at the tenant level but is intentionally suppressed for Phase 02. Mailbox writeback and full-body persistence are locked at four layers:

- **Pydantic policy** — `resources/config/email_intelligence_deferred_policy.yaml`, loaded via `hb_assistant.construction.policy.load_email_intelligence_deferred_policy()`, enforces `mailbox_writeback_allowed: false`, `persist_full_body: false`, and `review_required_for_sensitive: true` via `Literal[False]` / `Literal[True]` fields.
- **MSAL scope request** — `IdentityConfig.delegated_scopes` requests only `Mail.Read` at token acquisition. `Mail.ReadWrite.All` and `Mail.Send` are never asked for at runtime, even though the tenant has consented to the broader scope.
- **Python adapter** — `ConstructionStore.set_email_intelligence_deferred_state` raises if either locked flag is set to `True`.
- **SQLite** — V5 table `construction_email_intelligence_deferred_state` carries SQL `CHECK` constraints on the locked-false fields and a singleton `id = 1` row.

Full email-intelligence activation is deferred to a future phase.

## Validation & Evidence

Authoritative construction-intelligence evidence lives in two directories:

- `docs/evidence/construction-intelligence-phase-01/` — 12 files including session handoff and final closeout summary (Phase 01 scaffold).
- `docs/evidence/construction-intelligence-phase-02/` — 12 files at closeout (Phase 02 Prompts 00–10 + truthfulness closeout).
- `docs/evidence/construction-intelligence-phase-07c-document-intelligence/` — Prompts 00–13 + `phase-07d-08a-08b-handoff.md` (Phase 07C Document Intelligence Promotion closeout; arch docs `docs/architecture/30-42`).

Each Phase 02 prompt has a dedicated evidence artifact (e.g. `07-procore-mapping-correction-and-audit-readiness.md`, `08-obsidian-output-quality-proof.md`, `09-review-policy-and-ollama-live-readiness.md`, `10-email-intelligence-deferred-foundation.md`); per-prompt narratives live there rather than in this README.

## Historical Evidence

Earlier remediation-addendum and Phase 14 evidence remains in-tree at its original paths for audit continuity. Those records are historical and are superseded for current construction-intelligence work by the Phase 01 and Phase 02 evidence directories.

- `docs/evidence/remediation-addendum/`
- `docs/evidence/phase-14-local-runtime-workstream-intelligence/`
- `docs/evidence/remediation/remediation-baseline.md` (prior remediation baseline)
